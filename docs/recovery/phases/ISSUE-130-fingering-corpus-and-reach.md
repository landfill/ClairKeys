# ISSUE-130 — v2의 손 소모와 화음 도달 한계

Status: `IN_PROGRESS`
Depends on: ISSUE-126 2순위 (`phrase-dp-v2`가 이 단계의 수정 대상이다)

Progress (2026-09-05): **1단계 완료.** PR [#131](https://github.com/landfill/ClairKeys/pull/131)이 merge
commit `72627e9`로 병합되고 post-merge 체크가 전부 통과했다. CodeRabbit 리뷰 1건(화음 도달 검사가 인접
쌍만 본다)을 반영했고, 검증 과정에서 도달 한계 표가 어디서도 부가적이지 않다는 사실이 드러나 래칫이
23/132에서 **26/154**로 바뀌었다. 브랜치는 tip 포함 확인 후 삭제했다.

**2단계 구현 완료** (브랜치 `codex/issue-130-reach-table`). 도달 한계를 하드 제약으로 `eventCandidates`에
적용해 도달 불가 화음 쌍이 **26/154 → 0/154**가 됐다. 해부학 상수는 `src/utils/handReach.ts`로 옮겨
모델과 계측이 서로를 import하지 않고 둘 다 여기서 읽는다(D-043). `FINGERING_ALGORITHM_VERSION`은
`phrase-dp-v3`.

**진행 중 손 재배치는 65/171로 변하지 않았다** — 도달 한계는 화음에만 걸리고 이 지표는 단음 선율에서
측정되기 때문이다. 3단계(방향성 손가락 예산)가 그것을 다룬다.

## Objective

`phrase-dp-v2`가 실제 악보의 왼손에서 연주 불가능한 운지를 내는 두 기전을 제거한다. 첫째, 완전5도를
`5→1`로 건너뛰면 anchor가 같아 비용이 0이므로 **손 전체를 첫 걸음에 소모하고** 그 뒤로는 두 손가락을
왕복한다. 둘째, **손가락 쌍별 물리적 도달 한계가 없어** 화음에 닿지 않는 조합이 배정된다.

## Evidence

- 3마디 왼손 `E2 B2 G#3 B3 F#4 B3 G#3` → `5 1 2 1 2 1 2`. `E2`(5번, anchor 40)과 `B2`(1번, anchor 40)의
  손 이동이 0이다. `NATURAL_SPAN`이 정확히 7반음이기 때문이다.
- 왼손 29마디 중 19마디에서 음이 한 방향으로 가는데 손가락이 왕복한다.
- 화음 인접 쌍 132개 중 23개가 도달 불가(너그러운 한계 기준). `E4+E5`를 4·5번, `B2+A3`를 5·4번 등.
- 손 배정은 정상이다 — `staff 1→R` / `staff 2→L` 불일치 0건. 이 단계는 손가락만 다룬다.
- 이 악보에서 한 손이 동시에 두 성부를 내는 onset이 0개다. #126 1순위(`voice` 전달)는 이 결함을 못 고친다.

## Work stages

1. **corpus와 계측 래칫 (운영 코드 무변경).** 411음 운영 JSON을 `fixtures/fingering/`에 원본 바이트로
   보관하고, 세 지표를 계측 모듈로 만들어 현재 수치를 테스트에 고정한다. `npm run fingering:report`로
   사람이 읽을 수 있는 형태도 제공한다. (D-042)
2. **손가락 쌍별 도달 한계 표.** 후보를 쳐내는 하드 제약이므로 3단계보다 먼저다 — 3단계가 이미 물리적으로
   합법인 후보 집합 위에서만 동작하게 된다. 화음 후보와 전이 양쪽에 적용한다.
3. **방향성 손가락 예산.** 한 방향 진행 중 손 끝에 일찍 도달하는 데 비용을 매겨, 5도를 `5→1`로 건너뛰고
   손을 소모하는 경로가 최적해가 되지 않게 한다.
4. **도약 예외 재정의.** D-041이 정당한 예외로 문서화한 범위를 2·3단계 이후 실측해 다시 정한다.

## Completion criteria

- 이슈 #130의 GitHub 완료 조건을 모두 충족한다.
- 도달 불가 화음 쌍이 0이다 (현재 23/132).
- 정방향 진행 중 손 재배치가 크게 줄어든다 (현재 171 이벤트 중 65회). 목표는 악구 옥타브당 약 1회 수준이며,
  정확한 기준은 2·3단계 실측 후 이 문서에 고정한다.
- 3마디 왼손 아르페지오가 손을 한 걸음에 소모하지 않는다.
- 원본 `finger`는 한 음도 바뀌지 않고, 같은 입력은 같은 결과를 낸다.
- 12,000음 추론 시간이 `phrase-dp-v2` 대비 한 자릿수 배수 안에 머문다.
- 저장 canonical v1.0/v1.1 계약은 바꾸지 않는다.

## Out of scope

- #126 1순위(`keySignature`·`timeSignature`·`staff`·`voice` 전달)와 3순위(계약 확장).
- 손 배정 로직 — 이 악보에서 정상임을 확인했다. staff 정보가 없는 악보의 음역 기반 fallback 위험은 별도
  후보로 남긴다.
- 개인 손 크기별 최적화, 유일한 정답 운지 주장(D-039 유지).
