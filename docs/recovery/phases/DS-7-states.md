# DS-7 — 알림·빈 화면·오류 상태 완결

Status: `NOT_STARTED`
Depends on: **DS-2, DS-3, DS-4, DS-5, DS-6 전부**
Issue: [#76](https://github.com/landfill/ClairKeys/issues/76) 7단계

## Objective

각 단계가 만든 화면들의 빈 상태·오류 상태·복구 행동을 하나의 규칙으로 통일하고, 홈에서 첫 재생까지의
여정을 종단 검증한다. 이슈 #76을 닫을 수 있는지 판정하는 단계다.

## 왜 마지막인가

이 단계의 범위가 "**각** 핵심 화면의 빈 상태·오류 상태·복구 행동 통일"이다. 통일 대상 화면이 모두
확정되기 전에는 끝낼 수 없다. DS-3·DS-4만 선행 조건으로 두면 DS-2(홈·로그인)·DS-5(플레이어)·
DS-6(탐색·상세)의 화면이 나중에 들어오면서 통일이 다시 깨진다.

## In scope

- 완료 전달의 일관성 — **별도 알림 시스템을 만들지 않는다** (D-026 G1-3). 업로드 화면의 인라인
  완료와 내 악보의 파생 상태 배지가 같은 표현을 쓰는지 확인한다
- DS-2~DS-6이 확정한 모든 화면의 빈 상태·오류 상태·복구 행동 통일
- 오류 문구의 톤과 구조 통일 (무엇이 잘못됐는지 / 무엇을 하면 되는지)
- 종단 검증: 홈 → 업로드 → 페이지 이탈 → 완료 → 첫 재생

## Out of scope

- 각 화면의 주 레이아웃 (해당 단계 소유)
- 새 기능 추가
- DS0-1 (비공개 악보 public 버킷 노출) — DS 범위 밖

## 대상 화면과 소유 경계

| 화면 | 이 단계가 하는 것 | 하지 않는 것 |
|---|---|---|
| 홈 (DS-2) | 샘플 로딩 실패 상태 | 히어로·CTA 구성 |
| 로그인 (DS-2) | 인증 오류 문구 통일 | 이유 3가지·복귀 계약 |
| 업로드 (DS-3) | 오류 문구·복구 행동 통일 | 드롭존·상태 분리 |
| 내 악보 (DS-4) | 빈 상태 문구 통일 | 목록·상태 배지 구조 |
| 플레이어 (DS-5) | 데이터 로딩 실패 상태 | 컨트롤·기하 |
| 탐색·상세 (DS-6) | 결과 없음·접근 불가 상태 | 카드·퍼널 |

## 회귀 기준

**기능 회귀** — 앞 단계들이 고정한 모든 계약이 그대로다.

- 재생 기하 상수 7개와 D-019 가로 전환 조건식 (DS-5)
- `playback-chrome` (DS-1)
- 로그인 후 복귀 (DS-2), 비공개 악보 403 (DS-6)
- canonical 업로드 경로와 callback 저장 (DS-3)
- D-026의 상태 출처와 파생 상태 규칙 (DS-3, DS-4). `ProcessingJob`·`ProcessingNotification`에
  새 writer가 없다

**시각 회귀** — DS-1 토큰만 사용한다.

## 접근성·반응형 검증

- 모든 오류·빈 상태가 색상 외 수단을 동반한다.
- 상태 전환이 `aria-live`로 전달되고, 포커스가 사라지지 않는다.
- 전역 알림 진입점이 키보드로 도달 가능하다.
**WCAG AA 종단 판정** — DS-0 시점에 어떤 형태로도 실행된 적이 없다. 자동과 수동을 **둘 다** 한다.
axe 같은 도구는 WCAG 성공 기준의 일부만 기계적으로 판정하므로, 자동 검사 통과를 AA 준수로 읽지
않는다.

| 검사 | 대상 | 통과 기준 |
|---|---|---|
| 자동 (axe 등) | 전 화면 | 위반 0건 |
| 키보드 순회 | 전 화면 | 마우스 없이 모든 기능에 도달·조작 가능. 키보드 트랩 없음 |
| 포커스 가시성 | 전 화면 | 포커스된 요소가 항상 보인다. 순서가 시각 순서와 일치 |
| 200% 확대 | 전 화면 | 내용·기능 손실 없음. 가로 스크롤이 생기지 않음 |
| 명도 대비 | 전 화면 | 본문 4.5:1, 큰 텍스트 3:1 |
| 색상 외 구분 | 상태 표시 전부 | 색상만으로 구분되는 정보가 없음 |

## Completion criteria

- 각 화면의 빈 상태와 오류 상태가 같은 구조(무엇이 잘못됐는지 + 무엇을 하면 되는지)를 따른다.
- 완료 전달이 D-026 G1-3대로 두 지점(업로드 인라인, 내 악보 배지)에서 일관되게 이뤄진다.
  새 알림 시스템이나 `ProcessingNotification` writer가 추가되지 않았다.
- **종단 시나리오가 설명 없이 완주된다**: 로그아웃 상태 홈 진입 → 샘플 체험 → CTA → 로그인 →
  업로드 → 페이지 이탈 → 완료 확인 → 첫 재생. 수동 실행 기록을 남긴다.
- **WCAG AA 위반이 0건이다.** 위 표의 자동·수동 6개 검사를 전 화면에 실행하고 결과를 기록한다.
  **유예된 위반이 하나라도 남아 있으면 이 단계를 `DONE`으로 두지 않고 이슈 #76도 닫지 않는다.**
  위반을 남긴 채 종료해야 할 사정이 생기면 그것은 유예가 아니라 이슈 #76 완료 조건 7의 변경이므로,
  `DECISIONS.md`에 이유와 함께 기록한 뒤에만 닫는다.
- **이슈 #76 전체 완료 조건 8개**(ROADMAP)에 대한 판정이 기록되어 있다.
- DS-0의 "변경하지 않을 회귀 계약"이 종료 시점에도 그대로임을 확인했다.

## 검증 명령

```bash
npm run lint && npx tsc --noEmit && npm test && npm run test:e2e && npm run build
git diff origin/main -- src/utils/playbackGeometry.ts src/utils/pianoLayout.ts src/hooks/usePlaybackOrientation.ts
# ↑ DS 트랙 전체 범위에서 비어 있어야 한다 (DS-5가 D-019를 갱신한 경우는 예외로 기록)
npx @axe-core/cli <배포 프리뷰 URL>   # 또는 프로젝트가 채택한 동등 도구 — 위반 0건
```

수동 (자동 검사로 대체 불가):

1. 종단 시나리오를 시크릿 창에서 1회 완주하고 각 단계의 화면을 기록한다.
2. 전 화면을 키보드만으로 순회한다 — 트랩 없음, 포커스 항상 가시.
3. 브라우저 200% 확대에서 전 화면을 확인한다 — 내용·기능 손실 없음.
4. 상태를 표시하는 모든 요소가 색상 외 수단을 동반하는지 확인한다.

결과는 `docs/recovery/validation/`에 검사별 통과 여부와 함께 기록한다.
