# PR Review Log — PR #29

PR URL: https://github.com/landfill/ClairKeys/pull/29
Branch: `codex/chore-deploy-workflow`
Base: `main`
State: `OPEN` — review-ready (non-draft). Awaiting hosted CI and the user's explicit merge approval.
Last checked: 2026-07-25 KST
Closes: [#28](https://github.com/landfill/ClairKeys/issues/28)

## Scope

One commit `238cc22`:

- `.github/workflows/deploy.yml` — removes the `deploy`, `database-migrate`, `health-check`, and
  `notify` jobs; renames the workflow `Deploy` → `Post-merge checks` and its two surviving jobs to
  `Post-merge tests` / `Post-merge build`.
- `src/ci/__tests__/postMergeWorkflow.test.ts` — new, following the existing
  `src/ci/__tests__/prChecksWorkflow.test.ts` convention of asserting against workflow source.

## Why this exists

Every commit on `main` ended red because four jobs had never once succeeded. The repository has **no
secrets configured at all** — `gh secret list` and
`gh api repos/landfill/ClairKeys/environments/production/secrets` both return empty:

| Job | Failure |
|---|---|
| `Deploy to production` | `amondnet/vercel-action@v25` given an empty `secrets.VERCEL_TOKEN` |
| `Run database migrations` | `prisma migrate deploy` given an empty `DATABASE_URL` → Prisma `P1012` |
| `Notify deployment status` | its failure branch `exit 1`s when either of the above fails |
| `Post-deploy health check` | permanently `skipped` behind `needs: [deploy, database-migrate]` |

The permanent red is the point, not the cosmetics. When every commit fails identically, a real
failure carries no signal — which is why production served a pre-P0 bundle for days, with issue #18's
10-second audio cutoff reproducing live, while CI looked exactly as red as usual. That is recorded in
`docs/recovery/validation/2026-07-24-production-serves-pre-p0-bundle.md`.

## The rename, and why it is in scope

A workflow named `Deploy` that only runs tests is worse than no workflow: in the checks list, and in
the next session's reading of it, it presents as evidence that a merge shipped. That is precisely the
misreading this investigation existed to correct, so leaving the name would rebuild the trap.

New job names deliberately avoid `Run Tests` and `Build Check`, which are check names already owned by
`test.yml` and `pr-checks.yml`. `Run Tests` is one of the four required status checks, so a duplicate
name there would be actively dangerous.

## Left alone deliberately

The surviving `build` job still passes `secrets.DATABASE_URL`, `NEXTAUTH_SECRET`, `SUPABASE_URL`, and
`SUPABASE_ANON_KEY` to `npm run build`. All resolve to empty strings today and the job passes anyway,
so the build does not need them. That job was never failing and sits outside issue #28's scope. An
early draft of the test asserted their absence and failed — correctly, since the assertion was untrue.
The test file now records the reasoning inline instead.

## Regression evidence (established before the change)

| Workflow state | `postMergeWorkflow.test.ts` |
|---|---|
| Pre-change (`git checkout HEAD -- .github/workflows/deploy.yml`) | **3 of 5 fail** — workflow name, the four jobs, the Vercel credentials |
| Post-change | 5 of 5 pass |

The two that pass in both states (`still validates the merge commit`, `leaves every job reachable`)
are intentional: they guard what must survive the removal, not what it changes.

## Verification (local, before push)

| Check | Result |
|---|---|
| `npm test` | 40 suites / 367 tests pass (was 39 / 362 before the new file) |
| `npx tsc --noEmit` | exit 0, clean |
| `npm run lint` | "No ESLint warnings or errors" |
| Dangling job references | none — the only surviving `needs:` is `build: needs: test` |

## Not verified

- The renamed workflow has not run on GitHub, so the new check names `Post-merge tests` /
  `Post-merge build` are unobserved until this merges.
- `npm run build` was not re-run for this change; it touches no application code.

## What this does not fix

`main` still will not deploy itself after this lands. The cause is now **confirmed** rather than
inferred: the user opened the Vercel dashboard and found Production **Branch Tracking set to
`master`** ("Every commit pushed to the `master` branch will create a Production Deployment"). GitHub
has had no `master` since the DOC-1 rename `643ce71` (2026-07-19), so every `main` push builds as
Preview only. This single setting explains all of it: zero Production-environment deployments,
`clairkeys.vercel.app` frozen on a pre-rename build, and PRs showing a green `Vercel` check that only
ever meant the Preview build succeeded. Changing that field to `main` is a dashboard action, tracked
in #28.

## CI and review status

Pending at the time of writing. To be updated as hosted checks report.
