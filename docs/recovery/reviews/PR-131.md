# PR Review Log — PR #131

PR URL: https://github.com/landfill/ClairKeys/pull/131
Branch: `codex/issue-130-fingering-corpus-metrics` (head `355c317`)
Issue: [#130](https://github.com/landfill/ClairKeys/issues/130) 1단계 (corpus + 계측 래칫)
Last checked: 2026-09-05 KST — **MERGED** (merge commit `72627e9`)

## 목적

운영 코드를 바꾸지 않고, 411음 운영 악보를 저장소에 넣어 `phrase-dp-v2`의 결함을 숫자로 고정한다.
이후 2~4단계(도달 한계 표 → 방향성 손가락 예산 → 도약 예외 재정의)가 이 숫자를 근거로 진행된다.

## 왜 1단계가 먼저인가

같은 악보가 #120·#126·#130에서 세 번 분석 대상이 됐는데 저장소에 남은 것이 없다. #126의 "411음 운영 JSON
수동 검토" 완료 조건은 원본 부재를 이유로 세 세션 연속 이월됐다. corpus를 먼저 넣으면 이후 모든 단계가
검증 가능해지고, 운영 코드를 안 건드리므로 리뷰 위험이 없다.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| 전체 (`06ebfd9`) | pass | `gh pr checks 131` 전 항목 pass |
| CodeRabbit (`06ebfd9`) | Review completed | Major 1건 + docstring 경고 1건 — Iteration 2에서 처리 |
| `355c317` | 아래 Iteration 2에 기록 | |

로컬 검증은 전부 통과: 전체 Jest 879건(신규 8건), `tsc --noEmit`, `next lint`, `next build`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | self | `fixtures/fingering/love-affair-411.json` | 저작권 정책상 보관해도 되는가 | 사용자가 2026-09-05에 명시적으로 확인 — 금지 대상은 PDF뿐. D-040 결정 2와 일치 | CLOSED | D-042 Context |
| R2 | self | 같은 파일 | 재포맷하면 provenance가 깨진다 | 다운로드 바이트 그대로 저장 | CLOSED | `cmp` 확인, README에 sha256 |
| R3 | self | `scripts/lib/fingeringMetrics.ts` | 계측이 비용 모델을 import하면 안 된다 | import 없음. 공유 전제는 해부학(`NATURAL_SPAN`)뿐 | CLOSED | D-042 결정 2 |
| R4 | self | 도달 한계 표 | Parncutt et al. (1997) 수치를 인용할 것인가 | 인용하지 않는다 — 실제 문헌을 확인하지 않았다. 너그러운 자체 기준임을 주석에 명시 | CLOSED | 모듈 주석 |
| R5 | self | `fingeringCorpus.test.ts` | 목표값(0)을 단언할 것인가 | 아니다 — CI가 빨간 채로 남아 다음 회귀를 가린다. 현재값 래칫이 같은 압력을 주면서 초록을 유지 | CLOSED | D-042 Rejected 4 |
| R6 | self | `scripts/lib/` 위치 | `src/**/__tests__/**`는 전부 testMatch에 걸려 헬퍼를 못 둔다 | 도구이므로 `scripts/`에 두고 테스트가 상대 경로로 import | CLOSED | `jest.config.js` testMatch 확인 |
| R7 | self | `scripts/fingering-report.ts` | 초기 구현이 source fingering을 411개로 셌다 | `finger: null`이 `!== undefined`에 걸린 것. 모델은 `isValidFinger`로 정확히 걸러내고 있어 리포트만 수정 | FIXED | `06ebfd9` |
| R8 | CodeRabbit | `scripts/lib/fingeringMetrics.ts:130` | 화음 도달 검사가 인접 음 쌍만 본다 — 3음 이상에서 바깥 쌍 위반을 놓친다 (Major) | ACCEPTED. 검증해 보니 지적보다 넓다: 표가 **어디서도 부가적이지 않아**(3중 조합 10/10에서 `MAX[a-b]+MAX[b-c] > MAX[a-c]`, 차이 2~5반음) 모든 3음 화음에서 놓칠 수 있다. corpus에서 실제 3건 누락 | FIXED | `355c317`, 인접 전용 주입 재현 |
| R9 | CodeRabbit | pre-merge check | docstring 커버리지 33.33% < 80% | ACCEPTED — `measureFingering`·`handEvents`·`report`·`noteName`·`loadCorpusScore`에 실제로 정보를 담은 docstring 추가 | FIXED | `355c317` |

## Iteration log

### Iteration 1 — 생성 (2026-09-05)

- Feedback fetched: 아직 없음 (PR 생성 직후)
- Changes made: `06ebfd9` — corpus 1곡, 계측 모듈, 래칫 테스트 8건, 리포트 스크립트, D-042, 단계 문서
- 고정한 수치: 도달 불가 화음 쌍 **23/132**, 정방향 진행 중 손 재배치 **65/171 이벤트**,
  같은 손가락 3연속 **2건**(한 건은 실제 반복음 B2 B2 B2). 3마디 왼손 `5 1 2 1 2 1 2`도 정확히 고정.
- Validation: 전체 Jest 879건, `tsc --noEmit` exit 0, lint 경고 0, build 성공
- Remaining actionable items: CodeRabbit 리뷰 미실시.

### Iteration 2 — CodeRabbit 리뷰 (2026-09-05)

- Feedback fetched: CodeRabbit 자동 리뷰 (PR #129에서는 "10 star 미만 OSS repo"로 건너뛰었으나 이번에는
  실행됐다). 인라인 Major 1건, pre-merge docstring 경고 1건.
- **지적을 검증했더니 지적보다 넓은 문제였다.** 도달 한계 표의 부가성을 직접 계산한 결과, 손가락 3중 조합
  10개 **전부**에서 `MAX[a-b] + MAX[b-c] > MAX[a-c]`이며 차이가 2~5반음이다. 즉 인접 검사 누락은 특정
  코너 케이스가 아니라 **모든 3음 화음**에 해당한다.
- corpus에서 실제로 3건을 놓치고 있었고 전부 오른손 3화음의 바깥 쌍이었다: m11 `B4:2 + B5:5`(12>10),
  m14 `G#4:2 + F5:4`(9>7), m24 `G#4:2 + G#5:4`(12>7).
- Changes made: `355c317` — 모든 음 쌍 검사, 래칫 **23/132 → 26/154**, 성질 기반 회귀 1건 추가, 표가
  부가적이지 않다는 사실을 모듈 주석과 D-042 결정 5에 기록, docstring 추가.
- 회귀는 corpus 카운트가 아니라 성질로 썼다 — `C4-A#4-D#5`를 `1-2-3`으로 짚으면 인접 쌍은 한계에 정확히
  걸치고 바깥 1-3만 15반음으로 한계 12를 넘는다. 인접 전용 구현을 사본에 주입해 이 테스트가 그것을 잡음을
  확인했다.
- Validation: Jest **880건**, `tsc --noEmit` exit 0, lint 경고 0, build 성공.
- CodeRabbit 인라인 코멘트에 수정 내용과 측정 근거를 답글로 남겼다.
- Remaining actionable items: 없음. 사용자의 명시적 병합 승인 대기.

### Iteration 3 — 병합 (2026-09-05)

- 사용자의 명시적 승인 후 재확인: `mergeStateStatus` CLEAN, MERGEABLE, Draft 아님, 전 체크 pass,
  미해결 review thread 0건(CodeRabbit 스레드는 resolved). PR 제목·본문의 auto-close 패턴도 재확인했다.
- `gh pr merge 131 --merge` → merge commit `72627e9`. 병합 후 이슈 #130·#126 모두 OPEN 유지 확인.
- `main` post-merge 체크 전부 success: E2E, Post-merge build, Run Tests, Lint, Security Audit,
  Post-merge tests.
- `main`에 corpus가 반영되고 sha256이 보관 시점과 동일함을 확인했다
  (`97ff181dda5ee4835afb030e7bcaac0fc81f8b2068725d8d6e4ca5e2f5bcb8b7`). 래칫 테스트 9건 통과.
- 브랜치 정리: 로컬·원격 tip 모두 `355c317`로 동일, `main` 포함 확인, 고유 커밋 없음 → 양쪽 삭제.

## 병합 후 남는 것

- 2단계(손가락 쌍별 도달 한계 표) → 3단계(방향성 손가락 예산) → 4단계(도약 예외 재정의). 각각 별도 PR이며
  래칫 숫자를 의도적으로 갱신한다.
- 이 PR은 이슈 #130을 닫지 않는다.
