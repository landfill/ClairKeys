# PR #113 review log

Date: 2026-09-02 KST
PR: [마이페이지가 아는 것만 말하게 하고 공통 앱 셸에 맞춘다 (이슈 #104)](https://github.com/landfill/ClairKeys/pull/113)
Branch: `codex/issue-104-profile-shell`
Head: `93bda76`
Last checked: 2026-09-02 KST (생성 직후)

## Current state

- Review-ready PR created for 이슈 [#104](https://github.com/landfill/ClairKeys/issues/104). Not a draft.
- Two Lore commits: 회귀 `bd6f001`(수정 전 10 failed / 2 passed 관측) → 구현 `93bda76`.
- 사용자가 착수 전 결정했다: 동작하지 않는 컨트롤 7개는 **"거짓말하는 것부터 제거"**. 가입일은 항목
  제거가 아니라 실제 값 읽기로 정했다.
- 제거한 기능과 선행 조건은 후속 이슈 [#112](https://github.com/landfill/ClairKeys/issues/112)에 분리했다
  (이슈 #104 작업 범위 마지막 항목의 요구).
- **DECISIONS.md 신규 항목은 없다.** 이 PR은 기존 결정을 바꾸지 않는다 — DS-3이 세운 "화면이 아는 것만
  말한다"와 D-026의 파생 상태 원칙을 `/profile`에 적용할 뿐이다.

## 리뷰어가 볼 만한 지점

1. **범위가 이슈 본문보다 넓다 — 삭제가 포함된다.** 이슈는 "정렬"을 요구하지만 컨트롤 7개 중 동작하는
   것이 0개였다. 시각만 고치면 가짜가 더 진짜처럼 보이므로 허구를 먼저 걷어냈다. 특히 **계정 삭제**는
   눌러도 아무 일이 없어 "삭제했다"고 믿게 만들고, **가입일 `"2024년 1월 1일"`** 은 거짓이었으며,
   **비밀번호 변경**은 OAuth 전용 앱에 존재할 수 없는 항목이다.
2. **`ProfileSettings.tsx`를 재스타일링이 아니라 삭제했다.** 목업을 걷어내면 남는 내용이 없었고 다른
   import가 0개다. 되살릴 필요가 생기면 #112의 선행 조건부터다.
3. **새 라우트의 권한 경계.** `GET /api/user/profile`은 id 파라미터가 없어 호출자가 자기 행만 읽는다.
   회귀가 이를 고정한다(익명 401, 세션 id로만 `findUnique`, 404, 500에 DB 오류 미노출).
4. **셸 검사가 vacuous해지지 않게 고쳤다.** h1 개수 검사는 옛 페이지도 통과시켰다(h1을 이미 가짐).
   `MainLayout`이 만드는 `<main>` 랜드마크를 보도록 바꿔야 의도대로 실패했다.
5. **아바타 테스트가 실제 결함을 찾았다.** `OptimizedImage`가 기본 lazy라 페이지 최상단 요소가 스크롤을
   기다리고 있었다. 첫 구현은 Jest 전체가 통과하는데 `tsc`만 실패했다 — 그 경로를 어떤 테스트도 타지
   않았다는 뜻이었다.

## 검증

- 로컬: `npx jest` 87 suites / **813 tests**(직전 85 / 796, 신규 17), `npx tsc --noEmit` exit 0,
  `npm run lint` 무경고, `npm run build` 성공.
- 근거 문서: `docs/recovery/validation/2026-09-02-issue-104-profile-shell.md`.
- **미검증**: 브라우저·실기기 확인이 **전혀 없다**. 이슈 완료 조건 4(1440·1024·390), 5(긴 이름·긴
  이메일·이미지 없음), 7(인접 화면 비교)은 미충족이며 `/profile`은 인증 뒤 화면이라 공개 E2E·axe가
  지나가지 않는다. 새 라우트에 실제 요청을 보낸 적도 없다(목 기반).

## Iterations

### Iteration 1 — PR 생성 (2026-09-02)

Hosted CI 결과 대기. CodeRabbit은 이 저장소에서 OSS 정책으로 자동 리뷰를 건너뛰므로 수동 요청이
필요하고, PR #111에서는 수동 요청도 `Review rate limited`로 끝났다. 같은 결과면 PR #68·#109·#111
선례대로 독립 리뷰(`/code-review 113 high`)로 대체한다.

### Iteration 2 — 리뷰 두 갈래 (2026-09-02)

Hosted CI 16/16 pass. **이번엔 CodeRabbit이 실제로 리뷰를 실행했다** — PR #111에서 두 번 다 건너뛴 것과
다르다(수동 `@coderabbitai review` 요청 후 `Review in progress` → 인라인 스레드 1건). 독립 리뷰도
함께 돌렸다.

**독립 리뷰를 두 번 돌렸다.** 첫 실행(`/code-review 113 high`)은 인자가 `low effort → 1 diff pass →
no verify → ≤4 findings`로 잡혀 테스트 파일을 건너뛴 채 `(none)`을 냈다. 결과만 보면 "결함 없음"이지만
**의도한 강도가 아니었다** — 출력의 프롬프트를 직접 읽어서 알았다. 인자 순서를 바꿔(`high 113`)
재실행하니 4건이 나왔다. 지적 0건과 리뷰 없음이 다른 상태인 것처럼, **얕게 돈 리뷰도 세 번째 상태다.**

| # | 출처 | 지적 | 판정 | 처리 |
|---|---|---|---|---|
| R1 | 독립 | `page.tsx` — `OptimizedImage`가 className을 wrapper div에 붙이는데 `overflow-hidden`이 없어 `rounded-full`이 아무것도 자르지 않고 `object-cover`가 `<img>`에 닿지 않는다. **아바타가 사각형으로 렌더**되며 삭제한 `ProfileSettings` 대비 시각 회귀 | **ACCEPT** | `405d877` |
| R2 | 독립 + **CodeRabbit** | 가입일 테스트가 타임존 의존. fixture가 `T00:00:00Z`라 UTC 서쪽에서 전날로 렌더돼 `TZ=America/Los_Angeles`에서 실패 | **ACCEPT (동작은 유지)** | `405d877`, `abd1109` |
| R3 | 독립 | `OptimizedImage`가 뷰포트 `sizes`를 하드코딩하고 `w=3840`까지 srcset을 낸다. 80px 아바타에 폰 너비 변형을 `priority`로 preload | **ACCEPT** | `405d877` |
| R4 | 독립 | 루트 레이아웃(`layout.tsx:100`)이 이미 `<main>`을 렌더하는데 `MainLayout`이 또 렌더해 **중첩 랜드마크**. 이 PR이 도입한 것은 아니며 `library`·`explore`·`upload`·`sheet/[id]`도 같다. 따라서 셸 회귀는 실제 DOM이 아니라 **페이지 컴포넌트를 서술한다** | **ACCEPT (분리)** | 이슈 [#114](https://github.com/landfill/ClairKeys/issues/114), 테스트 주석 정정 |
| R5 | CodeRabbit | R2의 처방으로 `formatJoinDate`에 `timeZone: 'UTC'`를 고정하라 | **REJECT** | `abd1109` |

**R1·R3는 뿌리가 같았다.** 80px 고정 아바타를 반응형 이미지 도구에 넣은 것이 원인이고, 처음 만난
lazy 문제(페이지 최상단 요소가 스크롤을 기다림)도 같은 뿌리였다. 증상 셋에 원인 하나 — `next/image`로
되돌려 한꺼번에 해소했다. 삭제한 `ProfileSettings`가 원래 쓰던 것이다.

**R5는 기각했다.** `createdAt`이 UTC 저장인 것은 맞지만 사용자가 인식하는 "가입한 날"은 자기 달력
기준이다. KST 08:00 가입은 UTC로 전날 23:00이므로, UTC로 포맷하면 **하루 전 날짜를 보여준다** — 이
화면이 걷어내려던 종류의 작은 거짓을 새로 넣는 셈이다. 의존성은 동작이 아니라 fixture에 있었고 그쪽을
고쳤다(정오 값 + 구현과 같은 `Intl` 설정으로 기대값 계산).

**기각 근거가 검사되는지 확인했다.** 제안대로 `timeZone: 'UTC'`를 임시로 넣으면 새 회귀가
`TZ=Asia/Seoul`에서 1건 실패하고, 빼면 UTC·Asia/Seoul·America/Los_Angeles·Pacific/Kiritimati 네 곳에서
모두 통과한다. 포맷터는 `src/lib/profile/joinDate.ts`로 분리했다 — Next.js 페이지 모듈은 default 외
export를 허용하지 않으며, DS-3이 업로드 계약에 한 분리와 같은 방식이다. 기각 근거는 docstring과 커밋
`Directive`에 남기고 CodeRabbit 스레드에 답글로 달았다.

**R4는 이 PR에서 고치지 않는다.** 저장소 전역 패턴(5개 페이지)이라 여기서 고치면 범위가 넘치고, 회귀도
루트 레이아웃을 포함해 렌더하거나 E2E여야 한다. 이슈 #114로 분리하고 테스트 주석에 "이 단언은 페이지
컴포넌트를 서술하지 프로덕션 DOM을 서술하지 않는다"를 명시했다 — **PR 본문의 "vacuous 검사를 피했다"는
주장이 부분적으로 과했다.**

수정 후 재검증: Jest 87 suites / **815 tests**, tsc exit 0, lint 무경고, build 성공. 프로필 스위트는
네 타임존에서 각각 통과. 새 head `abd1109`, hosted CI 16/16 pass, `MERGEABLE`/`CLEAN`.
