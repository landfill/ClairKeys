# PR #79 — 미리 보이는 시간에 2.5초 상한, 남는 세로는 건반 비율에

- URL: https://github.com/landfill/ClairKeys/pull/79
- Branch: `codex/p1-playback-lookahead-cap` → `main`
- Created: 2026-08-29
- Last checked: 2026-08-29 (생성 직후)
- Scope: 사용자 관찰(폰에서 낙하 영역이 줄어든 쪽이 더 어울린다)에서 출발한 세로 기하 결함
- Records: **D-021** (D-020 Directive 이행)

## 커밋

| SHA | 내용 |
|---|---|
| `a0980fc` | 회귀 테스트 단독 — suite 실행 실패 (`src/utils/playbackGeometry` 미존재) |
| `4bb2640` | `planPlaybackGeometry` + 측정 래퍼 분리 + D-021 |

## 결함

`pxPerSec = 140`은 고정인데 낙하 영역이 "남는 높이 전부"라, **미리 보이는 시간이 기기마다
달랐다**: 폰 가로(주소창 표시) 약 1.1s, 데스크톱 1470×746 **4.0s**(실측), 높이 1440 모니터
약 8.9s. 같은 악보를 두 기기에서 연습하면 같은 연습이 아니다.

`useFallingNotesPlayer`의 `lookAheadSec = 1.5`는 유휴 박스 높이에만 쓰이고 재생 중에는 어디에도
쓰이지 않았다. 사용자가 폰에서 좋게 느낀 값이 바로 그 선언값 근처였다.

## 접근

`pxPerSec`는 고정(손이 익히는 값)하고 낙하 영역에 **초 단위 상한** 2.5s(350px)를 둔다. 남는
세로는 **건반**이 `keyWidth × 6.3`(실제 백건 약 23×145mm)까지 가져가되 **낙하 영역이 쓰지 않을
높이만** 쓴다 — 이 순서가 폰을 보호한다(남는 높이가 음수라 건반이 120px 바닥에 머문다).

**측정과 크기 결정을 분리했다.** 박스에 상한을 걸면 "박스 높이 → 측정값 → 상한 → 박스 높이"
피드백 루프가 생기므로, 바깥 래퍼가 가용 높이를 갖고(`ResizeObserver` 대상) 박스는 계산된 픽셀
높이를 갖는다. PR #74에서 없앤 래퍼와 다른 점은 **백분율이 아니라 픽셀**이라는 것이고, 회귀
테스트를 "박스는 픽셀 높이를 가질 수 있으나 백분율은 절대 안 된다"로 갱신해 원래 결함을 계속
지킨다.

## 실측 (로컬 `/test-finger`, 가용 562px)

| | 이전 | PR #79 |
|---|---:|---:|
| 낙하 영역 | 440px (3.14s) | **350px (정확히 2.5s)** |
| 건반 높이 | 120px | **154px** |
| 건반 비율 (폭 24.46px) | 1 : 4.9 | **1 : 6.30** |
| 박스 | 562px | 506px (= 350+154+2) |
| 남는 세로 | 0 | 56px 여백 |

## 검증

| 명령 | 결과 |
|---|---|
| `npx jest ... playbackGeometry.test.ts` (`a0980fc`) | suite 실행 실패 — 모듈 부재 |
| `npm test` (`4bb2640`) | 58 suites / 569 tests passed |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | No ESLint warnings or errors |
| `npm run build` | 성공 |

## 계측 함정 2건 — 다음 세션 주의

1. **서비스 워커.** 이 앱은 PWA이고 `localhost:3000`에 `sw.js`가 등록된다. 소스를 고쳐도 워커가
   캐시된 셸을 돌려주어 **바뀐 DOM이 안 보인다.** `.next` 삭제와 dev 서버 재시작으로도 풀리지
   않았고, `navigator.serviceWorker.getRegistrations()` 해제 + `caches.delete()`가 필요했다.
   증상은 "번들에는 새 코드가 있는데 DOM에는 없다"이다.
2. **백그라운드 탭의 `ResizeObserver`** (PR #77에서 기록). 크기를 바꾼 뒤 반드시 한 번
   렌더시키고 읽는다.

## 검증하지 못한 것

- 실기기 없음. 폰 가로에서 이전과 동일하다는 것은 **단위 테스트로만** 확인했다.
- `lookAheadSec`(1.5s)와 `MAX_LOOK_AHEAD_SEC`(2.5s)가 여전히 별개다. 하나로 합치는 것은 별도 과제.

## CI status

생성 직후 미확인 — 다음 갱신에 기록한다.
