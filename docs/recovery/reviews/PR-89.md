# PR Review Log — PR #89

PR URL: https://github.com/landfill/ClairKeys/pull/89
Branch: `codex/ds-1-design-foundation`
Last checked: 2026-08-29 KST

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Run Tests / Unit Tests | pass | `cb8a74b` |
| E2E Tests (×2) | pass | `cb8a74b` |
| Lint / Lint and Type Check | pass | `cb8a74b` |
| Build Check | pass | `cb8a74b` |
| **Accessibility Check** | pass | `cb8a74b` |
| CodeQL / Security Scan / Security Audit | pass | `cb8a74b` |
| Vercel | pass | Preview 배포 완료 |
| CodeRabbit | pass | actionable 1건 (아래 R1) |

코드 변경 PR이라 문서 PR에서 `skipping`되던 잡(Build Check, Accessibility Check, CodeQL,
Security Scan, Unit Tests)까지 전부 실행됐다.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | CodeRabbit (스레드) | `src/middleware.ts:15` | `startsWith('/admin')`이 `/administrator`·`/admin-tools`도 보호 경로로 잡는다. 세그먼트 경계로 판정하라 | **accept** | RESOLVED | `d9c1c92` |
| R2 | CodeRabbit (outside diff) | `src/app/globals.css:113` | `.playback-controls .slider:focus { outline: none }`의 specificity가 전역 `:focus-visible`보다 높아 재생 슬라이더의 키보드 포커스 표시가 사라진다 | **accept** | DONE | `5455810` |
| R3 | CodeRabbit (nitpick) | `src/components/ui/icons.tsx:13` | `IconProps`를 export하라 | **accept** | DONE | `5455810` |
| R4 | CodeRabbit (check warning) | — | Docstring Coverage 20% (기준 80%, 이 diff가 건드린 15개 함수 기준) | **accept** | DONE | `448f894` |

**수집 누락 기록**: 처음에는 `reviewThreads`만 조회해 R1만 발견했다. R2·R3는 **리뷰 본문**
(`pulls/89/reviews[].body`)의 접힌 블록에, R4는 **walkthrough 코멘트**(`issues/89/comments`)에
있었다. 리뷰 피드백은 세 표면을 모두 봐야 한다 — 리뷰 스레드, 리뷰 본문, 일반 코멘트.

## 병합

- 2026-08-29 — 사용자의 명시적 승인으로 merge commit `fc0978245200c207e96988b126d3855df6e10c53`로
  병합했다. 최종 head `448f894`(커밋 6개), 미해결 리뷰 스레드 0건, `mergeStateStatus: CLEAN`.
  로컬·원격 작업 브랜치 tip이 main에 포함됨을 확인한 뒤 양쪽을 삭제했다.

## Iteration log

### Iteration 1

- Feedback fetched: 없음 (PR 생성 직후)
- Changes made: 커밋 3개
  - `3fe3c12` 회귀 근거 — 셸 계약 테스트 (이 시점 3 pass / 6 fail, 의도된 실패)
  - `e9aa371` **A. 시각 변경** — 토큰 정의, 죽은 다크 블록 제거, Geist 복구, 전역 포커스 링,
    Footer 죽은 링크·저작권, 이모지 → 인라인 SVG 아이콘
  - `cb8a74b` **B. 도달 경로 변경** — 내비 3개, `/processing` 제거,
    `ProcessingStatusIndicator` 제거, 고아 라우트 7개 제거, `/admin` 격리
- Validation: 명도 대비 19/19 통과(최소 3.48), lint 무경고, `tsc` 통과,
  Jest 63 suites / 603 tests 전부 통과, `npm run build` 성공, E2E chromium 3/3.
  기록: `docs/recovery/validation/2026-08-29-ds1-token-contrast.md`
- Commit: `cb8a74b` (head)
- Remaining actionable items: CI 결과 확인, 사용자의 명시적 병합 승인 대기

### Iteration 2

- Feedback fetched: 없음
- Changes made: 코드 변경 없음. 배포 프리뷰(`cb8a74b`)에서 **변경 후 라우트 응답 코드를 실측**해
  검증 기록에 채웠다
- Validation: **제품 라우트 7개 전부 변경 전과 동일**(`/` `/explore` `/auth/signin` `/auth/error`
  200, `/upload` `/library` `/profile` redirect). 고아 7개는 200 → **404**,
  `/admin/update-finger-data`는 200 → **redirect**. 전부 의도한 방향이다.
  Vercel 배포 보호로 익명 `curl`이 302를 받아, 사용자의 브라우저 세션에서 측정했다
- Commit: 문서만 (`main` 직접 커밋)
- Remaining actionable items: 사용자의 명시적 병합 승인 대기
