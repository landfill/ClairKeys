# PR #69 — 폐기된 Fly 배포 표면 제거

- URL: https://github.com/landfill/ClairKeys/pull/69
- Branch: `codex/ops-remove-fly-artifacts` → `main`
- Created: 2026-08-28
- Head: `afa5a0a`
- State: `OPEN`, draft=false, `MERGEABLE`

## 목적과 범위

실제 OMR 런타임은 NAVER Cloud VM의 podman/systemd인데도 배포되지 않은
Fly 설정·절차·현재형 설명이 함께 남아 리뷰와 운영 판단을 오도했다.

- `omr-service/fly.toml`과 README의 Fly 배포 절차 제거
- 활성 배포 계약을 `omr-service/deploy/` 하나로 고정하는 회귀 테스트
- AGENTS, 서비스 주석, 테스트 설명을 실제 FastAPI/NAVER VM 기준으로 정정
- D-008을 D-012에 의해 superseded된 역사 항목으로 축약
- 과거 리뷰·검증 문서는 당시 사실의 증거로 보존

## 커밋

| SHA | 내용 |
|---|---|
| `c674a72` | 회귀 테스트 단독. 정리 전 main에서 `fly.toml` 존재 때문에 실패 |
| `afa5a0a` | 폐기 설정·절차 제거, 현재 배포 설명과 D-008 정정 |

## 검증

- Python 34 tests passed
- focused Jest 3 suites / 33 tests passed
- full Jest 54 suites / 515 tests passed
- production build, `tsc`, lint passed
- 활성 배포 표면의 Fly 참조는 회귀 테스트의 금지 파일명 외 0건

## CI / review

Head `afa5a0a`의 hosted checks **17/17 성공**, 실패·대기 0건.

- 두 E2E job, unit, build, type/lint, accessibility, security audit/scan, CodeQL 성공
- Vercel preview와 preview comment check 성공
- `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`
- CodeRabbit은 자동 리뷰를 skip했고 `@coderabbitai review` 수동 요청도 `Review rate limited`로
  끝나 실제 리뷰는 없으며, GitHub review object와 unresolved thread도 0건

사용자의 명시적 승인 전에는 병합하지 않는다.

## Review items

| ID | Source | Summary | Decision | Status | Evidence |
|---|---|---|---|---|---|
| R1 | PR #68 수동 리뷰 후속 | 폐기 설정이 라이브 설정으로 오인됨 | ACCEPT | FIXED | `c674a72`, `afa5a0a`; PR #68 R2는 REJECTED로 정정 |
| R2 | Claude Sonnet 5 독립 리뷰 | 회귀 테스트가 `fly.toml` 부재와 podman unit은 고정하지만 README 산문에 과거 배포 절차가 다시 들어오는 것까지 검사하지 않음 | NO_CHANGE | REJECTED | 산문 전체를 문자열 테스트로 고정하는 것은 취약하고, 실제 배포 계약 파일·unit을 고정하는 현재 테스트가 목적에 맞음. 리뷰어도 결함이 아닌 LOW 관찰로 분류 |

## Iteration 2 — Claude Sonnet 5 독립 리뷰

- Date: 2026-08-28
- Orca Run: `run_f299fbc68bf1`
- Task: `task_20ba3293da69`
- Dispatch: `ctx_b0699d3f8522`
- Worker: 새 Claude 터미널 `term_f699d904-9386-43d0-9079-0a80da1b86fc`
- Model receipt: requested/effective 모두 `claude-sonnet-5`
- Placement: 현재 `/Users/h0977/dev/ClairKeys` 워크트리 재사용, 공유 checkout은 `main` 유지
- Mode: 읽기 전용. `gh pr diff`, `git diff`, `git show`, `git grep`, GitHub live state로 검토

결론은 **blocking findings 없음**이다. 워커가 확인한 내용:

1. 활성 Fly 설정·배포 지침·현재형 주장은 제거됐고, 남은 이름은 날짜가 있는 역사 기록과
   회귀 테스트의 부재 단언뿐이다.
2. D-008은 D-012에 의해 superseded됐으며 PR #21 review log와 2026-08-21 validation이
   원래 후보 비교의 역사 증거를 보존한다.
3. 회귀 테스트는 `fly.toml` 부재와 실제 podman/systemd unit의 핵심 계약을 고정한다.
4. 삭제된 설정을 참조하는 CI·빌드·배포 경로가 없고 실행 코드 변경도 없다.
5. 변경 파일은 모두 폐기된 배포 표면 제거라는 단일 목적에 속한다.

LOW 관찰 R2는 비차단·비결함으로 분류해 코드 변경 없이 종료했다. 검증 공백은 두 가지다:
워커가 공유 checkout을 유지했으므로 PR tree에서 로컬 테스트를 재실행하지 않았고 동일 head의
hosted CI를 교차 확인했으며, 실제 NAVER VM 재배포는 PR 범위 밖이라 확인하지 않았다.

절차상 경미한 일탈도 기록한다. 워커는 초반 grep을 위해 `/tmp` detached git worktree를 한 번
만들었다가 "워크트리 분리 금지"와 충돌함을 인지하고 즉시 제거했다. 프로젝트 공유 워크트리와
브랜치에는 영향이 없었고, 이후 검사는 ref 기반 읽기 명령만 사용했다. 최종 `git worktree list`는
프로젝트 워크트리 하나만 표시하고 `git status --short`는 clean이다.

## Iteration — PR #68 병합 후 재검토 및 병합 (2026-08-28)

PR #68이 `41606e7`로 먼저 병합되면서 두 PR이 `omr-service/app.py`,
`docs/recovery/DECISIONS.md`, `src/app/api/omr/upload/__tests__/route.test.ts` 3개 파일을
공유하게 됐다. GitHub은 `MERGEABLE` / `CLEAN`을 보고했지만, 텍스트 충돌이 없다는 것과 병합
결과가 성립한다는 것은 다른 문제다 — #69의 테스트는 #68 이전 코드를 기준으로 작성됐다.

**trial merge 후 병합 결과에서 전체 게이트 재실행**

| 명령 | 결과 |
|---|---|
| `python3 -m unittest discover -s tests` | **44 passed** (#68의 43 + #69 신규 1) |
| `npm test -- --runInBand` | 55 suites / **524 tests passed** |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | No ESLint warnings or errors |
| `npm run build` | 성공 |

겹침은 실질적으로 없었다. #69가 `app.py`에 한 변경은 `/health` docstring 한 줄이고, #68의
변경은 `notify_completion`과 그 호출부다.

**정리 완결성 독립 확인** — 날짜가 있는 `reviews/`·`validation/` 기록을 제외하고 코드·테스트·
README·AGENTS·DECISIONS·워크플로·Dockerfile 전체를 `fly.io|fly.toml|flyctl|fly deploy|
clairkeys-omr.fly.dev|fly secrets`로 훑었다. 남은 것은 두 종류뿐이다.

- `omr-service/tests/test_audiveris_runtime.py:325` — 재유입을 막는 가드 테스트 자체
- `docs/recovery/HANDOFF.md`의 날짜 표제 아래 과거 기록

**추가 관찰 R3 (LOW, 비차단)** — `HANDOFF.md`의 `## Session handoff — 2026-08-21` 절이
"`test_audiveris_runtime.py` still asserts `memory = \"4gb\"` from `fly.toml`. Removing
`fly.toml` breaks the suite"를 **현재형 지시**로 담고 있다. 이 PR이 정확히 그 작업을 수행해
해소됐다. 날짜 표제 아래이므로 보존 정책상 문제는 없으나 문장 형태가 사실 기록이 아니라 다음
세션용 제약이라, 병합 후 HANDOFF 갱신에서 해소 표시를 덧붙였다.

**병합** — 사용자 명시적 승인 후 `aaae994`로 병합. 병합 직전 재확인: 비성공 체크 0건, 미해결
review thread 0건, `MERGEABLE` / `CLEAN`, draft 아님. 병합 후 main checks **6/6 성공**,
`omr-service/fly.toml` 부재 확인, 원격·로컬 작업 브랜치 삭제 완료.
