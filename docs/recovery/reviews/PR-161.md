# PR161 — 줄 위 3도 화음의 두 점을 점 처리 순서와 무관하게 유지한다 (D-062)

Date: 2026-09-14
PR: https://github.com/landfill/ClairKeys/pull/161
Head: 252aa27323e049045cc62829f0813cf07a4b17f1 (생성 시 42736a2)
Branch: `codex/issue-134-dot-head-link`
Issue: #134 (부분 — 본문은 `Refs #134`이고 `closingIssuesReferences`가 0건임을 확인했다)

- 생성 시 non-draft, MERGEABLE. **병합 승인은 존재하지 않는다.** 운영 배포 승인도 없다.
- 커밋 1개(`42736a2`): 패치 파일, `Dockerfile.audiveris`, 회귀 테스트, D-062. Lore trailer를 갖춘다
  (Confidence high, Scope-risk moderate).
- 근거: [검증 기록](../validation/2026-09-13-issue-134-dot-head-link.md).
  - 로컬 amd64 이미지로 main과 브랜치를 PDF 13개에 비교했다.
  - 성공 12개 중 9개는 결과가 같고, 3개는 줄 위 3도 점만 늘었다.
  - Clair 원본 기준 이벤트 143 → 153/191.
- 생성 직후 hosted 체크:
  - pass: Detect changes, Vercel Preview Comments.
  - pending: E2E Tests, Unit Tests, Lint, Lint and Type Check, Run Tests, Security Audit, Vercel.
  - skipping: Build Check, Security Scan.
  - CodeRabbit은 "manual review required for this OSS repository"로 skip.
  - 최신 결과는 PR에서 확인한다.

| Finding | 상태 | 대응 |
| --- | --- | --- |
| Codex P2 (`42736a2`, comment 3999954570): Dockerfile 회귀 테스트가 패치 문자열만 찾으므로 제어 흐름이나 컴파일된 jar가 틀려도 통과한다. 두 점 처리 순서를 실행 가능한 fixture로 검증하라 | FIXED (`252aa27`) | 생성형 합성 fixture와 native 테스트를 추가했다. 기본 이미지는 두 엔진 모두 3/3회 실패하고, 패치 이미지는 3/3회 통과한다. 근거를 달아 답하고(reply 4000013645) thread를 resolve했다 |

## 1차 리뷰 대응 (2026-09-14)

- 지적은 유효하다. 기존 테스트는 빌드 순서와 패치 문자열 계약만 고정하고, 엔진 동작은 로컬 corpus 기록에만 있었다.
- 추가한 파일:
  - `omr-service/tests/line_third_dots_fixture.py`: Audiveris jar 안의 Bravura(SIL OFL) 글리프로 A4 300dpi 한 보표
    3/4 악보를 그린다. 인접한 네 줄 쌍 각각에 점2분 3도 화음을 두 번 넣는다. 한 번은 아래 점 left x를 2px 작게,
    한 번은 반대로 해서 `Dot.byAbscissa` 처리 순서를 강제한다. PDF·이미지는 커밋하지 않는다.
  - `omr-service/tests/test_line_third_dots_native.py`: 설치된 normal·recovery 엔진마다 `-batch -export`를 실행한다.
    3/4 박자, 8마디, 각 마디의 두 음이 모두 점 1개·3박인지 확인한다. 엔진이 없으면 skip한다.
  - `src/utils/__tests__/omrRuntimeContract.test.ts`: Jest bridge 목록에 추가해 hosted CI에서도 파일 존재와 import를 강제한다.
    hosted CI에는 Audiveris가 없어서 skip으로 통과한다.
- 적색/녹색 근거(로컬 amd64 이미지, repo 읽기 전용 마운트, network none):
  - `clairkeys-omr:stock-main`(main `b74fcee`): `FAILED (failures=2)`를 3회 반복했다.
    normal·recovery 모두 첫 "아래 점 먼저" 마디가 `('E4','half',0,2)`로 나온다.
  - `clairkeys-omr:dot-link`: `OK`를 3회 반복했다(각 약 33초). 전체 이미지 suite는 **174 OK**다.
  - fixture 개발 중 박자표를 음자리표에서 너무 떨어뜨렸더니 `No target duration`이 났다. 간격을 좁혀 3/4 인식을
    확인했다. 첫 초안의 이런 실패는 테스트 근거로 쓰지 않았다.
- 로컬 결과:
  - Jest `omrRuntimeContract` 11/11.
  - `npm run lint` 경고 0.
  - `npx tsc --noEmit` exit 0.
  - `unittest discover` 162 ran / 1 error(`fastapi` 미설치) / 1 skip.
