# PR158 — 내 악보의 기존 상태·반응형·키보드 포커스·확대를 측정으로 검증

Date: 2026-09-13
PR: https://github.com/landfill/ClairKeys/pull/158
Head: c72d5fb8b9dd223def5676d7c654390350ab4800
Branch: `codex/issue-146-states-responsive`
Issue: #146 stage 4

- 이 항목을 쓰는 시점에 non-draft, MERGEABLE. 호스티드 체크는 실행 중이었으므로 결과는 이 줄이
  아니라 PR에서 확인한다. **병합 승인은 존재하지 않는다.**
- 커밋 하나(`c72d5fb`), 회귀 우선. 측정 훅(`data-testid`)만 먼저 넣고 미수정 코드에 대해 새
  테스트를 돌려 결함을 재현했다 — jest 8 failed / 11 passed, Playwright(chromium) 11 failed /
  3 passed. 근거: [library 상태·반응형 검증](../validation/2026-09-13-library-states-responsive.md).
  커밋의 Lore trailer는 12개가 파싱된다(`git show -s --format='%(trailers:only=true)'`로 확인).
- 변경 파일: `src/app/library/page.tsx`, `src/components/library/LibrarySheetMusicList.tsx`와 그
  테스트, `src/components/sheet/SheetMusicCard.tsx`와 그 테스트, `src/components/ui/Loading.tsx`,
  새 `src/components/ui/__tests__/Loading.test.tsx`, 새
  `e2e/library-states-responsive.spec.ts`, `docs/recovery/phases/ISSUE-146-ui-renewal.md`
  (stage 4 완료 조건 — 계획 변경이므로 예외 없이 이 PR에 포함).
  `git diff --stat -- src/app/api prisma src/types src/utils src/services src/hooks src/lib omr-service`
  는 비어 있다. API·DB·애니메이션 계약·재생·오디오·OMR 서비스를 건드리지 않았다.
- 측정이 드러낸 결함 네 가지와 그 수치. (1) 500 응답에서도 화면이 "악보가 없습니다"였다 — 빈
  목록과 글자까지 같았다. (2) 처리 중 카드의 주 동작만 32px(다른 상태는 44px), 폭도 글자 폭
  64px. (3) 로딩 상태에 `role`·이름이 없고 색이 `text-blue-600`. (4) 목록 동작의 접근 가능한
  이름이 9건 중복. 추가로 `/library` 콘텐츠 영역의 `min-h-screen`이 `MainLayout`의 것과 중복돼
  빈 상태에서 카드 아래 1116px이 비어 있었다(문서가 정확히 두 화면).
- 의도적으로 바꾸지 않은 것: 필드·질의·정렬·검색·카테고리 이동·삭제 확인 대화상자·제출 동작과
  **보이는 글자**. 달라진 것은 접근 가능한 이름(곡명이 앞에 붙는다)과 처리 중 버튼의 크기다.
  그 이름을 고정하던 기존 테스트 2건은 이름만 갱신했고 href·클래스 단정은 그대로 뒀다.
- 측정이 작업 자체를 두 번 바꿨고, 둘 다 기록한다. (1) 실패 상태 조건을 `loadError`만 보는
  대신 `loadError && 목록이 비어 있음`으로 좁혔다 — `useSheetMusic`의 `error`는 조회뿐 아니라
  수정·삭제 실패에도 설정되므로, 넓게 두면 제목 저장 실패가 "목록을 불러오지 못했습니다"로 잘못
  보고된다. 이 판단은 `keeps the list when the failure was a save, not a load`로 고정했다.
  (2) 빈 상태 판정식을 "문서 전체 높이 < 화면의 1.6배"에서 "빈 상태 카드 아래 공간 < 한 화면"으로
  바꿨다. 1.6은 근거 없는 배수였고 정상적인 머리글·탭까지 함께 셌다. 같은 빌드에서
  `min-h-screen`을 다시 넣어 등가 상태를 만들어 대조했다: 1116px(실패) → 687px(통과).
- 고치지 않고 관찰로 남긴 것 — **고친 것으로 기록하지 않는다.** `ConfirmDialog`(삭제 확인)가
  `bg-white`·`bg-gray-500`·`red-*` 등 원시 팔레트를 쓴다. 이 이슈의 화면 표는 기존 확인 대화상자
  보존을 요구하고, 이 컴포넌트는 카테고리 관리 등 stage 4 범위 밖 화면도 함께 쓴다.
  `availability === 'unknown'`의 주 동작이 `다시 업로드`인 것도 동작 변경 판단이므로 그대로 뒀다.
  남은 687px 중 약 237px은 `MainLayout`의 `min-h-screen`이 머리글·푸터와 쌓여 생기는 전역 성질로,
  `/library`만의 결함이 아니라 건드리지 않았다.
- 로컬 검증: 대상 jest 52/52(8 suite), 전체 jest 1020 passed / 1 failed, lint 0/0,
  `tsc --noEmit` 0 errors, `npm run build` success, 새 스펙 70/70(5 browser projects),
  `CI=1 npx playwright test` 229 passed / 1 flaky / 0 failed. 전체 jest의 단일 실패는
  `src/ci/__tests__/omrCallbackDelivery.test.ts`의 `fastapi` 미설치 환경 격차이고(PR155·157과 동일),
  flaky 1건은 이 슬라이스가 건드리지 않은 `[webkit] playback-session-transition`으로 재시도에서
  통과했다. **build가 타입·린트 검사를 대신한다고 기록하지 않는다 — 각각 따로 돌렸다.**
- 기본 병렬(로컬 5 worker)로 전체 E2E를 돌렸을 때 firefox 8건이 실패한 적이 있다. 같은 프로젝트
  단독 실행은 46/46 통과하고 CI 설정 실행은 통과하므로 이 기계의 경합으로 판단하되, 초록으로 덮지
  않고 남긴다 — 호스티드 E2E 잡이 권위다.
- 이 PR은 #146을 닫지 않는다. stage 4 완료 조건 중 실기기 터치, 실제 가로 방향 하드웨어, 브라우저
  자체의 확대, 스크린리더의 실제 출력, 계측된 색 대비, 실제 로그인 흐름은 자동 검사로 대체할 수
  없으며 남은 범위로 명시했다.

## 리뷰 항목

`c72d5fb`의 호스티드 체크는 14/14 success(E2E 두 잡 포함, 8m49s·7m47s). CodeRabbit은 지적 없이
완료, Codex가 인라인 4건을 달았다. 사용자는 "이슈 없으면 머지"를 조건으로 승인했으나 **지적이
남아 있어 머지하지 않고** 먼저 처리했다.

| # | 지적 (Codex) | 상태 | 근거·조치 |
|---|---|---|---|
| 1 | P1 — `c72d5fb`에 Lore trailer(Confidence·Scope-risk)가 없다 | REJECTED | 사실 아님. `%(trailers)`와 `git interpret-trailers --parse` 모두 12개, 두 키 포함. 이 저장소에서 반복 기록된 근거 없는 trailer 지적 패턴과 같다 |
| 2 | P2 — 캐시된 행이 실패한 필터 재조회를 가린다 | FIXED `2e66e30` | 재현됨: 전체 목록 후 `categoryId` 조회만 실패시키면 이전 행이 새 카테고리 이름 아래 남고 재시도가 없었다. 조회 함수가 성공 여부를 돌려주고 목록은 최근 조회로만 실패를 추적 |
| 3 | P2 — WebKit 키보드 테스트가 `focus()`로 우회 통과 | FIXED `2e66e30` | 측정: WebKit(데스크톱·모바일)은 Tab을 입력창·select에만 멈춘다. 분기는 유지하되 Tab 작동과 `tabIndex === 0`·비활성 아님을 단정 |
| 4 | P2 — 같은 제목의 카드는 동작 이름이 같다 | FIXED `2e66e30` | 재현됨. 저작자·배지·날짜를 `aria-describedby`로 연결, E2E는 같은 제목 두 장으로 이름+설명 유일성 판정 |

모든 스레드에 근거를 달아 답하고 resolve했다.

### 리뷰 대응 라운드 (2e66e30)

- 회귀 근거 선행: jest 6 failed(목록 4·훅 1·카드 1). 수정 전 빌드(`c72d5fb`)에 새 E2E를 돌려
  chromium·webkit 4 failed / 26 passed — 필터 재조회 실패와 같은 제목 구별이 두 브라우저 모두에서
  실패했다. WebKit 키보드 강화는 결함 재현이 아니라 가드 강화라 변경 전에도 통과한다.
- **조치 선택의 이유.** 조회 실패 시 훅이 행을 비우는 방식은 거절했다 — 같은 훅을 쓰는
  `CategorySheetMusicList`에서 실패가 빈 목록으로 표시되는, 이 슬라이스가 고친 바로 그 거짓을
  다른 화면에 새로 만든다. 이전 행 위에 인라인 경고만 띄우는 방식도 거절했다 — 목록 제목이 새
  카테고리 이름을 달고 있어 여전히 다른 질의의 결과를 지금 결과처럼 말한다.
- **처음 기록의 정정.** 첫 제출 때 PR 본문과 검증 기록은 `src/hooks` diff가 비어 있다고 적었다.
  이 라운드에서 `useSheetMusic.fetchUserSheetMusic`의 반환값이 `Promise<boolean>`으로 바뀌었으므로
  그 문장을 고쳤다. 요청·응답 형태와 API·DB·데이터 계약은 여전히 그대로다.
- 로컬: 대상 jest 80/80(카테고리 화면 포함), 전체 jest 1024 passed / 1 failed(`fastapi` 환경
  격차), lint 0/0, tsc 0 errors, `CI=1 npx playwright test` 235/235(flaky 0). 커밋 trailer 13개 파싱.
- 사용자가 "리뷰 수정 푸시하고 CI 통과하면 머지"를 명시적으로 지시했다. 새 head의 CI와 새 리뷰
  지적 여부를 확인한 뒤 머지한다.
- `main` 기록 커밋 `cae9aef`는 이 로그 갱신이 스크립트 실수로 빠진 채 검증 기록만 담았다. 이
  내용은 후속 커밋으로 채웠다.

### 2차 리뷰 라운드 (8fbc367)

`2e66e30`의 호스티드 체크 14/14 success, non-draft, MERGEABLE/CLEAN. 그러나 Codex가 새 커밋에 P2
1건을 추가해 **머지하지 않고** 처리했다.

| # | 지적 (Codex) | 상태 | 근거·조치 |
|---|---|---|---|
| 5 | P2 — 같은 날 같은 저작자로 다시 올린 카드는 이름+설명이 여전히 같다 | FIXED `8fbc367` | 유효. 첫 대응의 테스트가 중복 fixture의 저작자·날짜를 바꿔 이 경우를 피해 갔다. fixture를 흔한 재업로드 경우로 바꾸자 `2e66e30` 빌드에서 chromium·webkit 모두 실패. 올린 시각(초 단위)을 sr-only 설명으로 추가 |

- 거절한 대안: DB id(유일하지만 사람에게 의미 없음), 보이는 날짜를 시각까지 확장(모든 카드의 표현이
  바뀜). 남는 경계: 같은 초에 두 번 올린 카드 — 화면으로 보는 사람에게도 구별되지 않는다.
- 로컬: 대상 jest 48/48, 전체 jest 1024 passed / 1 failed(`fastapi` 환경 격차), lint 0/0, tsc 0 errors,
  `CI=1 npx playwright test` 235/235(flaky 0). 커밋 trailer 10개 파싱. 스레드에 답하고 resolve했다.

### 3차 리뷰 라운드 (95ab62f)

`8fbc367`의 호스티드 체크 14/14 success, MERGEABLE/CLEAN. Codex가 2건을 추가해 **머지하지 않고** 처리했다.

| # | 지적 (Codex) | 상태 | 근거·조치 |
|---|---|---|---|
| 6 | P1 — SHA `383f3f18ee8a7bb355fb37be87ae92f3e73f67e6`에서 trailer가 0개 파싱된다 | REJECTED | 그 SHA는 저장소에 없다(`git cat-file -t` 실패). 브랜치 커밋은 12/13/10개 파싱, 모두 Confidence·Scope-risk 포함. PR157에서 기록된 "존재하지 않는 SHA 인용" 패턴의 반복 |
| 7 | P2 — 편집 대화상자를 취소해도 포커스가 편집 동작으로 돌아가지 않는다 | FIXED `95ab62f` | 유효. stage4 조건 "취소가 목록으로 돌아온다"를 테스트가 검사하지 않았다. `8fbc367` 빌드에서 5개 프로젝트 모두 `toBeFocused` 실패로 재현. 연 동작을 기억해 취소·저장 성공 시 포커스 복귀 |

- 로컬: 대상 jest 50/50, 전체 jest 1026 passed / 1 failed(`fastapi` 환경 격차), lint 0/0, tsc 0 errors,
  `CI=1 npx playwright test` 235/235(flaky 0). 커밋 trailer 9개 파싱. 두 스레드에 답하고 resolve했다.

### 4차 확인과 병합 (2026-09-13)

| # | 지적 (Codex) | 상태 | 근거·조치 |
|---|---|---|---|
| 8 | P1 — SHA `ad03de6ef4ff275e5aab0a013de0ab909efb9be3`에서 trailer 0개 | REJECTED | 저장소에 없는 SHA. 완료 조건을 추가한 커밋은 `git log -S`로 `c72d5fb`(12개 파싱). 네 커밋 모두 파싱(9/10/13/12) |

- `95ab62f`에 대한 Codex 리뷰 완료까지 기다린 뒤 판정했다. 남은 지적은 위 1건뿐이었고 거절·resolve 후
  unresolved 0.
- 머지 직전 재확인: head `95ab62f`, check-runs 14/14 success, non-draft, MERGEABLE/CLEAN.
- 사용자 지시("리뷰 수정 푸시하고 CI 통과하면 머지")에 따라 `--match-head-commit`으로 병합:
  `194ffb8ff04cb0cb3cdacd1ff541631045d0c4b1`. 병합 커밋 check-runs 6/6 success, trailer 8개 파싱.
- 원격·로컬 브랜치 삭제 완료. 이슈 #146은 사용자 결정에 따라 수동 전용 조건을 한계로 명시하고
  COMPLETED로 종료했다.
- **리뷰 패턴 기록:** 이 PR에서 trailer 지적은 네 번 나왔고 세 번은 저장소에 없는 SHA
  (`383f3f18…`, `ad03de6…`)를 인용했다(첫 번째는 존재하는 `c72d5fb`였으나 역시 사실이 아니었다).
  반면 같은 리뷰어의 P2 지적 5건은 전부 측정으로 재현됐다. 지적의 SHA는 `git cat-file -t`로 존재부터
  확인하고, 기능 지적은 실제로 재현해 판단한다.

