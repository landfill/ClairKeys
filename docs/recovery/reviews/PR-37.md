# PR Review Log — PR #37

PR URL: https://github.com/landfill/ClairKeys/pull/37
Branch: `codex/p1-omr-naver-vm-runtime` (head `8045eb0`)
Last checked: 2026-08-21 19:10 KST

Scope: make `omr-service/Dockerfile.audiveris` produce an image that both installs and starts
Audiveris, with regression tests pinning the two undeclared needs of the official `.deb`.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Lint | PENDING | Opened 2026-08-21; not yet checked |
| Run Tests | PENDING | Opened 2026-08-21; not yet checked |
| E2E Tests | PENDING | Opened 2026-08-21; not yet checked |
| Security Audit | PENDING | Opened 2026-08-21; not yet checked |

Repository CI does not build the OMR image, so a green PR still says nothing about it. The image
evidence is the VM build recorded in
`docs/recovery/validation/2026-08-21-issue-22-naver-vm-omr-runtime-proof.md`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| — | — | — | No review received yet | — | OPEN | — |

## Iteration log

### Iteration 1

- Feedback fetched: none yet — PR opened 2026-08-21.
- Changes made: `Dockerfile.audiveris` installs `desktop-file-utils`, `shared-mime-info`,
  `libgtk-3-0` and creates `/usr/share/applications` + `/usr/share/desktop-directories` before the
  `.deb` install; the build now runs `/opt/audiveris/bin/Audiveris -version`.
  `tests/test_audiveris_runtime.py` gains
  `test_container_supplies_the_needs_the_deb_does_not_declare` and
  `test_build_proves_the_launcher_starts_not_merely_that_it_exists`.
- Validation: image built and tagged on the NAVER VM; in-build `-version` reported Audiveris
  5.11.0 / OpenJDK 25.0.3 / Tesseract 5.5.2; a real Mutopia PDF converted to `.mxl` and then to
  514 notes of animation JSON through `omr.cli`; 11 Python tests pass.
- Commit: `8045eb0`
- Remaining actionable items: none from review. Independent of review, the PR deliberately does
  not claim recognition accuracy, FastAPI service operation, Supabase upload, or any Next.js
  end-to-end path.

## Reviewer attention requested

- Whether the build-time `Audiveris -version` belongs in the same `RUN` layer as the install, or
  should be a separate layer so a failure is easier to attribute.
- Whether pinning package names (`libgtk-3-0`) in a test is the right granularity, given the name
  is Ubuntu-specific and would change on a base-image bump.

## Outcome

`MERGED` at `0265771` on 2026-08-21 with the user's explicit approval. Before merging, the branch was
updated from `main` and re-checked: `mergeable: MERGEABLE`, state `CLEAN`, no failing checks. The
tip is contained in `main` with 0 unique commits on both the local and remote refs.

Post-merge checks on `main` at `3208488` all report success: `Security Audit`, `Run Tests`,
`E2E Tests`, `Lint`, `Post-merge build`, `Post-merge tests`.

No review was received from any reviewer on this PR; CodeRabbit reported
"Review skipped: manual review required for this OSS repository".

Branch deletion is deferred, not forgotten — `.gitignore` was modified and uncommitted at merge
time, and the protocol forbids deleting a branch over user-owned worktree state. PR #40 carries
that change; delete the branches once it lands and `git status --short` is empty.
