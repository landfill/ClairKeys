# PR #59 — 악보 재생을 합성 음색에서 녹음 샘플로 교체

- URL: https://github.com/landfill/ClairKeys/pull/59
- Branch: `codex/p1a-piano-sample-timbre` → `main`
- State: `OPEN` (생성 2026-08-27)
- Commits: `c8854ba`(샘플 재생 경로), `a341db2`(첫 재생 대기와 seek 오프셋)
- Records: **D-014**

## 무엇을 왜

사용자 보고: "악보 재생 소리가 너무 웹오디오 한계처럼 삑삑거리는 수준 같다."

PR #30/#32/#33이 합성 음색을 세 번 귀로 튜닝했지만 전자음 인상이 남았다. 원인은
상수가 아니라 구조다 — `createNoteAudio`가 음 하나에 oscillator 1개와 gain 1개를
주므로 24개 배음이 하나의 엔벨로프를 공유하고 같은 속도로 죽는다. 실제 현은 상위
배음이 수백 ms 안에 사라지고 기음이 몇 초를 울린다. 어떤 rolloff 값으로도 그 차이는
나오지 않는다.

`AudioBufferSourceNode`가 `OscillatorNode`와 같은 `start(when)`/`stop(when)`을
제공하므로 변경이 `createNoteAudio` 안에 갇혔다. **PR #26의 클럭 앵커, 롤링
look-ahead, `VOICE_LIMIT`, seek 재발성 로직은 손대지 않았다.**

## 설계를 결정한 두 실측

- **메모리**: 원본 세트를 그대로 디코딩하면 **142MB**(스테레오 30개, 합계 422초).
  `scripts/build-piano-samples.sh`가 모노 변환 + 음역별 트림으로 120초 /
  **20.2MB** / 디스크 1.17MB로 만든다.
- **게인**: 빌드된 세트의 최대 피크는 A0의 -7.7 dBFS(선형 0.4112). 합성 경로의
  `PEAK_GAIN`은 0.3이므로 `SAMPLE_PEAK_GAIN = 0.73`으로 되맞췄다. 이게 없으면 첫
  화음이 약 37% 크게 나오고 `DEFAULT_MASTER_GAIN`·`MAX_MASTER_GAIN`의 헤드룸
  근거가 조용히 무효가 된다.

## 테스트가 아니라 브라우저 실측이 잡아낸 결함 2개

1. **도입부에서 악기가 바뀌었다.** 뱅크 로딩이 재생 클릭 시점의
   `initializeAudio`에서야 시작되므로 첫 음들이 합성음으로 났다(실측 샘플 5 : 합성
   3). `startAudio`가 뱅크를 기다리되 2.5초에서 끊는다.
2. **seek이 해머 타격음을 다시 재생했다.** `start(when)`은 버퍼를 0에서 다시
   시작한다. 합성 경로의 4ms 어택 뒤에서는 안 들렸고 녹음된 타격음에서는 들린다.
   경과 출력 시간을 playbackRate로 버퍼 오프셋으로 환산해 `start(when, offset)`로
   넘기고, 버퍼 길이로 clamp한다.

## 검증 기록

- 신규 테스트 32개: `pianoSamples` 12, `pianoSampleBank` 10, 재생 훅의 샘플 분기 10.
- **teardown 회귀 테스트 2개를 기존 oscillator 경로에 먼저 작성해 통과를 확인한 뒤**
  샘플 경로에서 다시 통과시켰다. 이 변경에서 가장 깨지기 쉬운 지점이 정지·탐색 시
  voice가 살아남는 것이었다.
- 헤드리스 Chromium으로 실행 중인 앱 검증: buffer source 8 / oscillator 0(대기
  수정 전에는 5 / 3), 음 게인이 정확히 `velocity × 0.73`, 콘솔 오류 없음.
- 브라우저 디코딩이 ffmpeg 상수를 확인: 30/30 디코딩, 모노 44.1kHz, 120.0초,
  20.2MB, 피크 0.4112 @ MIDI 21 (코드의 `SAMPLE_SET_PEAK`는 0.4111).
- `npm run build` 통과. `/samples/piano/60.mp3`가
  `public, max-age=31536000, immutable`로 서빙됨.

## 검증하지 못한 것

- **실제로 어떻게 들리는지.** 이건 사용자의 판단이고, 이 PR은 그 판단의 준비이지
  증명이 아니다. 귀로 조정될 수 있는 상수 셋: `SAMPLE_PEAK_GAIN`(피크는 맞췄으나
  해머 타격음이 있는 녹음은 같은 피크의 합성음보다 크게 들린다), 모노 폴드(메모리를
  절반으로 줄인 대가로 스테레오 폭을 잃음), `damperReleaseSec`.
- 저음에서 트림보다 긴 음(♩=42보다 느린 온음표)은 일찍 멈춘다. 트림 지점의 0.5초
  페이드 때문에 클릭은 없다. 저장소에 실제 저음 데이터가 없어 확인 불가.
- 실기기 모바일에서의 디코딩 메모리 거동.

## 이 머신의 사전 존재 실패 5건 (이 PR과 무관)

전체 스위트는 48/53 통과, 22건 실패. 전부 이 변경 영역 밖이고 환경 문제다.

- `converterCorpus`, `converterTempoContract`, `omrRuntimeContract` — Python으로
  shell out 하는데 `music21`이 설치돼 있지 않다.
- `uploadPathInventory` — 저장소 상대 POSIX 경로를 기대하는데 Windows 절대 경로
  (`C:\Users\...`)를 받는다.
- `prChecksWorkflow` — 워크플로 파일에 LF 개행 정규식을 걸었는데 CRLF다.

## CI — 2026-08-27 시점에 필수 체크가 하나도 돌지 않았다

`gh pr checks 59`가 보고한 것은 CodeRabbit(스킵)과 Vercel 둘뿐이고, 4개 required
status check(`Run Tests`, `Lint`, `Build application`, `Security Audit`)는 **실행
자체가 없었다.** 이 PR만의 문제가 아니다:

- `gh api repos/landfill/ClairKeys/actions/runs`의 `total_count`는 521인데 가장
  최근 run이 **2026-08-24T09:40:58Z**다. 그 이후 저장소 전체에 실행이 없다.
- `main`으로의 직접 푸시 `f93ebe0`의 check-runs가 **비어 있다**. AGENTS.md 주의 2가
  경고한 상황 그대로다 — 이 핸드오프 커밋은 CI 검증을 받지 않았다.
- Actions는 꺼져 있지 않다: `actions/permissions`는 `enabled: true`,
  `actions/workflows`는 4개 워크플로 모두 `active`.

원인은 저장소 설정 밖에 있다(사용량·결제 한도가 흔한 원인이나, 현재 토큰에 `user`
scope이 없어 billing API로 확인하지 못했다). **추정을 사실로 적지 않는다** — 확인된
것은 "활성 상태인데 2026-08-24 이후 실행이 없다"까지다.

따라서 이 PR의 검증 근거는 전부 로컬과 헤드리스 브라우저 실측이며, 호스팅 CI의
확인은 없다. 병합 전에 Actions가 왜 멈췄는지 먼저 해결해야 한다.

### 정정 (2026-08-27 09:00 KST) — 중단은 실재했고, 위 기록 3분 뒤에 끝났다

위 절은 **쓰인 시점에는 사실이었다.** 그 뒤 CI가 스스로 복구됐으므로 상태만 낡았다.
시각을 맞춰 보면 모순이 아니다:

| 시각 (UTC) | 사건 |
|---|---|
| 2026-08-26T16:13:06Z | PR #59 생성 |
| 2026-08-26T16:25:41Z | 위 절을 담은 커밋 `930e6b9` 작성 — 이때는 실행이 없었다 |
| **2026-08-26T16:28:45Z** | CI 실행 시작 — 기록 **3분 뒤** |
| 2026-08-26T16:33:15Z | 전체 통과 |

첫 커밋 `c8854ba`의 check-runs는 실제로 **0건**이므로 관측은 옳았다. 현재 head
`a341db2`에서는 **15개 체크가 모두 success**이고, required 4개(`Lint`,
`Security Audit`, `Run Tests`, `E2E Tests`)도 전부 통과한다. Actions가 왜 멈췄는지는
여전히 미규명이나, **병합 전 선결 조건이라던 이 항목은 해소됐다.**

이 정정 자체가 `AGENTS.md`가 HANDOFF에 transient 상태를 고정하지 말라고 정한 이유의
사례다 — 원인 분석(활성 상태인데 실행 없음)은 값이 남았지만, 상태는 3분 만에 낡았다.

## CodeRabbit 리뷰 — 지적 2건 중 1건만 처리했다 (정정)

`scripts/build-piano-samples.sh:89`의 `ffmpeg`가 `-nostdin` 없이
`echo "$SAMPLES" | while read` 루프 안에서 실행돼, ffmpeg가 파이프의 stdin을 소비하면
`read`가 다음 줄을 받지 못하고 루프가 조기 종료될 수 있었다(ShellCheck `SC2095`).

`6315a77`에서 `-nostdin`을 추가했다. 검증: shellcheck 0.11.0이 수정 전 `SC2095`를
**2건**(info + warning) 보고하고 수정 후 **0건**이며, `bash -n`도 통과한다.
스크립트를 재실행하지는 않았다 — 재실행은 커밋된 샘플을 다시 내려받아 재인코딩한다.

이 결함은 **동작한 실행이 루프의 건전성을 증명하지 않는** 형태다. PR에 샘플 30개가
전부 커밋돼 있어 실행한 머신에서는 완전한 세트가 나왔지만, 그것이 루프가 옳다는 근거는
아니다. 리뷰 스레드는 답신 후 resolved 처리했다.

### 정정 (2026-08-27) — 두 번째 지적을 놓쳤다

위 절의 원래 제목은 "actionable 1건, 처리 완료"였다. **틀렸다.** CodeRabbit은 두 가지를
지적했고 그중 하나만 처리했다. 요약 코멘트의 원문:

> It is mergeable with owner awareness: the build script should prevent ffmpeg from
> consuming the loop input, **and sample replacement needs a cache-busting or documented
> versioning procedure to avoid stale audio.**

**놓친 원인은 확인 방법에 있다.** `gh api repos/.../pulls/59/comments`(인라인 코멘트)만
조회하고, 요약 코멘트 본문은 `Actionable comments posted` 문자열로만 grep했다. CodeRabbit이
이 PR에서는 그 형식을 쓰지 않아 두 번째 지적이 통째로 걸러졌다. **인라인 코멘트 API는
리뷰 지적의 전부가 아니다** — 요약 코멘트의 산문도 읽어야 한다.

미처리로 남은 지적: `next.config.mjs`가 샘플 URL에 1년 immutable 캐시를 걸고 `public/sw.js`가
같은 경로를 cache-first로 보존하는데 URL에 버전이 없다. 샘플을 재빌드해도 기존 사용자는
최대 1년간 옛 오디오를 듣는다. cache-busting 또는 문서화된 버저닝 절차가 필요하다.

이 누락은 2026-08-27 코덱스 리뷰 워커(`run_ecb40c8f0429`)가 발견했고, 코디네이터가
CodeRabbit 요약 원문과 대조해 확인했다.

## Vercel 프리뷰 — 코드를 돌리지 않고 들어볼 수 있다

Preview 배포는 성공했다(`a341db2`):

  https://clairkeys-adbskt9zj-landfills-projects.vercel.app

Vercel SSO 뒤에 있어 `/samples/piano/60.mp3`에 익명으로 접근하면 302다. Vercel에
로그인된 브라우저에서는 열린다. `/test-finger`가 인증 없이 `FallingNotesPlayer`를
띄우므로 음색 판단에 가장 빠른 경로다.

## 범위

**P1-A의 업로드 경로 통합 목표 밖의 신규 범위**이며 사용자의 명시적 요청으로
착수했다. 사용자는 화면 건반 소리(현재 Tone.js triangle `PolySynth`,
`/demo-animation`·`/test-piano`·`FullScreenPiano`에서만 도달)도 같은 악기로
통일하기를 요청했다 — 그건 다른 목적이므로 별도 PR로 남겨두었고 여기 묶지 않았다.

## 다음

1. 사용자가 실제로 들어보고 위 세 상수를 판단한다.
2. 사용자의 명시적 병합 승인을 기다린다. CI 초록과 리뷰 완료는 승인이 아니다.
3. 병합 후 건반 경로 통일 PR을 별도로 만든다.

## 코덱스 리뷰 워커 결과 (2026-08-27)

Orca orchestration으로 읽기 전용 리뷰를 돌렸다. Run `run_ecb40c8f0429`,
Task `task_60bebc2fb4d9`, 워커는 Codex `gpt-5.6-sol` effort high, 워크트리는
코디네이터와 공유(`current`, 브랜치 변경 금지 조건). 대상 head `6315a77`.

**판정: 지금 병합하면 안 된다.** CI 17개 green이고 `MERGEABLE`이지만 아래 결함이 남아 있다.

### HIGH — 샘플 로딩 실패가 사용자에게 보이지 않는다

코디네이터가 코드로 확인한 세 지점:

- `src/utils/pianoSampleBank.ts`의 `loadOne`이 fetch·HTTP·decode 실패를 전부 `catch`해
  `console.warn`만 남긴다. `Promise.all`이므로 `load()`는 **항상 resolve**된다.
- `src/hooks/useFallingNotesAudio.ts`가 `Promise.race([bank.load(), 2.5초 타이머])`로
  기다리므로 실패 여부와 무관하게 진행한다.
- `src/components/animation/FallingNotesPlayer.tsx`가 `isReady={true}`를 **하드코딩**한다.

결과: 오프라인·404·손상 자산에서 사용자는 샘플 기능이 실패한 것을 구분하지 못하고 기존
합성음을 듣는다. **D-001·D-010이 반복해 제거해 온 "실패가 성공처럼 보이는" 형태다.**

워커의 추가 지적: `docs/recovery/DECISIONS.md`의 **D-014가 실패를 표면화하지 말라고
지시하는데, 이는 D-001/D-010 원칙과 충돌한다.** 결정 기록끼리 모순되므로 어느 쪽이
유효한지 먼저 정해야 한다. 제안된 방향은 폴백은 유지하되 `ready` / `degraded` / `failed`를
UI에 노출하는 것.

### MEDIUM 1 — 부분 로드 시 곡 중간 악기 전환이 남는다

2.5초 race 이후에도 로딩은 계속되고 `pianoSampleBank`는 음마다 준비된 버퍼를 동적으로
고른다. 느린 연결이나 샘플 하나만 실패한 경우, 커밋 `a341db2`가 고치려던 "도입부만 다른
악기" 증상이 그대로 재현된다. 재생 시작 시점에 **전체 샘플 또는 전체 합성으로 선택을
고정**하고 부분 로드 회귀 테스트를 추가해야 한다.

### MEDIUM 2 — 고음 슬라이더가 정상 경로에서 무효다

`FallingNotesPlayer.tsx`의 `고음` 슬라이더는 `TREBLE_ROLLOFF`를 조절하는데,
`useFallingNotesAudio.ts`의 샘플 voice 생성부는 주석으로 **`No lowpass`**를 명시하며
필터를 걸지 않는다(코디네이터 확인). 즉 샘플이 정상 로드된 경로에서 이 슬라이더는 아무
효과가 없고 폴백에서만 동작한다. 제거·비활성화·폴백 전용 표시 중 하나가 필요하다.

### MEDIUM 3 — 1년 immutable 캐시에 버전이 없다

위 "정정" 절에 적은 미처리 CodeRabbit 지적과 같은 건이다.

### LOW

- `scripts/build-piano-samples.sh`는 `set -euo pipefail`·`curl -sfL`·`-nostdin`으로
  실패가 nonzero가 되므로 조용한 성공은 아니다. 다만 출력을 최종 디렉터리에 직접 덮어써
  중간 실패 시 혼합·부분 세트가 남고, 원본 checksum 고정도 없다. 임시 디렉터리에 30개를
  완성·검증한 뒤 교체하는 편이 안전하다.
- `DECISIONS.md`의 D-014 서술이 최종 구현(startAudio 대기, seek offset 변경)과 어긋나고,
  이 PR 범위 밖인 건반 경로까지 샘플 bank를 만든다고 적혀 있다.

### 문제없다고 확인된 것 (검토 범위)

- **P0-C 단일 시계 계약 유지** — start/stop·gain automation·시각 playhead가 모두 기존
  AudioContext anchor의 절대 시각을 쓴다.
- **누수 없음** — 음마다 새 `AudioBufferSourceNode`를 만들고 stop·seek·pause·loop·unmount가
  공통 `stopAudioNodes`와 context dispose로 수렴하며 중복 stop 예외를 흡수한다.
  명시적 `onended`가 없어도 이 범위에서 무한 누수는 발견되지 않았다.
- **선행 teardown 테스트 2개가 의미 있다** — stop·seek 시 먼 미래 voice를 실제로 조기
  stop하는 계약을 고정하고, sample stop 테스트도 같은 helper를 고정한다.
- **샘플 매핑 정확** — 21~108의 30개, 3반음 간격, 최대 1반음 nearest 선택,
  `2^((midi-sample)/12)` playbackRate, 양 끝 경계 확인. 대표 파일 21/60/108이 80 kbps·
  44.1 kHz·mono MP3이고 LICENSE attribution도 구현과 일치한다.
- **`next.config.mjs` 변경은 샘플 서빙 자체가 아니라 재검증 회피용**이다.
- **`6315a77`의 `-nostdin` 수정은 SC2095 원인을 정확히 막는다.**

### 판단하지 못한 것

헤드리스 실측(8 buffer source / 0 oscillator, 30개 전체 decode, 콘솔 무오류)은 재실행을
요구하지 않았으므로 독립 검증하지 못했다. 코드 구조가 그 수치를 낼 수 있다는 것까지만
확인했다. 실제 청감과 모바일 메모리도 미검증이다.
