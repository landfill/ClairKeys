# PR #122 Review Log

PR: [#122](https://github.com/landfill/ClairKeys/pull/122)
Branch: `codex/issue-120-phrase-fingering`
Created: 2026-09-04 KST
Final state: MERGED on 2026-09-04 KST after explicit user approval
Current head: `927d81f`

## Scope

- Replace per-note default fingering with deterministic hand-separated phrase inference.
- Preserve source fingering and mark player-bound source/inferred provenance.
- Cover mirrored direction, repeated notes, black-key chords, interleaved hands, and an anonymized production excerpt.
- Retire storage-mutating random backfill paths; do not migrate stored JSON.

## Initial evidence

- Local focused Jest: 5 suites / 53 tests PASS.
- Local full Jest: 91 suites / 856 tests PASS.
- Typecheck, lint, build, and diff check PASS.
- Read-only full production JSON run: 411/411 valid inferred values, deterministic on repeat.
- Browser check with the branch production build and actual public 411-note JSON: PASS at 1440×900 and 844×390 idle;
  PASS at 844×390 playing at 0:02 with no page errors. Vercel preview itself is login protected.
- CodeRabbit automatic review was skipped by OSS policy; manual `@coderabbitai review` requested on 2026-09-04.
- Initial head hosted checks all passed. Review-fix head `927d81f` also passed every hosted check after one Security Audit
  retry (npm registry `audits/quick` returned transient HTTP 400 `Invalid package tree` on the first attempt; the unchanged
  head passed the rerun).

## Review items

| ID | Source | Finding | Disposition | Status | Evidence |
|---|---|---|---|---|---|
| R0 | CodeRabbit | Automatic review skipped because repository has fewer than 10 stars | manual review requested | FIXED | Manual review completed with 2 actionable comments |
| R1 | CodeRabbit | Phase stage said backfill could be consolidated, while implementation retires it | accepted; align phase and D-039 with authenticated 410 tombstone and player-only ownership | FIXED | `927d81f`; replied and resolved |
| R2 | CodeRabbit | Phrase gap used only the immediately previous event end, so a short intervening voice could hide an earlier sustained note | accepted; track maximum end across the whole active phrase and add regression | FIXED | Regression failed `1,2,1,2`, passes `1,2,3,4`; `927d81f`; replied and resolved |

## Review-fix validation

- Focused Jest: 5 suites / 54 tests PASS.
- Full Jest: 91 suites / 857 tests PASS.
- `npx tsc --noEmit`, lint, and `git diff --check`: PASS.
- Both review threads have explanatory replies and are resolved.
- Hosted current-head result: Build, two E2E jobs, two test jobs, lint/typecheck, Security Scan, CodeQL, Vercel, and retried
  Security Audit all PASS. No unresolved actionable review remains.

## Merge gate

Satisfied before merge: current head `927d81f` had all hosted checks successful, both actionable CodeRabbit threads were
resolved, the PR was mergeable/clean, and the user explicitly approved. Merge commit `10e9c72`; post-merge checks 6/6
success; Vercel Production success. Local and remote work branches were deleted only after both tips were confirmed included
in `origin/main`.
