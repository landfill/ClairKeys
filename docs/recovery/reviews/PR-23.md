# PR Review Log — PR #23

PR URL: https://github.com/landfill/ClairKeys/pull/23
Branch: `codex/p0-animation-contract`
Base: `main`
State: `OPEN`
Last checked: 2026-07-21 KST

## Scope

P0-A — establish one versioned animation contract and a golden corpus. Three commits:

1. `83a5e28` — canonical MIDI contract (`src/types/animationContract.ts`), legacy-tolerant validator/normalizer (`src/utils/animationContract.ts`, `AnimationContractError` on malformed input, no middle-C fallback), decision D-009.
2. `88724a1` — 7-case golden corpus (`fixtures/animation-contract/`: monophonic, chord, rests+ties, grand staff, multi-voice, triplet+dotted, tempo change) with musically-correct `expected.json`, plus `compareAnimationData` (`src/utils/animationCompare.ts`).
3. `41c2fa2` — render-path wiring: replaced the `sheet/[id]/page.tsx` `as PianoAnimationData` cast with `normalizeAnimationData`; `FallingNotesPlayer` takes `CanonicalAnimationData` via `canonicalToFallingNotes`; `test-finger` normalizes its legacy Shape A; `converter.py` emits `version` + top-level title/composer.

Evidence base: `docs/recovery/validation/2026-07-21-p0a-animation-shape-audit.md` (four divergent shapes; real OMR output unrenderable through the string-pitch path).

## Completion criteria (P0-A)

| Criterion | Status | Evidence |
|---|---|---|
| malformed/old JSON validated without a cast | MET | `normalizeAnimationData` wired into sheet page; 12 contract tests incl. legacy Shape A normalization + hard errors |
| Python output passes the TS validator | MET | converter.py-shape normalization test; `converter.py` now emits `version` explicitly |
| every fixture records pitch/onset/duration/hand/staff/voice | MET | 7 fixtures; 18 fixture tests validate each `expected.json` against the contract |
| comparison command + tolerances documented | MET | `compareAnimationData` + `DEFAULT_TOLERANCE` (10 ms) + `fixtures/animation-contract/README.md` |

## CI status

Awaiting hosted checks on the PR. Local gates: `npx jest src/utils src/components/animation src/hooks` (136 pass), `npx tsc --noEmit` (0), `npx eslint` touched files (0).

## Review items

_None yet — awaiting review._

## Iteration log

### Iteration 1

- Investigated the four shapes first and recorded the audit (committed to `main`) before writing code, per D-002's "fixture/migration policy first" directive.
- Wrote the validator tests and fixture tests alongside the code (regression-first).
- Left `pdfParser.ts`'s Shape A duplication in place (its cleanup is issue #20); this PR only stops the render path from depending on it.
- Opened PR #23 review-ready. Remaining action: process review feedback, then wait for the user's explicit merge approval (D-005).
