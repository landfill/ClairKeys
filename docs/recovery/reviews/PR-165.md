# PR165 — 2도의 밀린 머리를 쪽 판정 전에 잘라내지 않는다 (D-066, Clair m5·m7)

Date: 2026-09-18
PR: https://github.com/landfill/ClairKeys/pull/165
Head: 9645772 (생성 시)
Branch: `codex/issue-134-second-interval-head`
Issue: #134 (부분 — 본문은 `Related: #134`이고 `closingIssuesReferences`가 0건임을 확인했다)

- 생성 시 non-draft, `mergeable=MERGEABLE`. **병합 승인은 존재하지 않는다.** 운영 배포 승인도 없다.
- 2026-09-18 사용자가 기전 B를 고르고("b"), 구현·검증 후 PR 생성만 승인했다.
- 브랜치는 main `00df833` 위에 있고 그 이후 main의 상태 기록 커밋을 포함하지 않는다.
  HANDOFF가 `9645772`를 인용하므로 rebase하지 않았다. 세 점 diff는 의도한 7개 파일뿐이다.
- 커밋 1개 `9645772`: 패치 `0005`, Dockerfile 등록, 합성 fixture, 네이티브 테스트, 정적 계약 단언, Jest 목록, D-066.
- 근거: [구현·검증](../validation/2026-09-18-issue-134-second-interval-head.md),
  [원인 단계 추적](../validation/2026-09-18-issue-134-m5-empty-head-stage-trace.md).
- hosted 체크 **16/16 pass**(2026-09-18, head `9645772`): All Checks Complete, Build Check, CodeQL, Detect changes,
  E2E Tests ×2, Lint, Lint and Type Check, PR Summary, Run Tests, Security Audit, Security Scan, Unit Tests,
  Vercel, Vercel Preview Comments. CodeRabbit은 OSS 수동 리뷰 정책으로 skip. `mergeStateStatus=CLEAN`.
- Codex 자동 리뷰는 `9645772`에서 Completed이고 **리뷰·인라인 지적 0건**이다. PR에 👍 반응을 남겼다.
- 미해결 actionable review는 없다. **병합 승인은 여전히 존재하지 않는다.**

| Finding | 상태 | 대응 |
| --- | --- | --- |
| 개발 중 발견: 예외 범위에 같은 pitch를 포함하면 반대쪽 중복 판독 머리까지 살아나 음이 두 번 나온다 (corpus 2곡) | FIXED (`9645772`) | 범위를 정확히 한 칸(`pitch−1` 또는 `pitch+1`)으로 좁혔다. Clair raw 해시는 불변이라 이득은 줄지 않았다. D-066 Rejected·Directive |
| 합성 fixture 1·2판이 2도를 그리고도 다른 이유로 머리를 잃어 기전을 검증하지 못했다 | FIXED (`9645772`) | 3판이 Clair m5 실측 픽셀 관계를 재현하고 VIP 로그의 삭제 경로까지 일치한다 |
| 알려진 한계: 같은 pitch 제외 후에도 남을 수 있는 중복 판독 경로에 전용 fixture가 없다 | OPEN (한계로 기록) | D-066 미검증 범위 |
| 알려진 한계: 복원된 m7 C4가 8분음표로 나온다(기대 점4분) | OPEN (별개 원인) | 점·성부 배정 문제이며 이 PR 범위 밖 |
| 알려진 한계: 기전 C(m5 첫 화음 빈 머리 → 셋잇단)는 그대로다 | OPEN (후속 작업) | 원인은 확정됐고 별도 결정이 필요하다 |

## 병합 (2026-09-18)

- 사용자가 "승인. vm 배포도 승인"으로 PR165 병합과 운영 VM 배포를 함께 명시 승인했다.
- 병합 직전 head `9645772` 재확인: hosted 체크 **16/16 pass**, 리뷰 0건·인라인 0건, `mergeStateStatus=CLEAN`.
- `gh pr merge --merge --match-head-commit 96457725…`로 병합했다. 병합 커밋 `f5be5f98c9afe92e87a516ec1f843f6e5fc1ce41`.
  `--subject`·`--body`로 Lore 형식 메시지를 넘겨 병합 커밋에 trailer가 실렸다(PR164와 같은 방식).
- main 반영 확인: 패치 `0005`, 합성 fixture, 네이티브 테스트, D-066이 모두 main에 있다.
- 병합 커밋 post-merge 체크 **6/6 success**(E2E, Post-merge build·tests, Lint, Run Tests, Security Audit).
- 브랜치 정리: 로컬·원격 tip 모두 `9645772`이고 main 대비 고유 커밋 0이다. 사용자 미커밋 변경이 있어 처음엔 보존했고,
  사용자 지시("브랜치 정리해")로 원격 → 로컬 순서로 삭제했다. 미커밋 파일은 그대로 뒀다.

## 배포 (2026-09-18)

- 같은 승인으로 OMR VM에 배포했다. image `4c14123d…`, 롤백 태그 `rollback-pr165-20260918`(`9eef7512…`).
- 이미지 테스트 182 OK / 6 skipped, 네이티브 6개 모두 skip 없이 ok.
- 전환 전 게이트: `SigReducer.class` 해시가 normal·recovery 양쪽 모두 로컬 검증 빌드와 같은 `da3b0721…`,
  직전 운영은 로컬 기준선과 같은 `e4be8314…`.
- 전환 후 health 200, 무인증 `POST /process` 401, journal error/traceback 0줄.
- **운영 인식 결과는 아직 확인하지 않았다.** [배포 기록](../validation/2026-09-18-d066-second-interval-deployment.md).
