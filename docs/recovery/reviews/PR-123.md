# PR #123 Review Log

PR: https://github.com/landfill/ClairKeys/pull/123
Branch: `codex/issue-52-systemd-restart`
Head at creation: `d439173`
Date opened: 2026-09-04 KST

## Scope

Issue #52 only: guard the generated Podman systemd unit against the restart cidfile race, add a checked-in regression,
and document a repeat deployment that proves which image is running.

Explicitly excluded: OMR application behavior, callback delivery, monitoring, production VM mutation, and merge.

## Initial state

- Review-ready, not draft.
- Local focused regression: PASS.
- All OMR Python tests: PASS, 45 tests.
- Diff check: PASS.
- Production VM deployment/restart: NOT RUN; access was not confirmed.
- Hosted CI and review: pending at the time of this record.

## Review items

No actionable review item has been received yet. Record every later CI or review iteration here before claiming review
completion.

## Merge gate

Do not merge without the user's explicit approval. Do not close #52 until the production unit installation and real
restart/journal/external-boundary/image-ID checks succeed.

## Iteration — VM 검증 (2026-09-05)

- Feedback fetched: 호스팅 CI 전 항목 SUCCESS, actionable 리뷰 없음, `mergeStateStatus=CLEAN`
- Validation: 프로덕션 VM에서 재시작 검증을 실제로 수행했고 **수정이 반증됐다.** 수정 전 10회 중 2회 실패,
  수정 설치·실행 확인 후에도 10회 중 1회 실패, 오류 메시지 동일(`Error: remove /run/clairkeys-omr.service.ctr-id:
  no such file or directory` → `status=125`). 근거:
  `docs/recovery/validation/2026-09-05-pr123-vm-restart-falsified.md`
- Production state: 백업 유닛으로 복원 완료. 검증 이전과 동일(`active`, `ExecMainStatus=0`, 이미지 `12b9a021…`,
  health 200, 인증 없는 요청 401)
- Decision: **현재 형태로 병합하지 않는다.** 이 PR의 회귀 테스트는 유닛 파일의 내용을 고정할 뿐 재시작 동작을
  검증하지 않으며, 실제 동작은 개선되지 않는다. 정지 경로로 방향을 바꾸거나 주장 범위를 축소해야 한다.
- Remaining actionable items: PR #123의 향방 결정 (정지 경로 재조준 / 주장 축소 / 닫기)

## Iteration — 재조준과 검증 (2026-09-05)

- Changes made: `92f1dc4` — `ExecStart`에서 `--rm` 제거. cidfile의 소유자를 `ExecStopPost` 하나로 만든다.
  회귀 `test_cidfile_has_exactly_one_owner` 추가
- Validation: 프로덕션 VM 동일 하네스에서 원본 2/10 실패 → `ExecStartPre` 1/10 실패 → `--rm` 제거 **0/40 실패**,
  `status=125` 총계 불변. 컨테이너 누적 없음(1개 유지). 새 회귀가 `--rm` 있는 유닛에서 실패함을 확인.
  `omr-service` 46 테스트 통과. 근거:
  `docs/recovery/validation/2026-09-05-pr123-vm-restart-falsified.md`
- Production state: 기준선 유닛으로 복구 완료. 병합 전 호스트에 미병합 설정 없음
- Decision: 병합 가능 상태. 단 사용자의 명시적 승인 필요
- Remaining actionable items: `92f1dc4`의 CI 확인 → 병합 승인 대기 → 병합 후 VM 배포 및 배포된 유닛으로
  10회 재시작 재확인 → #52 종료

## Iteration — 병합·배포·재검증 (2026-09-05)

- Merge: 사용자의 명시적 승인 후 merge commit `72cfd8a`. 전 체크 SUCCESS, `mergeStateStatus=CLEAN`
- Cleanup: 로컬·원격 tip이 모두 `main`에 포함됨을 확인한 뒤 양쪽 삭제
- Deploy: `main`의 유닛을 VM에 설치(백업 `/root/clairkeys-omr.service.bak.20260905-023452`),
  `daemon-reload` 후 첫 재시작 exit 0
- Re-verification: 배포된 유닛으로 연속 재시작 **10회 중 0회 실패**, `status=125` 총계 불변.
  `active`/`ExecMainStatus=0`/`NRestarts=0`/컨테이너 1개/이미지 `12b9a021…`/health 200/인증 없는 요청 401
- Status: **CLOSED**. 이슈 #52도 종료
- Evidence: `docs/recovery/validation/2026-09-05-issue-52-deployed-unit-verified.md`
