# PR Review Log — PR #96

PR URL: https://github.com/landfill/ClairKeys/pull/96
Branch: `codex/ds-5-home-sample-static`
Last updated: 2026-08-30 KST (생성 직후)

D-031 / 이슈 #76 완료 조건 1·3 개정. Head `53bbd13`. **문서 전용 변경 — 코드 변경 0건.**

## Scope

사용자 지시로 홈 샘플을 정적 예시(GIF 등)로 바꾸기로 했고, 그 전 단계로 재생기를 걷어내고 영역만
확보한다. 구현은 DS-5가 함께 처리하므로 이 PR은 결정과 계획만 바꾼다.

## 이 변경이 푸는 매듭

`PlaybackControls`의 실사용처를 코드로 세어보니 **둘뿐**이다.

| 소비자 | 상태 |
|---|---|
| `AnimationPlayer` (악보 상세) | 살아있음 |
| `FallingNotesPlayer` (홈 샘플) | D-031로 제거 예정 |
| `AdvancedPlaybackControls` | **죽은 코드** — 배럴과 `LazyComponent`가 export만 하고 렌더 0곳 |

D-019 결정 8의 원래 근거였던 demo 페이지는 DS-1이 이미 제거했다(D-027). 홈까지 빠지면 실사용처가
하나만 남아 공유 컴포넌트가 아니게 되므로, DS-5는 구간 반복을 props 플래그나 별도 컴포넌트 없이
직접 넣을 수 있다.

## 완료 조건 변경

| 조건 | 변경 |
|---|---|
| 1 | ①`실제 낙하 노트 결과` → `낙하 노트 결과가 들어갈 영역`. 판정자 `DS-2` → `DS-2(②③), DS-5(①)` |
| 3 | 판정자에서 DS-2 제외 → DS-6 공개 악보 단독 |

조건 1의 완화는 사용자 선택이다. `①을 아예 삭제`하는 안은 기각했다 — 이슈 #76의 원래 문제의식이
"결과를 보여주지 않는 홈"이라, 자리조차 없으면 그 문제로 되돌아간다.

## DS-5가 처리할 파급 (완료 조건에 명시)

깨질 검사 3건을 DS-5 완료 조건에 적었다. D-030에서 요건만 지우고 그 요건을 쓰던 검사를 놓쳐 E2E가
5개 브라우저에서 깨진 전례가 있다.

- `e2e/application-smoke.spec.ts` — `lets a signed-out visitor play the sample without logging in`
  (조건 3이 DS-6 단독이 되므로 DS-6으로 이관 또는 제거)
- 같은 파일 첫 화면 검사의 `낙하 노트 건반` (`[aria-label$="octave marker"]`)
- `src/app/__tests__/page.test.tsx` — `샘플: {제목} · {작곡가}` 표시

자동 소멸: 이월 지적 **C5**(`tempoSource: 'user'`가 "직접 입력"으로 표시)는 재생기 안
`TempoDisplay`라 함께 사라진다.

## 소급 수정하지 않은 것

DS-2는 `DONE`이고 당시 계획대로 만든 것이므로 완료 조건과 Progress를 고치지 않고 **전방 주석**만
달았다. 대체되는 것이지 되돌려지는 것이 아니다. DS-6에는 조건 3의 단독 판정자가 됐다는 사실을 적었다.

## 로컬 검증

| 명령 | 결과 |
|---|---|
| `npx jest --ci` | PASS — 75 suite / 745 test (문서 전용이라 회귀 없음 확인) |
| D-031 참조 정합성 | ROADMAP 5, DS-5 5, DS-2 3, DS-6 2, DECISIONS 1 |

## 상태

- `isDraft: false` (review-ready).
- CI 결과와 리뷰 세 표면(스레드 · `pulls/96/reviews` 본문 · `issues/96/comments`) 조회는 미기록 —
  다음 갱신에서 채운다. CodeRabbit의 outside-diff 지적은 인라인 스레드에 나타나지 않으므로
  리뷰 본문을 반드시 확인한다 (PR #95 경험).
- 병합은 사용자의 명시적 승인 전까지 하지 않는다.
