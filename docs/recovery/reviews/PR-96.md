# PR Review Log — PR #96

PR URL: https://github.com/landfill/ClairKeys/pull/96
Branch: `codex/ds-5-home-sample-static` (삭제됨)
Last updated: 2026-08-30 KST (병합 완료)

D-031 / 이슈 #76 완료 조건 1·3 개정. Head `53bbd13`. **문서 전용 변경 — 코드 변경 0건.**

## Scope

사용자 지시로 홈 샘플을 정적 예시(GIF 등)로 바꾸기로 했고, 그 전 단계로 재생기를 걷어내고 영역만
확보한다. 구현은 DS-5가 함께 처리하므로 이 PR은 결정과 계획만 바꾼다.

## 이 변경이 푸는 매듭

`PlaybackControls`의 실사용처를 코드로 세어보니 **둘뿐**이다.

| 소비자 | 상태 |
|---|---|
| `AnimationPlayer` (악보 상세) | 살아있음 |
| `FallingNotesPlayer` (홈 샘플) | D-031로 제거 예정 |
| `AdvancedPlaybackControls` | **죽은 코드** — 배럴과 `LazyComponent`가 export만 하고 렌더 0곳 |

D-019 결정 8의 원래 근거였던 demo 페이지는 DS-1이 이미 제거했다(D-027). 홈까지 빠지면 실사용처가
하나만 남아 공유 컴포넌트가 아니게 되므로, DS-5는 구간 반복을 props 플래그나 별도 컴포넌트 없이
직접 넣을 수 있다.

## 완료 조건 변경

| 조건 | 변경 |
|---|---|
| 1 | ①`실제 낙하 노트 결과` → `낙하 노트 결과가 들어갈 영역`. 판정자 `DS-2` → `DS-2(②③), DS-5(①)` |
| 3 | 판정자에서 DS-2 제외 → DS-6 공개 악보 단독 |

조건 1의 완화는 사용자 선택이다. `①을 아예 삭제`하는 안은 기각했다 — 이슈 #76의 원래 문제의식이
"결과를 보여주지 않는 홈"이라, 자리조차 없으면 그 문제로 되돌아간다.

## DS-5가 처리할 파급 (완료 조건에 명시)

깨질 검사 3건을 DS-5 완료 조건에 적었다. D-030에서 요건만 지우고 그 요건을 쓰던 검사를 놓쳐 E2E가
5개 브라우저에서 깨진 전례가 있다.

- `e2e/application-smoke.spec.ts` — `lets a signed-out visitor play the sample without logging in`
  (조건 3이 DS-6 단독이 되므로 DS-6으로 이관 또는 제거)
- 같은 파일 첫 화면 검사의 `낙하 노트 건반` (`[aria-label$="octave marker"]`)
- `src/app/__tests__/page.test.tsx` — `샘플: {제목} · {작곡가}` 표시

자동 소멸: 이월 지적 **C5**(`tempoSource: 'user'`가 "직접 입력"으로 표시)는 재생기 안
`TempoDisplay`라 함께 사라진다.

## 소급 수정하지 않은 것

DS-2는 `DONE`이고 당시 계획대로 만든 것이므로 완료 조건과 Progress를 고치지 않고 **전방 주석**만
달았다. 대체되는 것이지 되돌려지는 것이 아니다. DS-6에는 조건 3의 단독 판정자가 됐다는 사실을 적었다.

## 로컬 검증

| 명령 | 결과 |
|---|---|
| `npx jest --ci` | PASS — 75 suite / 745 test (문서 전용이라 회귀 없음 확인) |
| D-031 참조 정합성 | ROADMAP 5, DS-5 5, DS-2 3, DS-6 2, DECISIONS 1 |

## CI 결과 (head `53bbd13`)

**11 pass / 4 skipping.** 문서 전용 변경이라 `changes` 필터가 frontend·core 경로를 감지하지 못해
Build Check·Unit Tests·Security Scan·E2E Tests(그중 한 workflow)를 건너뛰었다. 이는 워크플로 설계
그대로의 동작이고 실패가 아니다. **required check 4개**(`Lint`·`Security Audit`·`Run Tests`·
`E2E Tests`)는 모두 pass했다.

## 리뷰 결과

세 표면을 모두 조회했고 actionable 항목은 **0건**이다.

| 표면 | 조회 | 결과 |
|---|---|---|
| 리뷰 스레드 | `reviewThreads(first:50)` GraphQL | `totalCount=0` |
| 리뷰 본문 | `pulls/96/reviews` | 0건 |
| 일반 코멘트 | `issues/96/comments` | 2건 — 모두 봇 |

**CodeRabbit은 리뷰를 실행하지 않았다.** "별 10개 미만 저장소는 자동 리뷰를 받지 않는다"는 안내만
남겼다(Run ID `a5dc5fda`). PR #95에서 시간당 1회 쿼터를 소진한 상태였다. 코멘트의
`🔍 Trigger review` 체크박스로 수동 실행할 수 있으나, 문서 전용 변경이라 요청하지 않았다.

**PR #95와 다른 점**: #95에서는 인라인 스레드가 `totalCount=0`인데 리뷰 **본문**에 outside-diff
3건이 있었다. 이번에는 본문도 0건이다 — 리뷰 자체가 실행되지 않았기 때문이지, 지적이 없어서가
아니다. 두 상태를 구분해 읽는다.

## 병합

- 사용자의 명시적 승인 후 병합. merge commit `e53ad0f`, 2026-08-30T01:45:39Z (10:45 KST),
  merged by `landfill`. base `main`, head `53bbd13`, 커밋 1개.
- 병합 직전 재확인: `MERGEABLE`/`CLEAN`, required check 4개 pass, 미해결 스레드 0건.

### merge commit `e53ad0f` check-runs

| Check | 결과 |
|---|---|
| Lint | success |
| Security Audit | success |
| Run Tests | success |
| E2E Tests | success |
| Post-merge tests | success |
| Post-merge build | success |

**6/6 성공.** 마지막 완료는 E2E Tests, 2026-08-30T01:49:53Z (10:49 KST).
Vercel commit status: `success`.

## 브랜치 정리

로컬·원격 tip이 모두 `53bbd13`으로 동일하고 `main`에 포함됨을 `git merge-base --is-ancestor`로
확인했다. 어느 tip에도 고유 커밋이 없고 미커밋 변경도 없어 원격 → 로컬 순으로 삭제했다.
남은 브랜치는 `main` 하나, worktree도 `/Users/h0977/dev/ClairKeys` 하나다.
