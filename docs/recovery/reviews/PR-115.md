# PR #115 review log

Date: 2026-09-02 KST
PR: [헤더 계정 메뉴를 디자인 시스템에 맞추고 긴 주소를 담게 한다](https://github.com/landfill/ClairKeys/pull/115)
Branch: `codex/ui-header-user-menu`
Head: `4a2288c`
Last checked: 2026-09-02 KST (리뷰 2라운드 후)

## Current state

- Review-ready PR. **GitHub 이슈 없이 사용자의 브라우저 피드백에서 바로 온 작업**이다 — PR #100·#101과
  같은 경로다.
- **Four Lore commits**: 회귀 `2573a80` → 구현 `7db90eb` → 회귀 확장 `ebfbc8d` → 리뷰 수정 `33e2f66` →
  리뷰 2차 수정 `4a2288c`. **뒤 두 개는 동작을 바꿨다** — 아래 Iteration 2·3 참조.
- **최초 구현 이후 파괴적 동작이 하나 추가됐다**: 모바일 compact 변형에 **로그아웃**. 병합하면 모바일
  메뉴 레이아웃이 바뀐다.
- **DECISIONS.md 신규 항목은 없다.** 기존 결정을 바꾸지 않는다 — DS-1이 세운 토큰과 `Button.tsx`가 기록한
  포커스 링 규약을 이 컴포넌트에 적용할 뿐이다.
- **영향 범위가 넓다.** 이 메뉴는 `Header`에 있어 모든 페이지에 렌더되며, 드롭다운과 모바일 compact 두
  변형이 있다.

## 리뷰어가 볼 만한 지점

1. **테두리가 보고의 핵심이었다.** `border`에 색이 없으면 `currentColor`를 상속한다. 피드백의 computed
   style `border: 1px solid rgb(27, 31, 42)`가 그 증거다 — 헤어라인이어야 할 자리에 잉크색 실선이었다.
2. **보고에 없던 4건을 함께 고쳤다**: 이름 없는 계정의 빈 `<div>`, `<a href>`로 인한 전체 리로드
   (헤더의 나머지는 `Link`), 자체 `focus:ring-blue-500`(`Button.tsx`가 기록한 규약 위반), Escape 미지원과
   `aria-haspopup`/`aria-expanded` 부재.
3. **라벨 변경 `프로필 설정` → `프로필`.** 이슈 #104가 그 화면의 설정을 전부 제거했으므로 옛 라벨은 존재
   하지 않는 화면을 가리켰다.
4. **테스트의 한계를 명시했다.** jsdom은 넘침을 판정하지 못하므로 줄바꿈은 레이아웃이 아니라 그것을
   허용하는 클래스(`break-all`, `truncate`, `max-w-`)로 단언한다. 클래스 단언은 취약하지만 이 환경에서
   가능한 최선이고, 주석과 커밋 `Not-tested`에 적었다.
5. **`LogoutButton`의 기본 className도 함께 토큰화했다** — 같은 메뉴 안에서 이것만 `gray-*`로 남으면
   회귀가 잡지 못하는 자리가 생긴다.

## 검증

- 로컬: `npx jest` 88 suites / **825 tests**(직전 87 / 815, 신규 10), `npx tsc --noEmit` exit 0,
  `npm run lint` 무경고, `npm run build` 성공.
- **미검증**: 브라우저 확인 없음. 관리자 링크는 admin 세션에서만 렌더돼 실행되지 않았다.

## Iterations

### Iteration 1 — PR 생성 (2026-09-02)

Hosted CI 대기. CodeRabbit은 OSS 정책으로 자동 리뷰를 건너뛰므로 수동 요청이 필요하다. PR #113에서는
수동 요청이 실제로 동작했다(PR #111은 rate limited). 독립 리뷰도 함께 돌린다 — **PR #113에서 첫 실행이
low effort로 잡혀 `(none)`을 낸 적이 있으므로 인자 순서를 `high <번호>`로 준다.**

### Iteration 2 — 독립 리뷰 1차 (2026-09-02)

**CodeRabbit은 `Review rate limited`** — PR #111과 같은 결말이라 **이 PR의 리뷰는 독립 리뷰뿐이다.**
(오늘 세 PR 중 CodeRabbit이 실제로 리뷰한 것은 #113 하나다.)

`/code-review high 115` 결과 5건, **전부 이 PR이 고치겠다고 주장한 접근성 영역**이었다. 정확성 결함은 0건.

| # | 지적 | 판정 | 처리 |
|---|---|---|---|
| R1 | 메뉴의 `overflow-hidden`이 전역 `:focus-visible` outline을 항목마다 자른다. **트리거의 커스텀 링을 "전역이 담당한다"며 지운 뒤 그 전역 링을 가린 것** | **ACCEPT** | `33e2f66` |
| R2 | `role="menu"`인데 화살표 키·roving tabindex가 없고 신원 블록·로그아웃이 `menuitem`이 아니다. 스크린리더가 메뉴 모드에서 **로그아웃에 도달하지 못한다** | **ACCEPT (role 제거)** | `33e2f66` |
| R3 | `aria-label="계정 메뉴"`가 보이는 이름을 덮어써 **WCAG 2.5.3 Label in Name 위반** | **ACCEPT** | `33e2f66` |
| R4 | Escape가 포커스를 트리거로 돌려주지 않아 `<body>`로 떨어진다 | **ACCEPT** | `33e2f66` |
| R5 | mock `fetch`가 `act()` 밖에서 resolve돼 10개 테스트가 act 경고를 낸다 | **ACCEPT** | `ebfbc8d` |

**R1·R2가 자기모순이었다.** 링을 지우고 대체물을 가렸고, 구현하지 않은 메뉴 패턴을 선언했다. R2는
`role="menu"`를 유지한 채 전부 `menuitem`으로 만드는 대신 **role을 제거**했다 — 그 role은 화살표 키
네비게이션을 약속하는데 구현이 없다. `/profile`에서 목업을 걷어낸 것과 같은 원칙이다.

**R2가 결함 하나를 드러냈다 — 모바일에서 로그아웃할 방법이 없었다.** 로그아웃은 데스크톱 드롭다운에만
있었고 모바일 메뉴가 쓰는 compact 변형은 아바타와 이름만 렌더했다. **보고에도 최초 구현에도 없던 것이며,
이 PR이 추가한다.** 병합 시 모바일 메뉴 레이아웃이 바뀐다.

### Iteration 3 — 독립 리뷰 2차 (2026-09-02)

수정 폭이 커서(role 제거, 모바일 로그아웃 추가) 새 head에 다시 돌렸다. 3건, 전부 low.

| # | 지적 | 판정 | 처리 |
|---|---|---|---|
| R6 | `aria-haspopup="true"`는 `"menu"`와 동등하다. `role="menu"`를 뺐는데 이것이 남아 여전히 "menu button"이라 알리고 Arrow Down을 유도한다. **1차 수정이 만든 회귀가 이것을 고정하고 있었다** | **ACCEPT** | `4a2288c` |
| R7 | 신원 블록 이름의 `truncate`가 37자 이름을 ~30자에서 자른다. 바로 아래 이메일은 `break-all`로 감싸지는데, **계정을 식별하라는 블록이 식별을 자른다** | **ACCEPT** | `4a2288c` |
| R8 | **PR 본문과 이 리뷰 로그가 stale하다.** 본문은 최초 구현 시점에 쓰여 `role="menu"`를 여전히 해결 항목으로 적고 테스트 수가 틀리며 **모바일 로그아웃 추가를 언급하지 않는다.** 로그는 `Head: 7db90eb`, "Two Lore commits"인데 실제로는 다섯이고 뒤 둘이 동작을 바꿨다 | **ACCEPT (규약 위반)** | 본문 갱신, 이 Iteration |

**R8은 AGENTS.md 단계 4 위반이다** — "CI·리뷰 수정마다 `docs/recovery/reviews/PR-<number>.md`를 같은
방식으로 갱신한다". 리뷰 수정을 두 번 하고도 갱신하지 않아, 리뷰어와 다음 세션이 **모바일 메뉴에 파괴적
컨트롤이 추가된다는 사실을 어느 기록에서도 볼 수 없었다.** 이 PR에서 가장 값어치 있는 지적이다 —
코드가 아니라 인계의 결함이고, 이 저장소가 규약으로 막으려던 바로 그 종류다.

**R6은 회귀가 결함을 고정한 사례다.** 1차 수정에서 `aria-haspopup="true"`를 단언하는 테스트를 넣어,
잘못된 상태에 자물쇠를 채웠다. 회귀가 항상 안전망인 것은 아니다.

재검증: Jest 88 suites / **833 tests**, tsc exit 0, lint 무경고, build 성공.

### Iteration 4 — 병합 (2026-09-02)

`4a2288c`에서 hosted checks **16/16 pass**, 리뷰 스레드 0건(CodeRabbit이 rate limited로 아무것도 남기지
않았으므로 스레드 자체가 없다), `MERGEABLE`/`CLEAN` 재확인.

사용자의 명시적 승인으로 merge commit **`885cfd3`** 에 병합했다. 이 작업은 GitHub 이슈 없이 브라우저
피드백에서 왔으므로 닫힐 이슈는 없다. post-merge check-runs **6/6 success**, Vercel **Production 배포
`885cfd3` success**. 로컬·원격 tip이 `main`에 포함되고 고유 커밋 0건임을 확인한 뒤 원격 → 로컬 순으로
`codex/ui-header-user-menu`를 삭제했다. worktree는 clean `main`이다.

## 운영 확인이 필요한 항목 (병합 후)

jsdom이 판정하지 못해 회귀가 클래스로만 단언한 것들이다. 이 메뉴는 **모든 페이지의 헤더**에 있다.

| 확인할 것 | 회귀가 못 보는 이유 |
|---|---|
| 긴 이메일이 메뉴 안에서 감싸지는가 | jsdom은 넘침을 계산하지 않는다. `break-all` 클래스 존재만 단언한다 |
| 긴 이름이 헤더 트리거를 밀지 않는가 | 같음. `truncate`·`max-w-` 존재만 단언한다 |
| **메뉴 항목의 포커스 링이 잘리지 않는가** | jsdom은 outline 클리핑을 판정하지 못한다. `overflow-hidden` 부재만 단언한다 |
| **줄바꿈된 긴 이름의 높이가 어색하지 않은가** | R7 수정으로 두 줄이 될 수 있다 |
| **모바일 메뉴의 계정 행에 로그아웃이 자연스럽게 놓이는가** | 이 PR이 추가한 파괴적 컨트롤이다. 행 레이아웃이 바뀌었고 좁은 폭에서 줄바꿈될 수 있다 |
| 테두리가 헤어라인으로 보이는가 | 원래 보고의 핵심이었다. 계산된 색이 아니라 눈으로 판정한다 |
