# PR151 — callback origin validation

Date: 2026-09-11
PR: https://github.com/landfill/ClairKeys/pull/151
Published head: d07b855346ec0909cdbed4f2e459e812589bf2ca
Published branch: codex/implement-issue-#110-callback_url-validation

## Recovery and verification

- Original task: https://chatgpt.com/codex/cloud/tasks/task_e_6aa002b0e40c8322a876e3d379366416
- External Edge restored access after the user signed in. UI PR creation succeeded; GitHub confirms non-draft.
- Cloud reports 156 Python cases with six skips, 997 Jest tests, type/lint passing;
  build was blocked by Google Fonts network access. These are recovered claims, not local reruns.
- GitHub Vercel preview succeeded. At initial review, mergeability is CONFLICTING and
  required Actions jobs are absent; a successful preview is not complete validation.

## Actionable review requested in the existing cloud task

1. OPEN: `_origin_tuple` uses `parsed.port or default_port`, aliasing explicit port0 to443/80.
   Add regression-first strict port handling.
2. OPEN: The Jest bridge imports the actual FastAPI app, while unit/post-merge CI installs only
   Pillow. Reproduce in a clean Python environment and provide pinned dependencies in all
   relevant jobs, preserving executable no-send regression coverage.
3. OPEN: Merge latest main and remove stale HANDOFF/dispatch-document differences from this PR.
   Preserve concurrent state records; no force push.
4. OPEN: Test coverage for malformed ports and transport/response log redaction is narrower
   than the cloud summary; add execution evidence or narrow claims.
5. FIXED: Coordinator updated PR body with Related to #110, current publication and review status.

## Delivery boundary

The same task has been asked to fix, validate and prepare an update to this existing PR.
No merge, VM rollout, configuration change or issue closure is authorized by this recovery.
The UI may need to publish the next cloud commit. Read live GitHub state before delivery.

## Main status-check failure

Status-only commit a1931810b3da7d00c1de15748e1a84f098c5998f had five successful checks
and a failed Security Audit. `gh run view 34609081496 --log-failed` reports
`npm audit --audit-level high --json > npm-audit.json` exited1. Advisory details are not
in that output; cause is not yet classified and no fix is claimed.

## Additional coordinator evidence (2026-09-11)

- Executed PR head's stdlib delivery module locally with configured origin
  `https://app.example.com`; `validate_callback_url("https://app.example.com:0/api/omr/finalize")`
  returned the supplied port-zero URL. Finding1 is reproduced, not only inferred from source.
- Downloaded the `npm-audit` artifact of run34609081496. It reports four affected packages:
  `next` critical, `sharp` high, `js-yaml` high, `@humanfs/node` moderate.
  The audit suggests Next15.5.25 and compatible transitive fixes. This is a separate dependency
  follow-up; no dependency change or runtime exploitability claim is made in PR151.
- Advisory IDs in the report: GHSA-p293-qw3h-jr36, GHSA-2xp9-vwfh-vxw4,
  GHSA-rgj7-g3m4-5g8c, GHSA-2883-xcg3-v3hh, GHSA-p498-v437-472g.

## Local recovery revision — 2026-09-11

The cloud retry failed in setup before edits. Coordinator continued locally after user steering.
Merge28c7364 preserves current main's state records; implementation8f1d7fe41aaa240c0cc4ad472c0f6445e157f353
was pushed to the existing PR branch. GitHub confirms MERGEABLE; hosted CI started.

- Finding1 FIXED: explicit port0 and empty ports rejected; local regression reproduced four failures.
- Finding2 FIXED: requirements-ci.txt installed in pr-checks.yml, test.yml and deploy.yml.
  Clean Python import first failed with missing uvicorn; isolated runtime install then passed.
- Finding3 FIXED: both state files match main after the merge.
- Finding4 FIXED: real HTTPX MockTransport exposed request-log query leakage, now suppressed;
  response/exception redaction and retry tests execute, malformed port coverage added.
- Finding5 FIXED: body updated with Related to #110 and publication/review limits.
- Full local results: Jest997; Python160 tests with6 skips; tsc/lint/build passed.
- Hosted checks/reviews and separate security-audit failure remain OPEN. No delivery approval inferred.

## Hosted results after local repair — 2026-09-11

At head8f1d7fe, both E2E jobs, both unit jobs, type/lint/build and Vercel preview pass.
Three initial bot threads (dependency install, stale state docs, port0) are resolved.
Security Audit/Scan fail, causing All Checks Complete to fail. Run34612351013 logs identify
exactly the four previously recorded vulnerable packages. Dependency repair is a separate
follow-up candidate; no security check bypass or approval was performed. CodeRabbit skipped
its review, so its success context is not approval.

## Security propagation and exact configuration review — 2026-09-12

Merged approved main in3a8c92b, then corrected raw ENVIRONMENT comparison in0cba56d.
Review3990381898 accepted: normalized Development/DEVELOPMENT/whitespace variants violated
D-057's exact marker. Added policy and no-client-construction tests before the fix (7 failures).
Initial runtime probe entered real retry waits; stopped that test and bounded attempts to1 in
its mock context only. A wrong edit path caused no modification; corrected before validation.
Final Python162 tests pass with6 skips; callback Jest bridge passes. Combined-dependency full
Jest997/type/lint also pass. Pushed same PR; thread resolved. Hosted checks pending.

## Container-runtime documentation review — 2026-09-12

All hosted checks passed at0cba56d119f931b2278a289b41553fa9a27c1939. Review3990918391 then identified
the pre-rollout test command incorrectly using Rocky's old host Python. Documentation-onlyacc43fb
runs the same tests through the newly built image with --network none, --workdir /app and
--entrypoint python3, without production env mounts. Bash syntax and Dockerfile WORKDIR/COPY/import
dependencies checked; no VM command was executed. Thread resolved; latest hosted rerun pending.

## Final exact-head approval checkpoint — 2026-09-12

Head acc43fbf3128281eb6084e1478af763fe24f75df:14 check-runs successful, no pending/failing check-runs, Vercel preview success.
GitHub confirms non-draft/MERGEABLE and unresolved review count0. CodeRabbit skipped review.
This PR has not been merged. Explicit user approval for PR151 is the remaining delivery gate;
recheck head/checks/review before merge. Related issue remains open for its remaining scope.

## Explicitly approved merge — 2026-09-12

User approved PR151/152 together in response to the named merge question. Exact head/checks and
unresolved-review count0 were rechecked. PR151 merged as 381a17f3b75fe7607008e59fb9655106432e1ac3.
Main synchronized; corresponding local/remote branch tips each have0 unique commits. Existing
user-owned changes block cleanup, so branches/worktrees retained. Post-merge outcomes are tracked
in validation/2026-09-12-feature-merges.md. Related issue remains OPEN for its remaining scope.

## Final post-merge result — 2026-09-12

Exact merge 381a17f3b75fe7607008e59fb9655106432e1ac3: six check-runs successful, none pending or failing.
Combined final merge Production deployment6396973814 succeeded. Public home returned200 and
linked CSS contains compact-playback-bar/speed/volume selectors. Branch cleanup remains blocked
by preserved user-owned changes. Issue scope outside this PR remains OPEN; no OMR VM rollout.
