# PR Review Log — PR #25

PR URL: https://github.com/landfill/ClairKeys/pull/25
Branch: `codex/chore-sharp-advisory`
Base: `main`
State: `OPEN` — review-ready (non-draft). Awaiting hosted CI and the user's explicit merge approval.
Last checked: 2026-07-22 KST

## Scope

Dependency-only fix that unblocks the repo-wide `Security Audit` gate. One commit `9734a4f`: adds an npm `overrides` pin of `sharp` to `>=0.35.0` in `package.json` (resolves to 0.35.3) plus the regenerated `package-lock.json`.

## Why this exists

`npm audit --audit-level high` (the required `Security Audit` check) went red for every PR when libvips advisories (CVE-2026-33327/-33328/-35590/-35591, GHSA-f88m-g3jw-g9cj) were published against `sharp <0.35.0`. sharp is a transitive image-optimization dep of `next@15.5.20` (was 0.34.5). `next` itself was flagged high only because it inherited the vulnerable sharp, so one override clears both high entries. Surfaced as the PR #24 blocker; split out per "one purpose per PR". See `docs/recovery/reviews/PR-24.md`.

## Verification (local, before push)

- `npm install` clean; `npm ls sharp` → `sharp@0.35.3 overridden`.
- `npm audit --audit-level high` → **exit 0**, no high/critical remaining. `next` dropped high→moderate; remaining next-auth/postcss/uuid are moderate (do not fail the gate).
- `npm run build` succeeds (full route manifest, no error) — sharp 0.35 line is compatible with next 15.

## Rejected alternatives

- Admin-merge PRs past the red gate — leaves the debt and normalizes bypassing the security check on every future PR.
- `npm audit fix --force` — downgrades next 15 → 9.3.3, a breaking change.

## CI status

Pending at open — the key check to watch is `Security Audit` turning green on this PR, empirically confirming the override fixes the gate.

## Merge readiness

Awaiting required checks + the user's explicit merge approval. No merge performed. Recommended merge order: land #25 first so #24 re-runs against a green audit baseline (or merge #25 then rebase/re-run #24).
