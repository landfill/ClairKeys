# PR Review Log — PR #21

PR URL: https://github.com/landfill/ClairKeys/pull/21
Branch: `codex/docs-hosting-decision-draft`
Base: `main`
State: `MERGED` at `3349fd3` — CodeRabbit review feedback (C1–C7) all accepted and fixed in `a97e89a`, merged with the user's explicit approval; work branch deleted after tip confirmed in `main`
Last checked: 2026-07-21 KST

## Scope

- Docs-only. Adds `docs/recovery/DECISIONS.md` **D-008** (Status: `Proposed`): OMR service hosting comparison, Fly.io-reuse vs Google Cloud Run.
- On the PR track (not a direct-`main` handoff commit) because `AGENTS.md` lists `DECISIONS.md` new entries as *not* a direct-commit exception — recording a decision is itself a judgment call.
- Captures three things so the next session does not re-derive them:
  1. The deployed `Dockerfile.audiveris` installs neither JRE nor Audiveris and depends on Docker-in-Docker (unavailable on both Fly and Cloud Run), so the service most likely falls back to the server-side demo processor (`audiveris_alt`). Fixing the container is therefore a prerequisite on any host.
  2. The two platforms are technically equivalent for this workload (scale-to-zero, memory, timeout, Tokyo region); the choice reduces to resume-effort (reuse existing Fly config) vs staying near-free (Cloud Run free tier).
  3. Go-live is deferred to P0-B maturity so unverified OMR output is not stored as a successful conversion (D-001).
- Left `Proposed`, not `Accepted`: no host is switched or started on the strength of this entry.

## CI status

Docs-only change; code checks skip (no code paths touched). On the initial head, hosted checks passed (`Lint`, `Run Tests`, `Security Audit`, `E2E Tests`, `CodeRabbit`, `Vercel`, `All Checks Complete`). `git diff --check` clean; referenced `docs/recovery/phases/P0-B-musicxml-converter.md` filename verified. Re-run expected on `a97e89a` after the review fixes.

## Review items

CodeRabbit reviewed the docs draft and raised seven accuracy findings on `DECISIONS.md` D-008. All accepted and fixed in `a97e89a`.

| ID | Severity | Summary | Decision | Status | Evidence |
|---|---|---|---|---|---|
| C1 | high | Cloud Run Always-Free tier is US-region-only → conflicts with the Tokyo `asia-northeast1` assumption; a Tokyo deploy bills from the first request | accept | FIXED | Cost cell rewritten to state US-region limitation + Tokyo billing |
| C2 | medium | Comparison table indented under a `-` list item may not render as a table on GitHub | accept | FIXED | "후보 비교" made a normal paragraph; table de-indented |
| C3 | P2 | Fallback claim wrong: `app.py` picks `audiveris_docker` at **import** time (stdlib import always succeeds), so on a Docker-less host the first `docker run` throws and `process_pdf_background` marks the job **FAILED** — it does not fall through to the `audiveris_alt` demo | accept | FIXED | Verified against `omr-service/app.py`; diagnosis corrected to "job fails, not demo" |
| C4 | minor | Fly "현재 정지 상태" conflated config with observation | accept | FIXED | Restated as user report + scale-to-zero-capable config; live state unverified |
| C5 | minor | Cloud Run request-based billing throttles CPU after the response → current async job/poll model needs CPU-always-allocated (extra cost) or Cloud Tasks/Jobs rework; "near-free" premise incomplete | accept | FIXED | Added "처리(백그라운드) 모델" table row; recommendation qualified |
| C6 | minor | Cloud Run's no-Docker-in-Docker constraint attributed to gVisor; gen2 is a Linux microVM — the real point is no privileged/Docker daemon, generation-independent | accept | FIXED | Cell reworded off gVisor |
| C7 | major | Host switch must include updating `OMR_SERVICE_URL` (routes default to `clairkeys-omr.fly.dev`) and validating `/process`, `/status/:jobId`, `job_id`/result contracts | accept | FIXED | Added as a host-switch completion criterion in Directive |

## Iteration log

### Iteration 1

- Created PR #21 from `codex/docs-hosting-decision-draft` to `main`, review-ready.
- Trigger: resuming a ~1-year-paused project raised whether Fly.io (currently stopped via scale-to-zero) is still the right OMR host. User asked to capture the comparison as a draft in `DECISIONS.md`.
- This review log is a handoff document, committed directly to `main`; the D-008 content itself stays on the PR branch.

### Iteration 2

- CodeRabbit posted seven findings (C1–C7) on D-008's accuracy; all accepted and fixed in `a97e89a`, replied on each thread with the resolving commit.
- Most consequential: C3 corrected a factual error — the deployed service does not silently produce demo output on Fly; it selects `audiveris_docker` at import time and the job **fails** when Docker is unavailable. Verified against `omr-service/app.py`. C1+C5 materially qualified the Cloud Run "near-free" premise (US-region-only free tier; request-based billing incompatible with the current async background-processing model).
- Merged via merge commit `3349fd3` with the user's explicit approval; verified `main` fast-forwarded and the work-branch tip (`a97e89a`) is an ancestor of `main`, then deleted the remote and local `codex/docs-hosting-decision-draft` branches.
