# PR #67 — 재생 건반을 측정 폭과 곡 음역에서 유도한다

- URL: https://github.com/landfill/ClairKeys/pull/67
- Branch: `codex/p1-mobile-playback-geometry` → `main`
- Created: 2026-08-28
- Scope: 이슈 [#65](https://github.com/landfill/ClairKeys/issues/65)의 할 일 1~3. **할 일 4(재생 시 가로 전환)는 범위 밖.**
- Records: **D-017**

## 커밋

| SHA | 내용 |
|---|---|
| `262d94e` | 회귀 테스트 단독 커밋 — 고정 24px가 356px에서 14/52만 넣는다는 사실과, 아직 없는 반응형 계약을 고정. 그 시점 1 failed / 8 passed |
| `60c9fe9` | 반응형 geometry, 흰건반 스냅 크롭, 재생 전용 모드, 장식 스케일, 2px 어긋남, D-017 |

11파일 +250/−39.

## 어떻게 만들어졌나 — Orca 감독형 orchestration

- Run `run_d1078ff1739d` / Task `task_11ab9e6e6604`
- 워커: Codex `gpt-5.6-terra`, effort high, **현재 워크트리 재사용**(분리 안 함), 새 터미널
- Dispatch `ctx_1132cc0fc011` (retry). 첫 dispatch `ctx_fa92878b445e`는 실패 — 아래 참조

### 코디네이터 오류 1건 — 모델 id

첫 워커에 `--model terra`를 넘겼고 Codex가 400으로 거절했다:

```
{"type":"error","status":400,"error":{"type":"invalid_request_error",
 "message":"The 'terra' model is not supported when using Codex with a ChatGPT account."}}
```

실제 id는 `gpt-5.6-terra`다(사용자 `~/.codex/config.toml` 기준). 그 워커는 **18분간 프롬프트만 받은 채 아무것도 하지 않았다.**

**교훈 — `check --wait` 타임아웃만으로는 이 실패를 못 잡는다.** 가이드는 타임아웃을 "실패가 아니라 체크포인트"로 보라고 하고, 그 지침만 따르면 계속 기다리게 된다. 실패를 드러낸 것은 `worker-read`의 **transcript에 preamble 1건뿐이었다는 사실**이었다. `liveness: live`는 살아 있다는 뜻이지 일하고 있다는 뜻이 아니다. transcript가 비어 있으면 원본 터미널(`terminal read`)을 봐야 한다.

## 코디네이터 독립 검증

워커의 테스트를 쓰지 않고 별도 프로브로 `buildResponsiveKeyLayout` 출력을 직접 실행 검사했다.

| 악보 범위 | 사전 계산(코디네이터) | 구현 실측 |
|---|---:|---:|
| F#1–B5 (Love Affair 411노트) | 32건반 / 11.1px | 32건반 / **11.13px** |
| A1–E5 (Deborah's Theme 330노트) | 26건반 / 13.7px | 26건반 / **13.69px** |

단일 노트(C4) 케이스도 확인: 흰건반 1개, `keyWidth` 24px(상한 적용), 대칭 여백. 의도대로다.

### 불변식 경계 조건 — 확인했고 문제 아님

`snapRangeToWhiteKeys`가 `Math.max(A0_MIDI, ...)` / `Math.min(C8_MIDI, ...)`로 클램프하므로, midi 20이나 109인 노트는 layout에 없고 `notesToVisualNotes`가 `if (!keyPos) continue`로 조용히 버린다. **프로브로 실제 재현했다.**

그러나 `normalizeAnimationData`가 `MIDI_MIN=21`/`MIDI_MAX=108` 밖 노트에 **throw**하고 재생 페이지는 모든 JSON을 이를 통과시킨다. **실제 재생 경로에서 도달 불가능**하며, 기존 88건반 고정 코드도 같은 성질이었으므로 회귀가 아니다.

## 검증 (코디네이터가 재실행)

| 명령 | 워커 주장 | 재실행 결과 |
|---|---|---|
| `npx tsc --noEmit` | 통과 | exit 0 |
| `npm run lint` | 통과 | No ESLint warnings or errors |
| `npm test` | 53 suites / 514 tests | 53 suites / 514 tests passed |

회귀 테스트 우선 규약 준수도 확인: `262d94e`는 테스트 파일 단독 커밋이다.

## 검증하지 못한 것

- **실기기 렌더링 없음.** 모든 수치는 소스 좌표 계산과 jsdom 실행 결과다.
- 인증이 필요한 `/sheet/[id]` 재생 화면 E2E 미통과 — `AuthGuard` fixture 부재(이슈 #65 할 일 6).
- 방향 전환(할 일 4), `src/components/mobile/` 죽은 스택 처리(할 일 5) 모두 범위 밖.

## CI 1라운드 — Accessibility Check 실패와 그 원인

`60c9fe9` 기준 17개 체크 중 **Accessibility Check 1건만 실패**했다. CodeRabbit은 11파일 전수 검토 후 **actionable 0건**(요약 코멘트 `updated_at` 03:43:17Z가 최종본).

```
Violation of "scrollable-region-focusable" with 1 occurrences!  - .overflow-x-auto
```

정황이 어긋났다: axe가 검사한 것은 홈페이지(`http://127.0.0.1:3000/`)이고 거기서 `overflow-x-auto`는 `src/app/page.tsx:41` 하나뿐인데 **이 PR은 그 파일을 건드리지 않았고**, PR #64에서는 같은 검사가 통과했다. 그래서 고치기 전에 타당성을 교차검토시켰다.

### 교차검토 — Codex `gpt-5.6-sol` high (Task `task_d168d225421b`, Dispatch `ctx_1a8471472fd6`)

**판정: 이 PR이 결함을 만든 게 아니라 드러냈다.** 측정 근거:

| 측정 | 값 |
|---|---|
| 운영 `main`의 `.overflow-x-auto` | clientWidth 777, scrollWidth **1248**, tabIndex **−1** |
| PR의 C 라벨 주입 후 scrollWidth | **1248 (변화 없음)** |
| axe 단일 규칙 결과 | 0건 → **1건** |

메커니즘: **axe의 `isNoneEmptyElement`가 자손 content를 요구한다.** 빈 `div` 건반만 있던 `main`은 규칙이 건너뛰었고, C1–C8 텍스트 라벨이 생긴 PR은 검사 대상이 됐다. 즉 정적 도구가 "결함 없음"이라 한 게 아니라 **"검사하지 않았다"**였다.

`curl: (7)` 줄은 30회 readiness polling의 정상 첫 실패다 — PR #64도 동일하게 발생한 뒤 통과했다. **CI 환경 문제가 아니다.**

코디네이터가 소스로 검산했다: `overflow-x-auto` 컨테이너에 `tabIndex`가 없고 내부는 `min-w-[800px]` 래퍼 + 1248px 건반이다. 결함은 `main`에 이미 있었다.

**교차검토가 코디네이터 오류 2건도 지적했다**: (1) `pianoLayout.ts`도 홈페이지가 import하는 변경 파일인데 증거 목록에서 빠뜨렸다. (2) black `decorationScale`이 엄밀히 1이 아니라 `0.9999999999999999`다(결론에 영향 없음).

## 수정 — Codex `gpt-5.6-terra` high (Task `task_287e454e8e03`, Dispatch `ctx_6dd6fb4a6414`)

`711641b` — `src/app/page.tsx:41`에 `tabIndex={0}` + `role="region"` + `aria-label="피아노 건반 미리보기"` + `focus-visible:ring`. `SimplePianoKeyboard`에는 손대지 않았다(88개 건반이 전역 tab 순서를 오염시킨다 — 커밋 `Rejected`에 기록).

`src/app/page.tsx`는 원래 이 PR 범위 밖이지만, required check가 빨간 상태로는 병합할 수 없고 이 PR이 드러낸 결함이므로 같은 브랜치에 커밋을 추가했다. 커밋 메시지에 "이 PR이 만든 게 아니라 드러냈다"는 사실과 메커니즘을 남겼다.

### 코디네이터 독립 검증

| 항목 | 워커 주장 | 재실행 |
|---|---|---|
| `npx tsc --noEmit` | 통과 | exit 0 |
| `npm run lint` | 통과 | 경고 0건 |
| `npm test` | 54 suites / 515 tests | 54 / 515 passed |

**회귀 근거를 직접 확인했다**: `git checkout 60c9fe9 -- src/app/page.tsx`로 되돌린 뒤 새 테스트를 돌려 `getByRole('region', ...)`에서 실제로 실패함을 확인하고 복원했다. 통과만 하는 장식용 테스트가 아니다.

## CI 2라운드 — 전부 통과

`711641b` 기준 **Accessibility Check 포함 12/12 pass** (Build, Lint and Type Check, Unit Tests, E2E Tests ×2, Security Scan, Security Audit, CodeQL 등).

**주의 — `711641b`에는 CodeRabbit 리뷰가 없다.** 요약 코멘트의 검토 범위는 `0476983...60c9fe9`이고, 최신 커밋에 대해서는 `Review skipped: manual review required for this OSS repository`로 표시됐다. 앞선 리뷰가 시간당 할당량을 소진했다(`0 remain after this review`). PR #34·#35에서 반복된 rate limit 패턴과 같다. 이 커밋의 리뷰는 **코디네이터의 수동 검토뿐**이다.

## 병합 — 2026-08-28

**사용자의 명시적 승인 후 `4e084a1`로 병합했다.**

병합 직전 재확인: `state=OPEN`, `draft=false`, `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, 실패·대기 체크 0건, 미해결 인라인 리뷰 코멘트 0건.

- 머지 커밋 체크 **6/6 성공** (Lint, Run Tests, E2E Tests, Security Audit, Post-merge build, Post-merge tests)
- **Vercel Production 배포 성공** — `4e084a1`
- 브랜치 정리: 로컬·원격 tip(`711641b`)과 중간 커밋 전부 `main` 포함 확인, 고유 커밋 0건, 미커밋 변경 없음 → 원격·로컬 작업 브랜치 삭제. `main`이 유일한 브랜치다.

### 사고 — 이슈 #65가 자동으로 닫혔다

**코디네이터 실수다.** PR 본문에 이렇게 썼다:

> `Closes #65 의 할 일 1~3. 할 일 4~6이 남아 있으므로 **이슈는 닫지 않는다.**`

GitHub는 그 문장에서 **`Closes #65`만 파싱한다.** 뒤에 붙인 한국어 단서는 파서에게 아무 의미가 없다. HANDOFF와 커밋 `Directive`에 "이슈 #65를 닫지 마라"를 세 번 적어놓고 정작 PR 본문에서 그 규칙을 어겼다.

**조치**: 병합 직후 이슈 #65를 재개하고(`OPEN` 확인), 재개 코멘트에 원인과 남은 할 일 4~6을 명시했다. 병합된 PR 본문에서도 종료 키워드를 제거했다.

**교훈**: 자연어 단서로 기계 동작을 무효화할 수 없다. `Closes #N`은 문장 어디에 있든 발동하므로, **닫지 않으려면 키워드 자체를 쓰지 않는다.** 이 저장소에는 선례가 있다 — 이슈 #48이 수정 커밋 없이 `completed`로 닫혔다가 2026-08-23에 재개됐다.

## 남은 것

- `src/app/test-finger/page.tsx:95`의 다른 `overflow-x-auto`도 같은 결함일 수 있다. axe 검사 대상 페이지가 아니어서 이번 범위에서 제외했다 — **별도 감사 항목**
- **이슈 #65는 열려 있다** — 할 일 4(재생 시 가로 전환), 5(`src/components/mobile/` 죽은 스택), 6(인증 fixture E2E)
- **실기기 확인 없음.** 이 PR의 목적(모바일에서 88건반이 보인다)은 **아직 사람 눈으로 검증되지 않았다.** Vercel Production에 배포됐으므로 이제 확인 가능하다
- 이슈 #65는 할 일 4~6이 남아 있으므로 이 PR 병합으로 닫지 않는다
