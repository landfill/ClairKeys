# PR168 — m1 점 잉크를 곡선 청소로부터 보존 (D-069)

- Date: 2026-09-19
- PR: https://github.com/landfill/ClairKeys/pull/168
- Head: `2458daf` (공백 정리; 런타임 동일)
- non-draft 확인. 병합·운영 배포는 미승인/미수행. 전체 #134 phase IN_PROGRESS.

## 범위·검증

원본의 고립된 점 일부가 slur glyph에 포함되어 SYMBOLS 청소에서 잘리는 기전을 수정했다.
head 오른쪽의 제한된 크기·밀도·종횡비를 만족하는 원본 연결 성분만 eraser clip에서 제외한다.
픽셀 추가와 slur graph 수정, XML/JSON 사후 보정은 없다.

- Clair185→187/191, 정식 이미지3회 동일. m1 C5/E5 duration/dots만 변경; 나머지191 raw의 필드·160canonical 유지.
- 타이30/43 및 누락13/오검출1 개별 목록 보존.52개 곡선 좌표·타이 판정·glyph 픽셀도 동일.
- 기준선 양성 native 실패, 후보54사례 통과. baseline 부정/보존48사례 통과.
- 정식 Dockerfile·source/class 해시 확인. 이미지182통과/6진단skip; native11개 모두 실행.
- corpus12개:11성공 출력 동일,1동일3쪽 SCALE 실패. 타입/lint/Jest105 suites1038 tests 통과.
- [상세 검증·실패 이력·한계·자체 리뷰](../validation/2026-09-19-issue-134-m1-tie-cut-dots.md).

## 현재 head CI·자동 리뷰

- [Codex 실제 리뷰](https://github.com/landfill/ClairKeys/pull/168#issuecomment-5738493559)는 head56736f7에서
  2026-09-19 02:10:41 UTC **Completed**. bot의 PR +1 반응과 inline comments0/GraphQL reviewThreads0을 확인했다.
  새 finding0, 미해결 actionable feedback0이다.
- [CodeRabbit](https://github.com/landfill/ClairKeys/pull/168#issuecomment-5738493291)은 OSS 자동 실행 조건으로 **skip**했다.
  status가SUCCESS로 표시되어도 실제 리뷰 통과로 세지 않는다. 리뷰 근거는 위 완료된 Codex 리뷰다.
- Lint, Lint and Type Check, Security Audit, Unit Tests, Run Tests, Vercel/preview 성공.
  required E2E 두 workflow는 아직 진행 중이다. Build Check/Security Scan은 기존 path 조건의skip이다.
- 현재 head56736f7, non-draft, mergeable MERGEABLE. 필수 E2E 완료 전 goal 완료로 보지 않는다.

## 최종 diff 공백 정리 / 최신 head 재검증

- 최종 `git diff --check main...HEAD`가 patch의 빈 문맥 줄2개에 trailing whitespace를 보고했다.
  작업 중 unstaged diff 검사로는 새 patch 파일 전체를 검사하지 못했으므로 마지막 committed diff 검사가 이를 찾았다.
- `2458daf`에서 해당 문맥 줄의 공백만 제거했다. 전체 실제 Dockerfile로 d069b-patched 재빌드 성공.
  양쪽 이미지의 두 엔진 디렉터리, 모든 JAR 내부 항목, native test classes, Python/Java 테스트 및 애플리케이션 소스를
  해시 비교했다. **40,909항목 전부 동일, Python packages74개 이름/버전 동일**. 실행 코드와 의존성에 차이가 없다.
- 자체 리뷰 finding은 FIXED이며 최종 diff check는 clean이다. 기존12곡 corpus 결과는 정확한 런타임 동등성 근거로 유지한다.
  새 이미지 전체 suite와 Clair3회는 추가 재실행 중이다.
- 최신 head2458daf를 push하고 `@codex review`를 요청했다. 이전56736f7의 완료 리뷰/CI를 최신 head의 완료로 세지 않는다.
  새 current-head CI·실제 재리뷰가 남았다. 병합·운영 변경 없음.

- [최신 Codex 재리뷰](https://github.com/landfill/ClairKeys/pull/168#issuecomment-5738569919)는
  reviewed commit2458daffc8을 명시하고 새 지적 없음으로 완료했다. summary도2458daf,02:21:22 UTC Completed다.
  GraphQL head2458daffc8e563beb3a4dd78144547703fcbb1dc 및 reviewThreads0 확인. 최신 리뷰 미해결 finding0.
  새 이미지 suite/Clair 반복과 current-head E2E는 아직 진행 중이다.
