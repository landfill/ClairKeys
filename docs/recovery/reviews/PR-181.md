# PR181 — 문서 전용 커밋 Vercel 배포 생략 (#178)

Date: 2026-09-22
PR: https://github.com/landfill/ClairKeys/pull/181
Branch: codex/p2a-skip-docs-only-deploys
Head: 0b4b65494022308eb171b08ebd7f98521dd99189

`vercel.json` `ignoreCommand` → `scripts/vercel-ignore-build.sh`(D-077). 마지막 성공 배포 이후 `docs/`, `.github/`, 루트 agent 문서만 바뀌었으면 배포를 생략한다.
판단할 수 없으면 빌드한다. main 30일 이력을 재생하면 push 634건 중 88건만 빌드된다. [계측](../validation/2026-09-22-issue-178-function-storage.md).

## CI — head 0b4b654

필수 Lint, Security Audit, Run Tests, E2E Tests(2)를 포함한 실행 job 전부 성공. Build Check와 Security Scan은 경로 필터로 skip.
Vercel Preview `dpl_C87kqnDtZiz7HQR9ynZMZJk3bdMh` READY(새 브랜치의 첫 배포라 이전 SHA가 없어 빌드됨. 설계대로임). mergeStateStatus CLEAN, non-draft.

## 자동 리뷰 — Codex

| finding | 상태 | 근거 |
|---|---|---|
| P2 D-077의 durable validation 기록 필요 | FIXED | main 상태 기록 `validation/2026-09-22-issue-178-function-storage.md`에 명령·결과·재생 방법·미검증(Vercel에서의 실제 실행) 범위를 기록 |

CodeRabbit은 실제 리뷰 skip.

## 병합 후 확인할 것

- 병합 후 첫 문서 전용 main push가 Vercel에서 "Ignored Build Step"으로 취소되는지 확인한다.
- 사용자의 명시적 병합 승인은 아직 없다. #179와 DECISIONS 끝부분이 겹치므로 나중에 병합하는 쪽은 rebase가 필요하다.

## #179 병합 후 rebase — 2026-09-22

사용자 미커밋 변경을 stash하지 않기 위해 별도 worktree에서 main(f133d26) 위로 rebase했다. 충돌은 `DECISIONS.md` 끝 추가 부분뿐이었고, D-076 뒤에 D-077을 두었다(main 대비 삭제 줄 0).
새 head `2abfffa8c351abc6162858f2c1ba11724fca8e2d`, `--force-with-lease=…:0b4b654`로 push했다.
- 로컬(main lockfile로 `npm ci`): tsc, lint(0), build 통과. Jest 114 suites / 1098 tests 통과(Python 3.10 CI 환경).
- CI: 필수 Lint, Security Audit, Run Tests, E2E Tests(2)를 포함한 실행 job 전부 성공. Build Check와 Security Scan은 필터로 skip. MERGEABLE/CLEAN.
- Codex: 새 커밋 자동 리뷰(04:19Z)와 수동 요청 리뷰(04:33Z) 모두 `2abfffa` 기준 완료, 새 finding 없음. 이전 P2 thread는 FIXED/resolved 유지.
- **Vercel Preview `dpl_5RDpTxKs4vviaaW9Skxcr1LSJvy5` 빌드 로그**: `Running "sh scripts/vercel-ignore-build.sh"` → `Previous deployment 0b4b654… is not available; building.`
  스크립트가 Vercel에서 실행되고 `VERCEL_GIT_PREVIOUS_SHA`를 받는다는 것이 확인됐다. 0b4b654는 force-push로 브랜치 이력에서 빠져 판단 불가 경로(빌드)를 탔으며, 이는 설계대로다.
  이 로그만으로는 Vercel clone 안에서 `git fetch`가 되는지 구분할 수 없다. fetch가 전혀 안 되는 최악의 경우를 depth 10으로 재생하면 push 635건 중 빌드 109건(83% 감소, 그중 depth 때문에 강제된 빌드 21건)이다.
- 이 PR에 대한 사용자의 명시적 병합 승인은 아직 없다.

## 승인 병합·정리 — 2026-09-22

사용자 "승인" 후 head 2abfffa의 실행 CI 전부 성공, 미해결 thread 0, 04:33Z 이후 새 코멘트 없음, MERGEABLE/CLEAN, non-draft를 재확인했다.
`gh pr merge 181 --merge --match-head-commit 2abfffa…`로 `eee9166f2316a40149b1701f16aa779da6787ad7`에 병합, 로컬 main 반영을 확인했다.
로컬·원격 tip 2abfffa가 main에 포함되고 고유 커밋 0, worktree는 main 하나. 원격 삭제 → main 확인 → 로컬 `-d` 순서로 정리했다. 사용자 파일 해시 a1f1ecace6ea는 전후 동일.
Vercel Production `dpl_GMWZdiNPsaYH1dZLvu9H4WLD6wp9` READY/PROMOTED, 24.73MB. 로그: `Running "sh scripts/vercel-ignore-build.sh"` → `Deployable files changed since 1d7564e…; building.`
(`vercel.json`·스크립트·테스트가 바뀌었으므로 빌드가 맞다. 직전 배포 SHA를 clone 안에서 찾아 diff까지 수행했다.)
문서 전용 push의 생략 여부는 이 기록 커밋의 Vercel 결과로 확인한다(아래 절).
