# PR #97 review log

Status: MERGED — 2026-08-30, merge commit `d970faeec5ce698b73763259877fc91452a728f5`. Required contexts `Lint`, `Security Audit`, `Run Tests`, and `E2E Tests` all passed for head `2e497b8f1f8ddbfe41bbe79489f97d62b63df483`. Branch protection matches contexts by name; `test.yml` and `pr-checks.yml` each run an `E2E Tests` job, and both executions passed.

Scope: DS-5 home result-area placeholder and learner-selected A/B playback loops. Original PDF viewer is excluded because its URL/permission data contract belongs to DS-6; `AdvancedPlaybackControls` retains compatibility only although it has no render consumer.

Validation: `npm run lint`, `npx tsc --noEmit`, Jest (78 suites, 751 tests), CI-environment E2E (20), and `npm run build` passed. Physical-device landscape checks remain manual.

## CodeRabbit review — 2026-08-30

CodeRabbit was manually triggered and **did execute a review** of `bc44491`; it posted four inline threads and three review-body nitpicks (seven distinct findings). Its status context still says “Review skipped: manual review required for this OSS repository”, which describes automatic-review availability, not this manual run.

| Finding | Disposition | Evidence / rationale |
|---|---|---|
| A — engine expands a validated A/B endpoint | FIXED | `2e497b8` preserves bounded `start`/`end` as supplied and clears invalid intervals; engine regression covers an endpoint at score duration. Inline reply `r3888364916`. |
| B — invalid B marker is committed | FIXED | `AnimationPlayer` and `useFallingNotesPlayer` now update B only after `createLoopSection` returns a section; hook regression covers invalid then valid B. Inline reply `r3888364877`. |
| C — active loop recreates rAF effect every frame | FIXED | `useMemo([loopStart, loopEnd, totalLength])` stabilizes the loop section dependency. The review-body nitpick has no replyable inline thread. |
| D — compact loop reset has no accessible name | FIXED | The `↻` button now has `aria-label="루프 초기화"`; this is a file-local consistency improvement outside D-030’s named removal scope. Inline reply `r3888364954`. |
| E — A/B test does not exercise state transition | FIXED | State-owning harness asserts A enables B, B selects the active reset state, and reset disables B. The review-body nitpick has no replyable inline thread. |
| F — restore `PlaybackControls` play/pause/stop `aria-label`s | REJECTED | D-030 Decision 4 and Directive explicitly require rejecting this recurring automated-review request unless a user-approved decision supersedes D-030. Tests continue to use `data-testid`, not ARIA selectors. Inline reply `r3888364989`. |
| G — move `LoopSection` to `src/types/` | REJECTED | Existing local utility interfaces (`pianoSampleBank.ts`, `animationCompare.ts`, `playbackClock.ts`) establish a non-uniform convention; moving only this one type would make it less consistent. Repository-wide type-placement normalization is a separate-work candidate. The review-body nitpick has no replyable inline thread. |

Final CI for `2e497b8`: `Lint`, `Lint and Type Check`, `Run Tests`, `Unit Tests`, both `E2E Tests`, `Build Check`, `Security Audit`, `Security Scan`, CodeQL, Vercel, and the PR aggregate check passed.

## Merge outcome — 2026-08-30

The user explicitly approved this merge on 2026-08-30. GitHub merged with the repository's merge-commit strategy at `d970fae`; post-merge check-runs all succeeded: `Security Audit`, `Lint`, `Run Tests`, `Post-merge tests`, `Post-merge build`, and `E2E Tests`. After `git fetch origin`, local and remote `codex/ds-5-learning-player` tips (`2e497b8`) were both ancestors of `origin/main`; with a clean worktree, the remote branch was deleted first and then the local branch.
