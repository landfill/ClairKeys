# PR #100 review log

Date: 2026-08-30 KST
PR: [Unify pill-shaped controls and metadata labels](https://github.com/landfill/ClairKeys/pull/100)
Branch: `codex/ui-button-badge`
Code commit: `7ba646cfbb5da16c74ce2b191925db7baf3c04ed`
Previous fix commit: `6a8c5dd59bf252fbed89529722ffad3fad85ff69`
Latest fix commit: `31b7241bdee0781ac52c10bf81aa30d7cbbb5daa`

## Final state

- Review-ready PR created on 2026-08-30 and merged with explicit user approval on 2026-08-30.
- All PR checks and post-merge checks completed successfully.
- Merge commit: `bff1cc07b92d219c0250f3685f67feb019a75890`.
- The remote and local `codex/ui-button-badge` branches were deleted after confirming both tips
  were contained in `main`.

## User browser feedback

On 2026-08-30, the user reported three visual issues on the preview for `/sheet/2`:

1. A/B/loop controls were visibly smaller than the transport controls.
2. The playback speed select did not fit the surrounding control language.
3. The `전체 설정` disclosure read as an unrelated list box.

Status: `FIXED` in `6a8c5dd`. Transport and loop actions now share one footprint while using
role-specific variants, both playback selects use rounded surfaces, and the settings disclosure
uses a rounded panel with a separated content area. The compact mobile bar received the same
control treatment. Focused and full Jest, typecheck, lint, build, and diff checks passed.

## Follow-up browser feedback

The user then reported that play, pause, and stop were also not self-explanatory. Status: `FIXED`
in `31b7241`. The primary playback row now shows visible Korean labels alongside each icon, and
the A/B controls read `A 시작` and `B 종료`; reset is labeled `초기화`. Compact controls retain
their narrow footprint and expose explicit accessible names and tooltips.

## Change summary

The PR adds a shared pill-shaped `Badge`, makes the shared `Button` pill-shaped, and replaces
page-local button and metadata-label treatments on the active explore, library, search, sign-in,
and sheet-detail paths. Verification evidence is in
`docs/recovery/validation/2026-08-30-ui-button-badge.md`.
