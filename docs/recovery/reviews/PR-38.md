# PR Review Log — PR #38

PR URL: https://github.com/landfill/ClairKeys/pull/38
Branch: `codex/p1-omr-service-contract` (head `9b85d82`)
Last checked: 2026-08-21 19:10 KST

Scope: make the OMR service honour the contract `/api/omr/upload` already assumes — read the
multipart fields it is sent, and fail a job whose result was never stored.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Lint | PENDING | Opened 2026-08-21 |
| Run Tests | PENDING | Opened 2026-08-21 |
| E2E Tests | PENDING | Opened 2026-08-21 |
| Security Audit | EXPECTED FAILURE | Red on `main` since ~2026-08-21 from newly published advisories with no dependency change; 5th occurrence of the PR #25/#27/#31 pattern. Not caused by this PR and not fixable within its scope. |

Repository CI does not run `omr-service/tests/*.py`, so a green PR does not exercise this change.
The evidence is the VM run recorded in
`docs/recovery/validation/2026-08-21-omr-service-contract-fixes-verified.md`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| — | — | — | No review received yet | — | OPEN | — |

## Iteration log

### Iteration 1

- Feedback fetched: none yet — PR opened 2026-08-21.
- Changes made: `app.py` declares `title`/`composer`/`user_id`/`sheet_music_id` with `Form(...)`
  and records `sheet_music_id`; `omr/storage.py` gains `StorageUploadError` and
  `assert_local_fallback_allowed()`, raises on a rejected upload and on any transport failure, and
  keeps the local fallback for development only; `tests/test_service_contract.py` adds 9 tests.
- Validation: 18 tests pass; the VM run binds all four form fields, fails the job at progress 80
  under `ENVIRONMENT=production` without writing a fallback file, and still completes under
  `ENVIRONMENT=development`.
- Commit: `9b85d82`
- Remaining actionable items: none from review.

## Reviewer attention requested

- Whether asserting `app.py`'s request contract from its AST is acceptable, or whether the PR
  should instead add a CI job that installs `omr-service/requirements.txt` and tests the endpoint
  with FastAPI's `TestClient`.
- Whether `ENVIRONMENT` is the right key for the guard, given `NODE_ENV` is what the TypeScript
  equivalent uses.
- Whether a degraded-but-stored outcome should ever exist as its own job status, rather than the
  current binary completed/failed.

## Outcome

`MERGED` at `3208488` on 2026-08-21 with the user's explicit approval. Before merging, the branch was
updated from `main` and re-checked: `mergeable: MERGEABLE`, state `CLEAN`, no failing checks. The
tip is contained in `main` with 0 unique commits on both the local and remote refs.

Post-merge checks on `main` at `3208488` all report success: `Security Audit`, `Run Tests`,
`E2E Tests`, `Lint`, `Post-merge build`, `Post-merge tests`.

No review was received from any reviewer on this PR; CodeRabbit reported
"Review skipped: manual review required for this OSS repository".

Branch deletion is deferred, not forgotten — `.gitignore` was modified and uncommitted at merge
time, and the protocol forbids deleting a branch over user-owned worktree state. PR #40 carries
that change; delete the branches once it lands and `git status --short` is empty.
