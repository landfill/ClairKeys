# PR #57 — 88건반 검은건반 좌표를 MIDI 순서와 흰건반 경계에 맞춘다

- URL: https://github.com/landfill/ClairKeys/pull/57
- Branch: `codex/p1a-piano-black-key-layout` → `main`
- Head: `299951d`
- 상태 확인은 GitHub live state로 한다 (이 문서에 transient 상태를 고정하지 않는다)

## 원인과 수정

`pianoLayout.ts`는 왼쪽 흰건반의 절대 x에 옥타브 C 기준 절대 오프셋을 더했다. C#만 원점이
우연히 같았고 D#/F#/G#/A#은 각각 1/3/4/5칸 밀렸다. 오프셋을 왼쪽 흰건반 기준
`0.65/0.6/0.65/0.6/0.6`으로 바꾸고, 건반과 낙하 노트에 함께 남아 있던 `* 0.2`
렌더 보정을 양쪽에서 제거했다.

## 회귀 근거

코드보다 먼저 88건반 전체 불변식 7개와 두 렌더 소비자의 canonical x 계약을 추가했다.
수정 전 focused Jest는 2 suites / 5 tests가 실패했고, 수정 후 2 suites / 8 tests가 통과했다.

통합 검증:

- `npm test -- --runInBand`: 50 suites / 457 tests 통과
- `npm run lint`: warnings/errors 0
- `npm run type-check`: package script 부재로 실행 불가
- `npx tsc --noEmit`: exit 0

세부 근거: `docs/recovery/validation/2026-08-24-issue-56-piano-black-key-layout.md`

## 리뷰 기록

- 2026-08-24 — review-ready PR 생성 확인 (`isDraft=false`). 최초 CI와 자동 리뷰 대기.
