# PR174 — 신규 DB 구성 문서

- PR: https://github.com/landfill/ClairKeys/pull/174
- Head: `5f3f3faf73bb3fae48476d68ae3b5642956403eb`
- 2026-09-20: non-draft 생성. 최초 CI 진행 중, 실제 리뷰 대기.
- 자체 검증: [DB 설치 실행](../validation/2026-09-20-db-setup-docs.md).
- 자체 수정: private Storage 권장과 실제 public URL 코드 충돌을 해결된 것으로 표현하지 않고 제약 명시.
- 병합 미수행. 새 PR 병합은 별도 승인 대상이다.

## 2026-09-20 실제 리뷰 대응

- Codex 5f3f3fa 리뷰 P2: 기존 DB에서 provenance 선행 migration 없이 index부터 적용하면 부분 실패.
- FIXED `886448f`: 모든 미적용 선행 SQL을 순서대로 적용·이력 기록한 다음 concurrent index, 이후 migrate deploy.
- 로컬 이전 순서 재현: 001/002/20250816만 적용 후 index 실행은 provenance 없음으로 실패.
- 수정 순서 재검증: 독립 corrected DB에 원본7개 순차 적용·resolve·status 성공.
- 해당 review thread resolved, 현재 head 재리뷰 요청. CI/재리뷰 진행 중.
- CodeRabbit는 자동리뷰 skip이며 통과로 계산하지 않는다.

- 886448f 재리뷰 P2 두 건 FIXED `66054e3`: 스키마 존재/이력 없음의 검증 후 baseline 절차 명시;
  실제 플레이어 `/api/sheet/[id]`→animationDataUrl 직접 fetch 경로로 문서 수정.
- 원본 SQL을 수동 적용한 뒤 resolve하는 baseline 경로는 최초 설치 검증에서 실행했다.
  partial schema는 자동 baseline하지 않도록 명시했다. 플레이어44/63/73행으로 실제 흐름 확인.
- 현재 head66054e3 재리뷰 요청, 최신 CI 확인 대기. 이전 head 통과를 대체 근거로 사용하지 않는다.

## 최종 확인 — 2026-09-20

- 최종 head: `66054e382b7f78d9ced9789ee65b72d218886146`.
- 필수 Lint·Security Audit·Run Tests·Tests workflow E2E 모두 SUCCESS(E2E8m23s).
  PR Checks의 문서 변경용 E2E/Unit/Build/Security skip은 실제 실행 통과와 구분한다.
- Codex 재리뷰 2026-09-20T08:59:03Z 완료, 새 finding0, 전체3threads resolved.
- CodeRabbit는 skip. 실제 리뷰 통과로 계산하지 않았다.
- PR non-draft, mergeable. 병합/운영DB·VM변경/이슈종료 미수행.
- 검증용 Docker 컨테이너 및 해당 임시볼륨 제거 완료. 기존 사용자 파일은 보존.

## 2026-09-21 최신 main 대비 병합 검토

검토 head `66054e382b7f78d9ced9789ee65b72d218886146`, main `dfa23ce2a3fb2ce7bd5d1c820a11b78f791e7add`.
판정: **수정 후 병합 권고**, 실제 병합하지 않음.

- P2 OPEN: DATABASE_SETUP.md 76–77행의 소유자 전용/공개 악보 XML 비공개 설명과 SUPABASE_STORAGE_SETUP.md 44행의 소유자 전용 설명은 최신 main의 PR175/D-075와 불일치한다. 현재 score API는 public sheet를 누구에게나 제공하고 private sheet만 소유자로 제한한다. DB RLS 직접 접근 차단과 서버 API의 공개 제공을 구분하고, DATABASE_SETUP.md 서비스 연결 검증의 비로그인 접근 거부도 비공개 악보로 한정해야 한다.
- `gh pr view/checks`, GitHub review/comments/GraphQL threads 확인: non-draft, MERGEABLE/CLEAN, 실행된 CI 모두 성공, 기존3threads resolved, 최신 head Codex finding0. CodeRabbit 실제 리뷰 skip, PR Checks 문서 필터 skip은 실행 통과로 계산하지 않음.
- PR의 6개 문서 전체와 migration SQL7개/schema/Storage/player/score API 대조. PR head 자체에는 기존 owner-only 설명이 맞지만 병합 대상 main의 후속 권한 변경 때문에 위 finding이 생겼다.
- `git diff --check origin/main...HEAD`는 PR branch에서 통과. DB 설치·앱 테스트는 이번 세션에서 재실행하지 않았으며 기존 설치 검증 기록을 확인했다. hosted Supabase 신규 구성은 미검증.
- 사용자 미커밋 파일 보존. 병합 승인 전에 위 finding 수정 및 새 head CI/리뷰 재확인이 필요하다.

## 2026-09-21 수정 및 승인

사용자 "고친뒤 병합하라"로 PR174 수정 후 병합 승인.
FIXED `c5e6f6a414edf48e22dce50de5e3ab248ab4d4a8`: DB 직접 RLS와 서버 API 권한을 구분하고 공개 XML 비로그인 조회/비공개 XML 비소유자404를 명시. 최신 main을 merge하여 D-075 기준으로 재검토했다.
`npm test -- --runInBand --runTestsByPath 'src/app/api/sheet/[id]/score/__tests__/route.test.ts'`: 13/13 통과. `git diff --check` 통과. 새 hosted Supabase 설치는 미실행.
현재 head의 자체 재리뷰에서 추가 finding 없음. 최신 원격 CI 및 자동 리뷰 상태 확인 후 승인된 병합을 진행한다.

## 2026-09-21 병합 및 절차 오류

PR174는 사용자 승인으로 `56f15fb0b9bb8f56d873e2c9596bd9389c5a63eb`에 병합됐고 main 반영을 확인했다. head c5e6f6a의 CI는 E2E 포함 성공했다.
단, 마지막 GraphQL 결과의 신규 unresolved 2threads를 읽기 전에 같은 명령 묶음에서 merge를 실행한 절차 오류가 있었다. 완료로 표시하지 않는다.
- ACCEPTED P1: 기존 DB 업데이트의 psql 명령에 DATABASE_URL이 빠져 SQL과 Prisma가 다른 DB를 대상으로 할 수 있음.
- ACCEPTED P2: 인덱스 resolve의 bare prisma 명령은 로컬 설치 CLI를 찾지 못할 수 있음.
두 finding은 후속 문서 PR로 수정한다. 병합된 브랜치는 재사용하지 않으며 후속 PR 병합은 해당 PR의 명시적 승인 대상이다.

후속 PR176 c1e20a9에서 위 P1/P2를 수정했고 최신 CI/E2E·리뷰 검토까지 완료했다. PR176 병합 승인은 별도 대기다. PR174 merge56f15fb의 post-merge checks6개 전부success를 확인했다.

2026-09-21: PR176이51daf9a로 병합되어 미검토됐던P1/P2 모두 FIXED/main 반영. 관련 두 review threads resolved, PR174/176 브랜치 모두 정리 완료. 상세는PR176 로그 참조.
