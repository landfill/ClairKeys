# PR174 — 신규 DB 구성 문서

- PR: https://github.com/landfill/ClairKeys/pull/174
- Head: `5f3f3faf73bb3fae48476d68ae3b5642956403eb`
- 2026-09-20: non-draft 생성. 최초 CI 진행 중, 실제 리뷰 대기.
- 자체 검증: [DB 설치 실행](../validation/2026-09-20-db-setup-docs.md).
- 자체 수정: private Storage 권장과 실제 public URL 코드 충돌을 해결된 것으로 표현하지 않고 제약 명시.
- 병합 미수행. 새 PR 병합은 별도 승인 대상이다.

## 2026-09-20 실제 리뷰 대응

- Codex 5f3f3fa 리뷰 P2: 기존 DB에서 provenance 선행 migration 없이 index부터 적용하면 부분 실패.
- FIXED `886448f`: 모든 미적용 선행 SQL을 순서대로 적용·이력 기록한 다음 concurrent index, 이후 migrate deploy.
- 로컬 이전 순서 재현: 001/002/20250816만 적용 후 index 실행은 provenance 없음으로 실패.
- 수정 순서 재검증: 독립 corrected DB에 원본7개 순차 적용·resolve·status 성공.
- 해당 review thread resolved, 현재 head 재리뷰 요청. CI/재리뷰 진행 중.
- CodeRabbit는 자동리뷰 skip이며 통과로 계산하지 않는다.

- 886448f 재리뷰 P2 두 건 FIXED `66054e3`: 스키마 존재/이력 없음의 검증 후 baseline 절차 명시;
  실제 플레이어 `/api/sheet/[id]`→animationDataUrl 직접 fetch 경로로 문서 수정.
- 원본 SQL을 수동 적용한 뒤 resolve하는 baseline 경로는 최초 설치 검증에서 실행했다.
  partial schema는 자동 baseline하지 않도록 명시했다. 플레이어44/63/73행으로 실제 흐름 확인.
- 현재 head66054e3 재리뷰 요청, 최신 CI 확인 대기. 이전 head 통과를 대체 근거로 사용하지 않는다.

## 최종 확인 — 2026-09-20

- 최종 head: `66054e382b7f78d9ced9789ee65b72d218886146`.
- 필수 Lint·Security Audit·Run Tests·Tests workflow E2E 모두 SUCCESS(E2E8m23s).
  PR Checks의 문서 변경용 E2E/Unit/Build/Security skip은 실제 실행 통과와 구분한다.
- Codex 재리뷰 2026-09-20T08:59:03Z 완료, 새 finding0, 전체3threads resolved.
- CodeRabbit는 skip. 실제 리뷰 통과로 계산하지 않았다.
- PR non-draft, mergeable. 병합/운영DB·VM변경/이슈종료 미수행.
- 검증용 Docker 컨테이너 및 해당 임시볼륨 제거 완료. 기존 사용자 파일은 보존.
