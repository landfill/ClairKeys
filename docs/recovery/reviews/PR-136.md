# PR Review Log — PR #136

PR URL: https://github.com/landfill/ClairKeys/pull/136
Branch: `codex/issue-130-run-lookahead` (head `554eed9`)
Issue: [#130](https://github.com/landfill/ClairKeys/issues/130) 4단계 — **보류하고 그 이유를 고정한다**
Last checked: 2026-09-05 KST — **MERGED** (merge commit `6ab2d7d`)

## 목적

4단계(모델 변경)가 실패했고, 실패 원인이 또 목표 지표에 있음을 밝혔다. 운영 코드는 바꾸지 않고,
검증 하네스의 구멍 두 개를 막고 `wastedHandTravel`을 실격시킨다.

## 4단계 시도 기록

Codex 워커에 위임했다(dispatch `ctx_13a958ff0bae`, 현재 워크트리 공유). 워커가 lookahead 가설을 여러
형태로 구현했으나 `wastedHandTravel` 36/155를 하나도 낮추지 못했다:

| 시도 | 결과 |
|---|---|
| zero-travel edge 비용 | 37~51로 악화 |
| projected-anchor | 51로 악화 |
| same-finger 보정 | 53으로 악화 |

워커가 `ask`로 "실패 보고로 정리할까요, 특정 방향을 원하나요"라고 물었다. **답하기 전에 코디네이터가
지표에 바닥이 있는지 직접 측정했고, 뒤집혀 있음을 확인했다.** 워커에게 실패 보고를 지시하고 트리를
되돌리게 했다.

## 결정적 측정

corpus 3마디 왼손에서 모델 `5 1 2 1 2`가 **2/4**, 정석 `5 4 2 1 2`·`5 3 2 1 3`이 각각 **3/4**.
5^5 전수 탐색 1등은 **1/4**이고 그 운지는 `1 4 1 4 1`이다.

## 하네스의 구멍 두 개

1. **구별 능력 미검사** — 음계 악구에서 정석·결함이 둘 다 0/7. 역전만 보므로 `0 > 0`이 거짓이라 통과.
2. **결함이 사는 악구 부재** — 세트가 음계 3개 + 도약 1개였다. 아르페지오를 넣자마자 즉시 실격.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| (PR 생성 직후) | — | 아래 Iteration 1에 기록 |

로컬 검증 전부 통과: Jest 904건, `tsc --noEmit`, `next lint`, `next build`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | self | 4단계 목표 | `wastedHandTravel`이 아르페지오에서 역전한다 | 실격. 삭제하지 않고 역전을 테스트로 고정 | CLOSED | D-045 결정 1 |
| R2 | self | 검증 하네스 | 구별하지 못하는 지표가 통과한다 | 구별 능력 요건 추가 — 최소 한 악구에서 엄격히 분리 | FIXED | D-045 결정 2 |
| R3 | self | 검증 세트 구성 | 이슈가 다루는 악구가 세트에 없었다 | 3마디 아르페지오 추가 | FIXED | D-045 결정 3 |
| R4 | self | 4단계 강행 여부 | 지표 없이 모델을 고칠 것인가 | 보류. 목표가 없으면 개선을 정의할 수도 측정할 수도 없다 | CLOSED | D-045 결정 5, Rejected 4 |
| R5 | 사용자 | 진행 방식 | 실제 마디 특정이 까다롭다 | PR은 타협하고 아르페지오 판단은 후속 확인 대상으로 남긴다 | CLOSED | 2026-09-05 사용자 확인 |

## Iteration log

### Iteration 1 — 생성 (2026-09-05)

- Changes made: 검증 세트에 아르페지오 추가, 구별 능력 요건 추가, `wastedHandTravel` 실격,
  D-045, 단계 문서 `BLOCKED` + `Blocked on` 절
- 워커 기동 중 첫 디스패치가 `agent_prompt_blocked`로 실패했다. Codex CLI의 업데이트 다이얼로그가
  입력을 막고 있었고, 커서 기본값이 `1. Update now (curl … | sh)`이라 무심코 Enter를 보내면 원격
  스크립트가 실행될 뻔했다. 방향키로 `3. Skip until next version`을 선택해 재발까지 막고
  `--retry-of`로 재기동했다. Orca가 이 상태를 `blockedReason: codex-update-prompt`로 진단한다.
- Validation: Jest **904건**, `tsc --noEmit` exit 0, lint 경고 0, build 성공
- Remaining actionable items: 없음. 사용자의 명시적 병합 승인 대기.

### Iteration 2 — 병합 (2026-09-05)

- 승인 후 재확인: CLEAN / MERGEABLE / Draft 아님 / 전 체크 pass / 미해결 thread 0건 / auto-close 패턴 없음.
- `gh pr merge 136 --merge` → merge commit `6ab2d7d`. 병합 후 이슈 #130·#126 모두 OPEN 유지 확인.
- `main` post-merge 체크 전부 success. `main`에서 검증 하네스 14건 통과 재확인.
- 브랜치 정리: `codex/issue-130-run-lookahead` 로컬·원격 tip 모두 `554eed9`, `main` 포함 확인,
  고유 커밋 없음 → 양쪽 삭제.
- **`codex/issue-130-directional-budget`은 여전히 로컬에 남아 있다.** 고유 커밋 2개를 가진 폐기된 시도의
  기록이며 병합 대상이 아니다(AGENTS.md가 고유 커밋 있는 브랜치의 삭제를 금지한다).

## 병합 후 남는 것

- **아르페지오 운지 판단이 미해결이다.** 진전에는 실제 화면에서 이상한 마디와 그 정답 운지가 필요하다.
  이슈 #130에 코멘트로 기록했다.
- 남은 유효 목표: `sameFingerLeaps`(10/155, 약함), `longestRepetitionRun`(3),
  `pitchChangingRepetitionRuns`(1). 셋 다 이미 양호하다.
- 5단계(도약 예외 재정의)도 같은 이유로 근거가 부족하다.
- 이 PR은 이슈 #130을 닫지 않는다.
