# PR Review Log — PR #39

PR URL: https://github.com/landfill/ClairKeys/pull/39
Branch: `codex/p1-audit-advisory-pins`
Last checked: 2026-08-21 19:10 KST

Scope: dependency-only. Restore the `Security Audit` required check, which newly published
advisories turned red on `main` with no dependency change. Split from PR #37 and #38 following the
PR #25-from-#24 and PR #31-from-#30 precedent.

## CI status

All checks passed; no failures.

| Check | Status | Last evidence |
|---|---|---|
| Security Audit | **SUCCESS** | The point of the PR — red on `main`, green here |
| Run Tests | SUCCESS | Locally 42 suites / 389 tests also pass |
| E2E Tests | SUCCESS | Not run locally |
| Lint | SUCCESS | Locally exit 0, no warnings |
| Lint and Type Check | SUCCESS | Locally `tsc --noEmit` exit 0 |
| Build / Vercel | SUCCESS | Preview deployment completed |
| All Checks Complete, PR Summary, Detect changes | SUCCESS | — |
| CodeRabbit | SKIPPED | "manual review required for this OSS repository" |

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| — | — | — | No review received yet | — | OPEN | — |

## Iteration log

### Iteration 1

- Feedback fetched: none yet — PR opened 2026-08-21.
- Changes made: `package.json` overrides gain `nanoid >=3.3.18 <4`, `deepmerge-ts >=8.0.1 <9`, a
  bump of `brace-expansion` to `>=5.0.9 <6`, and two **nested** `js-yaml` pins —
  `@eslint/eslintrc` to `>=4.3.1 <5` and `@istanbuljs/load-nyc-config` to `>=3.15.1 <4`.
- Validation: audit exits 0; both `js-yaml` majors resolve side by side; `prisma generate`, `tsc`,
  lint, and build all exit 0; 42 suites / 389 tests pass.
- Commit: on `codex/p1-audit-advisory-pins`
- Remaining actionable items: none. CI is fully green and the required `Security Audit` check passes, which unblocks the merge button for PR #37 and #38.

## Reviewer attention requested

- **`deepmerge-ts` is forced across a major** under `prisma`, which pins it to exactly `7.1.5` in
  every published version including `7.10.0-dev.58`. `prisma generate` succeeds, but migrations
  against a real database were not exercised. If prisma later breaks in a way that traces to
  config loading, this pin is the first thing to check.
- Whether the recurrence itself should be addressed rather than patched a fifth time — for example
  by auditing against a pinned advisory snapshot, or by moving `Security Audit` off the required
  list and running it on a schedule instead. That is a policy decision, not a dependency one.

## Outcome

`MERGED` at `9b31d82` on 2026-08-21 with the user's explicit approval. Before merging, the branch was
updated from `main` and re-checked: `mergeable: MERGEABLE`, state `CLEAN`, no failing checks. The
tip is contained in `main` with 0 unique commits on both the local and remote refs.

Post-merge checks on `main` at `3208488` all report success: `Security Audit`, `Run Tests`,
`E2E Tests`, `Lint`, `Post-merge build`, `Post-merge tests`.

No review was received from any reviewer on this PR; CodeRabbit reported
"Review skipped: manual review required for this OSS repository".

Branch deletion is deferred, not forgotten — `.gitignore` was modified and uncommitted at merge
time, and the protocol forbids deleting a branch over user-owned worktree state. PR #40 carries
that change; delete the branches once it lands and `git status --short` is empty.
