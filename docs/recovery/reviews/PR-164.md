# PR164 — 빔 끝을 기둥에 고정해 곡선과 합쳐진 빔이 삭제되지 않게 한다 (D-065, Clair m9)

Date: 2026-09-18
PR: https://github.com/landfill/ClairKeys/pull/164
Head: c36f26b (생성 시)
Branch: `codex/issue-134-m9-beam-extension`
Issue: #134 (부분 — 본문은 `Related: #134`이고 `closingIssuesReferences`가 0건임을 확인했다)

- 생성 시 non-draft, `mergeable=MERGEABLE`. **병합 승인은 존재하지 않는다.** 운영 배포 승인도 없다.
- 2026-09-18 사용자가 선택지 (a)("우려를 known limit으로 명시하고 그대로 PR")를 선택해 PR 생성만 승인했다.
- 브랜치는 main `1391c4d` 위에 있고 그 이후 main의 상태 기록 커밋을 포함하지 않는다.
  검증 기록이 `b59ea08`·`bea1431` SHA를 인용하므로 rebase하지 않았다. 세 점 diff는 의도한 9개 파일뿐이다.
- 커밋 3개:
  - `b59ea08`: D-065 원본(확장 금지 + 끝 되돌리기). main `362a00f`에서 cherry-pick했고 `git diff 362a00f b59ea08 -- omr-service src`는 비어 있다.
  - `bea1431`: 두께 가드(결정 3b, 기준 빔 두께의 0.95), Deborah m24형 합성 fixture `slur_across_stems`, 정적 계약 단언, Jest 목록.
  - `c36f26b`: D-065 결정 기록을 2차 독립 검증 결과에 맞게 정정. 반증된 코디네이터 가설을 확정된 기전으로 교체하고 Medium 3·Low 2를 known limit으로 명시.
- 근거: [2차 독립 검증](../validation/2026-09-17-issue-134-m9-beam-trim-thickness.md), [단계 추적](../validation/2026-09-16-issue-134-m9-beam-stage-trace.md).
  - Codex 워커(`gpt-5.6-sol` high)가 8단계를 처음부터 재실행해 **PASS WITH CONCERNS**: 무결성 통과, `--no-cache` 빌드 171.24초,
    이미지 테스트 180 OK/skip 0, fixture 판별 18회 통과, Clair 171/191 ×3(m9만 변화), corpus 비교 가능한 11곡 모두 D-064와 동일, 시간 +1.5%.
- hosted 체크 **전부 pass**(2026-09-18, head `c36f26b`): All Checks Complete, Build Check, CodeQL, Detect changes, E2E Tests ×2,
  Lint, Lint and Type Check, PR Summary, Run Tests, Security Audit, Security Scan, Unit Tests, Vercel, Vercel Preview Comments.
  CodeRabbit은 OSS 수동 리뷰 정책으로 skip. `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.
- Codex 자동 리뷰는 `c36f26b`에서 Completed이고 **리뷰·인라인 지적 0건**이다. PR에 👍 반응(모든 리뷰가 findings 없이 완료)을 남겼다.
  아래 Medium·Low 5건은 2026-09-17 워커의 8단계 독립 검증에서 나온 것이며 이 자동 리뷰가 새로 제기한 것이 아니다.
- 미해결 actionable review는 없다. **병합 승인은 여전히 존재하지 않는다.**

| Finding | 상태 | 대응 |
| --- | --- | --- |
| Codex 워커 Medium: 두께로 거부한 후보가 `rawSystemBeams`에 남아 공유 기둥을 CENTER로 만들고 진짜 빔까지 REDUCTION에서 삭제된다 | REJECTED (한계로 기록) | 2026-09-18 사용자 결정 (a). 결과가 D-064와 바이트 동일이라 새 손해가 아니라 구제 기회 상실이다. D-065 Known limit (`c36f26b`) |
| Codex 워커 Medium: `minTrimHeightRatio` 0.95는 보편적으로 안전한 하한이 아니다 (corpus 7개 페이지에 0.95 미만 인쇄 빔, 최소 0.838) | REJECTED (한계로 기록) | 같은 결정. 실행된 회귀 0건. D-065 Known limit + Directive(임계값 변경 시 분포 재측정) (`c36f26b`) |
| Codex 워커 Medium: 끝 기둥 seed 미검출 정상 빔이 안쪽 seed까지 잘릴 위험 | REJECTED (한계로 기록) | 같은 결정. corpus 사례도 전용 fixture도 없다. D-065 Known limit (`c36f26b`) |
| Codex 워커 Low: 모집단 선택(`instanceof SmallBeamInter`)과 재계산(`getItemParams` 최근접 높이)이 어긋날 수 있다 | REJECTED (한계로 기록) | 같은 결정. 어긋나는 corpus 사례 없음. D-065 Known limit (`c36f26b`) |
| Codex 워커 Low: 새 fixture가 "진짜 빔도 읽히는지"를 검사하지 않아 첫 번째 기전이 숨을 수 있다 | REJECTED (한계로 기록) | 같은 결정. 두 빌드 모두 못 읽어 지금은 판별 fixture를 만들 수 없다. 선택지 (b) 선행 필요. D-065 Known limit (`c36f26b`) |
| 코디네이터 가설: 비율 1.01인 진짜 빔까지 가드가 거부한다 | SUPERSEDED | 워커가 BEAMS·STEMS를 분리해 반증했다. `#1616`은 이미 양 끝이 기둥에 고정돼 자르기 대상이 아니다. `c36f26b`에서 기록 교체 |

## 남은 선택지 (이 PR 밖)

- (b) 두께 미달로 거부한 후보를 `rawSystemBeams`에 남기지 않는다. 진짜 빔까지 살릴 가능성이 있으나 BEAMS 단계 범위가 커지고 corpus 12곡 재검증이 필요하다.
- (c) fixture에 "진짜 빔도 읽혀야 한다"를 추가한다. 지금은 두 빌드 모두 실패하므로 (b)가 선행돼야 한다.

## 병합 (2026-09-18)

- 사용자가 "승인"으로 PR164 **병합만** 승인했다. 직전 메시지에서 운영 VM 배포는 별도로 여쭙겠다고 명시했으므로 배포 승인은 아니다.
- 병합 직전 head `c36f26b` 재확인: hosted 체크 16개 전부 pass(All Checks Complete, Build Check, CodeQL, Detect changes,
  E2E Tests ×2, Lint, Lint and Type Check, PR Summary, Run Tests, Security Audit, Security Scan, Unit Tests,
  Vercel, Vercel Preview Comments, CodeRabbit(OSS 수동 리뷰 정책으로 skip)). 리뷰 0건·인라인 0건. `mergeStateStatus=CLEAN`.
- `gh pr merge --merge --match-head-commit c36f26bb…`로 병합했다. 병합 커밋 `bbcc09b40afa88d26e404033da1e35a810508115`.
  - PR163 때와 달리 `--subject`·`--body`로 Lore 형식 메시지를 넘겨 병합 커밋에 trailer가 실렸다
    (`git log -1 --format='%(trailers:only)' bbcc09b`로 확인). Directive에 "이 병합 승인은 VM 배포를 포함하지 않는다"를 남겼다.
- main 반영 확인: `omr-service/audiveris-patches/0004-beam-end-stem-anchor.patch`, 두 합성 fixture와 네이티브 테스트,
  D-065 Known limit 5건이 모두 `main`에 있다.
- 브랜치 정리: 로컬·원격 tip 모두 `c36f26b`이고 main 대비 고유 커밋 0을 확인했다.
  그러나 **사용자 미커밋 변경(`validation/2026-09-13-handoff-history.md`)이 남아 있어 AGENTS 규약에 따라 두 브랜치를 보존했다.**
  삭제하려면 사용자의 명시적 지시가 필요하다.

## 배포 (2026-09-18)

- 사용자가 "vm 도 배포하라"로 운영 VM 배포를 별도 승인했다. PDF 업로드 스모크는 이 승인에 포함하지 않았다.
- image `9eef7512…`, 롤백 태그 `rollback-pr164-20260918`(`2a71ede5…`), revision 라벨 `bbcc09b…`.
- 이미지 테스트 180 OK / 6 skipped. 새 빔 네이티브 테스트 2개와 기존 네이티브 3개가 skip 없이 ok.
- 전환 전 게이트: `BeamsBuilder.class` 해시가 normal·recovery 양쪽 모두 로컬 검증 빌드와 같은 `8d15e1b2…`,
  직전 운영은 기록된 기준선과 같은 `324132f0…`.
- 전환 후 health 200, 무인증 `POST /process` 401, journal error/traceback 0줄.
- **운영 인식 결과는 아직 확인하지 않았다.** [배포 기록](../validation/2026-09-18-d065-beam-stem-anchor-deployment.md).
