# PR #116 review log

Date: 2026-09-03 KST
PR: [Fix nested main landmarks in shared page layouts](https://github.com/landfill/ClairKeys/pull/116)
Branch: `codex/issue-114-main-landmark`
Head: `3bec12e`
Last checked: 2026-09-03 KST (hosted CI와 CodeRabbit 리뷰 완료)

## Current state

- Review-ready PR, issue #114를 `Closes #114`로 연결했다.
- Two Lore commits: 회귀 `db4cc6c` → 구현 `3bec12e`.
- 루트 레이아웃과 `MainLayout`을 함께 구성한 회귀가 수정 전 main landmark 2개를 관측했다.
- 구현은 `MainLayout`의 내부 `<main>`만 제거하고 기존 `min-h-screen` 래퍼를 유지한다.
- 결정 변경과 `DECISIONS.md` 신규 항목은 없다.

## Review focus

1. 회귀가 직계 자식만 보는 기존 검사의 공백을 실제로 막는가.
2. 루트 레이아웃이 `MainLayout` 사용 여부와 관계없이 유일한 `<main>`을 제공하는가.
3. `MainLayout`의 래퍼 유지가 다섯 영향 페이지의 레이아웃 책임을 보존하는가.
4. 프로필 페이지 단위 검사가 실제 렌더 경계를 과장하지 않는가.

## Validation

- Focused Jest: 2 suites / 18 tests PASS.
- Full Jest: 88 suites / 834 tests PASS.
- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS.
- `npm run build`: PASS.
- Not tested: authenticated browser visual comparison and browser accessibility tree.

## Iterations

### Iteration 1 — PR creation (2026-09-03)

Hosted CI와 리뷰 대기. 아직 actionable review 판정은 없다. 모든 체크와 리뷰가 정리돼도 사용자의 명시적
병합 승인 전에는 병합하지 않는다.

### Iteration 2 — hosted CI and CodeRabbit review (2026-09-03)

Hosted checks **16/16 success**: Detect changes, Run Tests, Lint and Type Check, Lint, Unit Tests,
Security Audit, E2E Tests 두 실행, Security Scan, Build Check, PR Summary, All Checks Complete, CodeQL,
CodeRabbit, Vercel, Vercel Preview Comments. PR은 `MERGEABLE`/`CLEAN`이다.

CodeRabbit 자동 리뷰는 OSS 정책으로 처음 건너뛰어 `@coderabbitai review`를 수동 요청했다. 수동 리뷰는
변경 파일 3개를 모두 처리했고 **actionable comment 0건**, merge risk `Minimal`로 끝났다. 인라인 스레드와
GitHub review body는 0건이다.

Pre-merge warning 둘은 다음처럼 분류했다.

| # | 지적 | 판정 | 처리 |
|---|---|---|---|
| R1 | PR 본문에 템플릿의 Recovery phase, Key decisions, 전체 Validation/Baseline/Review checklist가 없다 | **ACCEPT** | PR 본문을 `.github/pull_request_template.md` 전체 구조로 갱신 |
| R2 | 변경된 함수 2개의 docstring coverage가 0%라 일반 기준 80% 미달 | **REJECT** | 대상은 테스트 안의 국소 재귀 helper와 기존 단순 React 컴포넌트다. 공개 계약이 아니고 코드 정확성 지적도 없으며, 설명용 docstring 추가는 이 작은 수정의 신호 대비 잡음만 늘린다 |

현재 actionable review와 실패한 check는 없다. 인증된 브라우저에서 다섯 페이지를 비교한 시각 근거는 여전히
없다. **병합은 사용자의 PR #116 명시적 승인 대기 중이다.**
