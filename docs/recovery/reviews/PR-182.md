# PR182 — 화면 일관성과 내 악보 영구 삭제 확인

Date: 2026-09-22 KST
PR: https://github.com/landfill/ClairKeys/pull/182
Branch/head at creation: `codex/ui-site-consistency-delete` / `032037428edc4604efddc6bd1aa4e6ca44a5c03f`
Latest head: `d27a4c6` (successful-delete focus fix). State: `OPEN`, review-ready (non-draft), **not merged**.

## Scope and evidence

- Existing DS-1 tokens and shared buttons govern library tabs, cards, edit/delete dialogs.
- Deletion shows title plus composer/category/upload time, requires acknowledgement, stays open
  during the request and on failure, and no longer promises unconditional storage removal.
- DELETE route and data contract are unchanged. No production sheet was deleted.
- Local validation and inspected screenshots: [2026-09-22 record](../validation/2026-09-22-ui-site-consistency-delete.md).

## CI and review

| Finding/check | State | Resolution |
|---|---|---|
| Initial PR CI | SUPERSEDED | `0320374`의 WebKit E2E 실패를 `7f27beb`에서 재현·해결했고, 최종 head `d27a4c6` 전체 CI가 성공했다. |
| Codex inline P2 4069413627: pending DELETE loses dialog focus | FIXED | Reproduced by Chromium E2E: focus moved outside dialog after the confirm button became disabled. Focus dialog root while busy and trap Tab with no active controls; `d9fdf71`. 6/6 desktop/mobile delete E2E passed, focused Jest 21/21, lint/type pass. |
| CodeRabbit inline 4069430324: same pending focus issue | FIXED | Same root cause and `d9fdf71` correction. Both threads resolved on GitHub after verifying 6/6 browser tests. |
| Previous head full E2E WebKit fixture/focus failure | FIXED | WebKit locally reproduced clicked-trigger focus loss; hosted `0320374` E2E failed when an unmocked DELETE bypassed `page.route` (service worker). `7f27beb` gives the dialog the actual trigger ref and blocks workers in the test fixture. 5 browser projects, 15/15 local deletion E2E pass; final head hosted E2E 2/2 pass. |
| Codex inline P2 4069555026: successful deletion removes trigger and loses focus | FIXED | New success-response E2E failed before fix: library `<h1>` inactive after last card removal. `d27a4c6` focuses the stable page heading when the trigger is detached. Five-browser deletion suite 20/20 passed; lint/type passed. Thread resolved. |
| Current head CI | FIXED | `d27a4c6`의 PR Checks/Test 워크플로 전체 성공: E2E 2개, Unit Tests, Run Tests, Lint/Type, Build, Security Scan/Audit, CodeQL. Vercel preview 성공. `All Checks Complete` 성공. |
| Current head review | FIXED | Codex `d27a4c6` 재리뷰 완료, 신규 인라인 지적 0. CodeRabbit의 유일한 지적은 기존 포커스 문제와 같고 수정됨. GraphQL reviewThreads 미해결 0. PR은 non-draft OPEN, mergeable `MERGEABLE`; 병합은 사용자 승인 대기. |

Merge requires the user's explicit approval for PR182 and a fresh CI/review/mergeability check.
