# PR Review Log — PR #19

PR URL: https://github.com/landfill/ClairKeys/pull/19
Branch: `codex/p0-c-audio-lookahead-scheduler`
Base: `main`
State: `OPEN`
Last checked: 2026-07-21 KST

## Scope

- Fixes issue [#18](https://github.com/landfill/ClairKeys/issues/18): long scores fell silent after ~10s while notes kept falling.
- Root cause: `src/hooks/useFallingNotesAudio.ts` scheduled audio once at play time and hard-capped it to notes starting within 10s (`relativeStart > 10`), never re-filling while playing.
- Replaces the one-shot scheduler with a rolling look-ahead scheduler:
  - New pure module `src/utils/audioScheduler.ts` (`selectNotesInWindow`, `nextScheduleWindow`, `SCHEDULE_AHEAD_SEC`, `TICK_MS`, `VOICE_LIMIT`) isolates the window-selection decision from AudioContext side effects.
  - `useFallingNotesAudio` now advances a song-time cursor on a 100ms timer, keeps `SCHEDULE_AHEAD_SEC` of audio queued, maps song time to AudioContext time through a single shared anchor, and carries voice/polyphony bookkeeping across ticks.
  - seek/tempo/mute changes keep funnelling through `startAudio`, which tears down the timer and re-anchors so future nodes cannot double-fire. Public hook API unchanged; only caller `useFallingNotesPlayer` needs no edits.
- Corresponds to P0-C Work stages 1–3. Work stages 4 (visual/key-activation share the same clock) and 5 (long-run drift measurement) are out of scope and remain `NOT_STARTED`.
- Dependency-order deviation (P0-C started before P0-A/P0-B) recorded as D-007 in `docs/recovery/DECISIONS.md`, committed on this PR's branch with the fix per `AGENTS.md`.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Lint (required) | PASS | run 29791740906, 49s |
| Security Audit (required) | PASS | run 29791740906, 39s |
| Run Tests (required) | PASS | run 29791740906, 1m14s |
| E2E Tests (required) | PENDING | runs 29791740899 / 29791740906 in progress |
| Unit Tests | PASS | run 29791740899, 1m7s |
| Build Check | PASS | run 29791740899, 56s |
| Lint and Type Check | PASS | run 29791740899, 39s |
| Accessibility Check | PENDING | run 29791740899 |
| Security Scan | PENDING | run 29791740899 |
| Vercel | PASS | deployment completed |
| CodeRabbit | PENDING | review in progress |

## Review items

_None yet — awaiting CodeRabbit and remaining checks._

## Iteration log

### Iteration 1

- Wrote the regression test first (`src/utils/__tests__/audioScheduler.test.ts`, 11 tests) per `AGENTS.md` "동작 변경 전 회귀 fixture 먼저": proves a note starting at 25s is selected, window bounds are lower-inclusive/upper-exclusive, `includeSounding` re-articulates a held note at a seek point, and a 0–30s roll schedules every note exactly once (no gap, no duplicate).
- Implemented the rolling scheduler and confirmed the public API is unchanged.
- Local verification before push: `npx jest audioScheduler.test.ts visualUtils.test.ts` (24 pass), `npx tsc --noEmit` (exit 0), `npx eslint` on changed files (exit 0).
- Committed as `7d0774a` (Lore format), pushed, opened PR #19 review-ready.
- Remaining action: let CI finish, process any CodeRabbit feedback, then wait for the user's explicit merge approval.
