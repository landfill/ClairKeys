# PR #68 — OMR 완료 저장을 브라우저 수명주기에서 분리한다

- URL: https://github.com/landfill/ClairKeys/pull/68
- Branch: `codex/p1b-issue-55-server-finalization` → `main`
- Created: 2026-08-28
- Last checked: 2026-08-28 (리뷰어 독립 검토)
- Scope: 이슈 [#55](https://github.com/landfill/ClairKeys/issues/55)
- Records: **D-018**

## 커밋

| SHA | 내용 |
|---|---|
| `7c0242f` | 회귀 테스트 단독 — 구현 전 Jest 4 assertions, Python contract 2 assertions 실패 |
| `00b6b41` | producer callback, authenticated Next.js finalizer, shared idempotent storage path, D-018 |
| `6f624f7` | CodeQL SSRF 대응 — UUID allowlist + DB에서 다시 읽은 job id만 fetch target에 사용 |
| `caab7f7` | 리뷰 지적 R3·R4·R5·R7의 회귀 근거 단독 커밋 — stub 기준 Python 8건, Jest 1건 실패 |
| `638e478` | R3·R4·R5·R7 수정 — delivery를 `else`로, `omr/delivery.py` 신설, timeout 70s, 영구 실패 즉시 중단, `/status` 인코딩 |

## CI status — 생성 직후

| Check | Status | Last evidence |
|---|---|---|
| Detect changes | PASS | GitHub Actions run `33161926148` |
| Vercel Preview Comments | PASS | Vercel bot comment |
| Run Tests / Lint / Security Audit | IN_PROGRESS | GitHub Actions run `33161926121` |
| Build / type / unit / E2E / accessibility / security | IN_PROGRESS | GitHub Actions run `33161926148` |
| CodeRabbit | MANUAL_TRIGGER_REQUIRED | OSS 저장소 자동 리뷰 skip 코멘트 |

PR은 `OPEN`, draft=false, `MERGEABLE`; 체크 진행 중이라 `mergeStateStatus=BLOCKED`다.

## CI status — head `6f624f7`

**17/17 success, pending/failing 0.** `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.

- Tests: Run Tests, Lint, Security Audit, E2E 모두 pass
- PR Checks: build, type/lint, unit, E2E, accessibility, security scan, CodeQL, summary 모두 pass
- Vercel preview 배포 pass
- 최초 CodeQL alert 1은 `most_recent_instance.state=fixed`; review thread resolved=true,
  outdated=true 확인
- CodeRabbit은 수동 요청 2회 모두 `Review rate limited`로 종료. **실제 코드 리뷰 없음.**

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | CodeQL | `src/lib/omr/finalizeJob.ts:35` | callback request의 job id가 server-side fetch URL에 도달 | ACCEPT | FIXED | `6f624f7`; UUID 검증 + DB canonical id |
| R2 | 리뷰어 | 폐기된 `omr-service/fly.toml` + `app.py` | Fly auto-stop이 callback 재시도를 종료시킨다는 지적. 저장소의 과거 설정을 현재 배포 설정으로 오인했다 | REJECT | REJECTED | 실제 배포는 NAVER Cloud VM의 podman/systemd이며 Fly.io는 배포된 적 없음. 일반적인 프로세스 재시작 내구성만 D-018의 기존 제약으로 유지 |
| R3 | 리뷰어 | `omr-service/app.py` `process_pdf_background` | `await notify_completion(...)`이 `try` 안에 있어, 이미 COMPLETED로 확정된 job이 전달 단계 예외로 FAILED로 뒤집힐 수 있다 | ACCEPT | **FIXED** | `638e478`; delivery를 `try`의 `else`로 이동하고 자체 `except`로 로깅. AST 회귀 3건이 배치를 고정 |
| R4 | 리뷰어 | `app.py` httpx / `finalize/route.ts` | httpx `timeout=30.0` < finalize `maxDuration=60`이라 finalize가 중복 실행된다 | ACCEPT | **FIXED** | `638e478`; `CALLBACK_TIMEOUT_SECONDS=70.0`. 회귀가 finalize 라우트의 `maxDuration`을 직접 읽어 비교하므로 한쪽만 바뀌면 실패한다 |
| R5 | 리뷰어 | `app.py` `notify_completion` | 400/401 같은 영구 실패도 12회 재시도한다 | ACCEPT | **FIXED** | `638e478`; 재시도 정책을 stdlib-only `omr/delivery.py`로 분리해 실제 단위 테스트. 404는 upload 경합 때문에 의도적으로 retryable 유지 |
| R6 | 리뷰어 | `prisma/schema.prisma` | `omrJobId`에 `@unique`도 `@@index`도 없다. 이제 모든 callback·재시도가 타는 경로인데 시퀀셜 스캔 | OPEN | 후속 마이그레이션 | 리뷰 코멘트 M4 |
| R7 | 리뷰어 | `status/[jobId]/route.ts` | `/status/${jobId}`가 라우트 파라미터 원문 보간 — D-018 directive를 같은 파일의 절반만 지킨다. 현재 악용 불가 | ACCEPT | **FIXED** | `638e478`; `encodeURIComponent(storedJobId)`. Jest 회귀가 두 URL 모두 세그먼트 수·쿼리 부재를 확인 |
| R8 | 리뷰어 | `finalize/route.ts` | `storedJobId`는 정의상 항상 `jobId`와 같아 3번째 커밋의 DB 왕복은 런타임 no-op이고 409 분기는 도달 불가. CodeQL alert를 실제로 닫은 것은 `UUID_PATTERN` + `encodeURIComponent`인데 주석은 왕복을 방어 근거로 설명한다 | OPEN | 주석 수정 권고 | 리뷰 코멘트 L1 |
| R9 | 리뷰어 | `omr-service/tests/test_service_contract.py` | 새 테스트가 `APP_SOURCE` 문자열 검사라 포매팅에 깨지고 `notify_completion`이 아무것도 보내지 않아도 통과한다 | ACCEPT | **PARTIAL** | `caab7f7`·`638e478`; 문자열 매칭을 AST로 교체하고 재시도 정책은 실제 실행 테스트로 전환. `notify_completion`의 HTTP 경로 자체는 여전히 미실행 (app.py import 불가) |
| R10 | 리뷰어 | `upload/route.ts` | `NEXTAUTH_URL` 미설정 시 `request.nextUrl.origin`(Host 헤더 유래)으로 fallback하고 그 주소로 공유 비밀이 나간다. `/process`가 인증돼 있어 현재 열린 구멍은 아님 | OPEN | fail closed 권고 | 리뷰 코멘트 L3 |
| R11 | 리뷰어 | `finalize/route.ts` | `alreadyStored` 단축 경로가 응답에는 `completed`를 담지만 DB `processingStatus`는 고치지 않는다. status 라우트의 대응 분기와 불일치 | OPEN | 한 줄 `update` 권고 | 리뷰 코멘트 L4 |

## Iteration log

### Iteration 1 — initial

- Feedback fetched: inline 0건; CodeRabbit 자동 리뷰 skip
- Changes made: 없음
- Validation: Jest 55/522, Python 34, tsc, lint, build 통과
- Commit: `00b6b41`
- Remaining actionable items: hosted CI와 수동 CodeRabbit review 대기

### Iteration 2 — CodeQL critical SSRF

- Feedback fetched: CodeQL `js/request-forgery`, critical, alert 1. callback JSON의 `job_id`가
  `fetch(${OMR_SERVICE_URL}/result/${jobId})`까지 흐른다고 판정.
- Decision: **ACCEPT.** 공유 비밀 인증이 있어도 request 값이 server-side target 구성에 직접
  참여할 이유가 없다. 서비스 job id 계약은 UUID4다.
- Changes made: callback input UUID allowlist; request 값은 Prisma lookup에만 사용; 실제 fetch는
  row에서 다시 읽은 `omrJobId`; path component도 `encodeURIComponent` 적용. D-018 directive 갱신.
- Validation: focused Jest 3 suites / 19 tests, `npx tsc --noEmit`, lint, diff check 통과.
- Commit: `6f624f7`
- Remaining actionable items: hosted CodeQL 재실행, E2E, CodeRabbit review 대기.

### Iteration 3 — final hosted state

- Hosted checks: **17/17 pass**, pending/failing 0.
- CodeQL alert 1: fixed; thread resolved/outdated.
- CodeRabbit: manual request 2회 모두 rate limited; actionable을 0건 찾았다는 뜻이 아니라
  **리뷰 자체가 없었다.**
- Remaining actionable items: 확인된 항목 없음. 사용자의 명시적 병합 승인 대기.

### Iteration 4 — 리뷰어 독립 검토 (2026-08-28)

CodeRabbit이 두 번 모두 `Review rate limited`로 끝나 **서드파티 코드 리뷰가 실제로 없었으므로**,
전체 diff를 직접 읽고 검증을 재실행했다.

재실행 결과 (worktree `6f624f7`):

| 명령 | 결과 |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `npx jest src/app/api/omr --runInBand` | 4 suites / 29 tests passed |
| `python3 -m unittest discover -s tests` (omr-service) | 34 passed |
| `gh pr checks 68` | 17/17 pass |
| CodeQL review thread | `resolved=true, outdated=true` — Iteration 3의 주장 확인됨 |

- Feedback fetched: 리뷰어 자체 검토 10건 (R2~R11). 외부 소스 신규 0건.
- 병합을 막는 정확성 결함 없음. 설계 방향(생산자 소유 delivery + 폴링 fallback + D-011 boundary 보존)은 타당.
- R2는 후속 확인에서 **잘못된 전제**로 판명됐다. `fly.toml`은 작성만 되고 배포된 적 없는
  과거 파일이며, 현재 OMR 서비스는 NAVER Cloud VM의 podman/systemd에서 실행된다. Fly idle
  auto-stop은 운영 위험이 아니다. 프로세스·컨테이너·호스트 재시작 시 in-memory delivery가
  사라지는 일반 제약은 D-018에 이미 기록되어 있다.
- R3·R4·R5·R7은 모두 몇 줄짜리이고 이 PR 범위 안이다.
- 리뷰 코멘트: https://github.com/landfill/ClairKeys/pull/68#issuecomment-5451368487
- Remaining actionable items: R3·R4·R5·R7은 Iteration 6에서 수정됨. R6·R8·R10·R11은 후속.

### Iteration 5 — Fly.io 전제 오류 정정 (2026-08-28)

- R2를 `REJECTED`로 변경했다. 리뷰어가 저장소에 남아 있던 과거 `fly.toml`을 라이브 배포
  설정으로 오인했다.
- 실제 운영 계약은 `omr-service/deploy/clairkeys-omr.service`와 D-012의 NAVER Cloud VM
  podman/systemd 배포다. 이 unit에는 idle auto-stop이 없다.
- PR #68의 실제 배포 검증 공백은 그대로다: 새 callback 코드를 VM에 배포하고 화면 이탈
  종단 흐름을 확인하지 않았다. 다만 그 공백을 Fly auto-stop 위험으로 설명하지 않는다.
- 혼동을 만든 현재형 Fly 설정·문서·테스트는 별도 목적의 정리 PR로 제거한다. 과거 시점의
  리뷰·검증 기록은 사실 보존을 위해 삭제하지 않고 역사 자료로 남긴다.

### Iteration 6 — R3·R4·R5·R7 수정 (2026-08-28)

사용자가 R3·R4·R5·R7 수정 후 병합을 승인했다. 규약대로 회귀를 먼저 단독 커밋(`caab7f7`)했다.

**수정 전 회귀 실패 근거**

| 대상 | 실패 |
|---|---|
| R3·R4·R5 (`omr.delivery`를 현행 동작으로 stub) | Python **8 assertions 실패** |
| R7 | `npx jest src/app/api/omr/status` **1 failed / 13 passed** |

**수정 후 전체 검증 (로컬 재실행)**

| 명령 | 결과 |
|---|---|
| `python3 -m unittest discover -s tests` | 34 → **43 passed** |
| `npm test -- --runInBand` | 55 suites / **524 tests passed** (522 → 524) |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | No ESLint warnings or errors |
| `npm run build` | 성공 |
| `gh pr checks 68` (head `638e478`) | **17/17 SUCCESS** |

**설계 메모** — 재시도 정책을 `omr/delivery.py`로 분리한 것은 새 패턴이 아니라 `omr/auth.py`의
선례를 따른 것이다. `app.py`는 fastapi·aiofiles 때문에 이 환경에서 import되지 않으므로, 그 안에만
있는 로직은 읽을 수는 있어도 실행할 수는 없다. R5의 판단 규칙만 stdlib-only 모듈로 내리면 실제로
돌려볼 수 있고, R9가 지적한 "읽기만 하는 테스트" 문제도 그만큼 줄어든다.

**R3의 배치가 핵심** — `try`의 `else`는 두 조건을 동시에 만족한다. 변환이 예외를 내지 않았을
때만 실행되고(실패한 job은 아무것도 알리지 않는다), 그 안에서 난 예외는 위의 handler가 잡지
않는다(전달 결함이 완료를 실패로 되돌리지 못한다).

- Remaining actionable items: R6·R8·R10·R11은 후속. 병합 승인 받음.

## 검증하지 못한 것

- VM에 이 branch의 `omr-service/app.py`를 배포하지 않았으므로 실제 callback outbound 요청 없음.
- Vercel preview와 VM을 연결한 실제 PDF 업로드·화면 이탈 시나리오 없음.
- OMR 프로세스 재시작을 견디는 영속 전달 없음 — D-018이 P1-B queue 범위로 명시한다.
