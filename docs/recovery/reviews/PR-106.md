# PR #106 review log

Date: 2026-09-01 KST
PR: [Reduce library and public search wait time](https://github.com/landfill/ClairKeys/pull/106)
Branch: `codex/issue-105-performance`
Head: `356891f0d7640ee3762a837141f29048307c2a3c`

## Current state

- Review-ready PR created on 2026-09-01 for issue #105.
- Four Lore commits separate regression evidence (`453224e`), implementation (`3aeee65`), review fixes (`b8de029`),
  and the hosted matcher correction (`356891f`).
- Production before-values, root-cause classification, and local verification are recorded in
  `docs/recovery/validation/2026-09-01-issue-105-performance.md`.
- Local verification passed: 85 Jest suites / 777 tests, typecheck, lint, production build, Prisma validation,
  diff check, and 5 Playwright projects / 30 tests.
- All CI and change-aware checks passed at latest head `356891f`: Lint, Lint and Type Check, Run Tests, Unit Tests,
  Security Audit, Security Scan, Build Check, two E2E Tests jobs, CodeQL, Vercel, and aggregate checks.
- Vercel preview deployed successfully. First/cold public-search result wait was 2.298 seconds versus production
  before 3.068~3.270 seconds; three warm-cache waits were 17 ms, 16 ms, and 219 ms.
- A manual CodeRabbit review was requested because automatic review was skipped by the fewer-than-10-stars policy.
  It selected 17 changed files, emitted four inline actionable comments and one outside-diff props-export comment,
  and confirmed all accepted fixes. It also withdrew the user-index portion after verifying the active route.
- All four inline threads are resolved. Review bodies and general comments have no remaining actionable item.
- Hosted Run Tests and Unit Tests failed once on `b8de029` because the migration test counted an explanatory comment
  as a fourth SQL statement. `356891f` anchored the matcher to executable line starts; all latest-head checks passed.
- The PR is `MERGEABLE` and `CLEAN`. It awaits explicit user merge approval.
- No production code, database index, or Vercel deployment has changed yet. Vercel preview/production measurement
  and production migration application remain explicit completion gates.
- Merge requires the user's explicit approval after CI and actionable review are clean.

## Review items

| ID | Severity | Finding | Decision | Status | Evidence |
|---|---|---|---|---|---|
| R1 | Major | Regular index creation can block production writes | ACCEPTED | FIXED | `b8de029`: three `CREATE INDEX CONCURRENTLY` statements; no transaction wrapper; reviewer confirmed/resolved |
| R2 | Minor | Index order does not match active lookups | PARTIAL | FIXED / REJECTED IN PART | Public index → `(isPublic, createdAt)`; kept `(userId, updatedAt)` for active `/api/sheet`; reviewer withdrew rejected portion |
| R3 | Minor | `X-Database-Queries` can undercount auth callback queries | ACCEPTED | FIXED | Route-scoped `X-Category-Queries`, `X-Sheet-Queries`, `X-Search-Queries` plus absence regressions |
| R4 | Minor | Strict Mode can queue an identical second search | ACCEPTED | FIXED | searched/queued parameter keys in `b8de029`; reviewer confirmed/resolved |
| R5 | Review body | Props interface is not exported | ACCEPTED | FIXED | `export interface LibrarySheetMusicListProps` in `b8de029`; answered in general comment |
| R6 | Hosted CI | Migration comment was counted as a fourth concurrent index statement | ACCEPTED | FIXED | `356891f` anchors regex at executable line starts; latest hosted tests green |
