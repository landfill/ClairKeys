# PR157 — upload form grouped into three labelled sections

Date: 2026-09-12
PR: https://github.com/landfill/ClairKeys/pull/157
Head: e3d5d325cb37821f112ef6ca660f9e9e08c74800
Branch: `codex/issue-146-upload-form-grouping`
Issue: #146 stage 3, upload form only

- Non-draft and MERGEABLE at this entry. Hosted checks were still running when this was written;
  consult the PR for their outcome before any merge decision. NO MERGE APPROVAL EXISTS.
- One commit, regression-first: the tests and the implementation are in `e3d5d32`, and the
  before-state was run and recorded before the component was touched — jest 3 failed / 24 passed,
  Playwright 7 of 9 failed. Evidence: [upload form validation](../validation/2026-09-12-upload-form-grouping.md).
- Changed files: `src/components/upload/OMRUploadForm.tsx`,
  `src/components/upload/__tests__/OMRUploadForm.test.tsx`, `e2e/upload-form-grouping.spec.ts`,
  `docs/recovery/DECISIONS.md` (D-058). No API, database, animation-contract, playback or audio
  file is touched, and the OMR service is not touched.
- Behaviour deliberately unchanged: fields, validation, defaults and the submit payload are the
  same. The component's logic region (lines 1–324) has no diff; every change is JSX placement and
  className. The `덩어리를 나눠도 컨트롤 구성과 순서는 그대로다` test pins the eight controls and
  their DOM order and passed before the change as well as after.
- Two things the measurement changed about the work itself, both recorded rather than smoothed
  over. (1) Group description paragraphs added in the first attempt pushed the 곡명 input from
  535px down to 579px, spending the entire drop-zone saving; they were copy no phase document
  asked for and were removed. (2) `<fieldset>` carries the UA stylesheet's
  `min-inline-size: min-content`, which Tailwind preflight does not reset, and it overflowed the
  document to 386px at a 320px viewport until `min-w-0` was added.
- The form is NOT uniformly shorter. It lost 30px on wide screens and gained 36–40px on narrow
  ones, because two extra group headings cost more than the drop-zone compression saves where
  there is no column to collapse. Only the drop area was compressed; the PR body says so.
- A WebKit CSS-zoom measurement discrepancy was confirmed against a clean `main` worktree
  (`64989c7`, built and served separately) before the assertion was scoped, exactly as PR155 did.
  Both builds measured `documentElement.scrollWidth` 1982 and `body.scrollWidth` 991 at a 1440px
  viewport, so it is pre-existing and not introduced here.
- New precedent recorded as D-058: this is the first spec in the suite to open a protected route,
  and it mints a next-auth session cookie rather than reviving a sign-in flow. It is a rendering
  fixture and asserts nothing about authentication. A reviewer who disagrees with that precedent
  should say so — it is the one decision in this PR that outlives the slice.
- Local verification: jest upload 27/27; full jest 1010 passed / 1 failed; full Playwright 155/155
  across five browser projects; lint 0/0; `tsc --noEmit` 0 errors; `npm run build` success. The
  single unit failure is `omrCallbackDelivery`, which needs `fastapi` and is absent on this
  machine; CI is the authority for it.
- Issue #146 is NOT closed by this PR. Stage 4 (existing states and responsive behaviour, including
  keyboard focus and zoom) remains, so the phase stays `IN_PROGRESS`.

## Review round handled; head is now 46c0d4c (2026-09-12)

- Head advanced `e3d5d32` → `ecff241` → `8d8c1c4` (message rewrite, trees identical) → `46c0d4c`.
  All 16 hosted checks pass on `46c0d4c`; non-draft, MERGEABLE. NO MERGE APPROVAL EXISTS.
- Codex raised two P1 findings and both were answered rather than obeyed or dismissed.
- FINDING 1, breakpoint: the reported conclusion was right and its stated cause was wrong. The
  citation (D-058 at `DECISIONS.md:2229`) does not contain a column criterion — D-058 governs the
  E2E fixture and the criterion lived in `HANDOFF.md:100`. Measuring anyway exposed a real defect:
  at exactly 640px the `sm` breakpoint split each field to 263px, narrower than the 308px a 390px
  phone gets in one column. Widening the screen shrank the input. The boundary moved to `md`.
- The prescription to stack 844×390 was declined with measurement (365px per field, wider than the
  phone baseline, on the viewport with the least vertical room), and the decision was then written
  down rather than left implicit — see D-059.
- FINDING 2, Lore trailers: correct on first appearance and fixed. `e3d5d32` and `ecff241` parsed
  zero trailers because wrapped `Rejected:`/`Tested:` values broke git's parser; the three commits
  were rewritten with single-line trailers and now parse 11/10/10. The repeat against `8d8c1c4` was
  not acted on — it cited a SHA absent from this repository and named a commit that parses 10.
- Consequence worth carrying: `git log -1 --format=full`, which the Lore protocol prescribes, cannot
  detect this class of error. It prints the message, not the parse. Use
  `git show -s --format='%(trailers:only=true)'` as well.
- D-059 was added in `46c0d4c` because Codex correctly noticed that D-058's own Context restated the
  criterion in device terms while the code had moved to a width rule — an inconsistency this branch
  introduced. AGENTS requires the decision to change before the implementation deviates, so the
  decision, the component comment and the spec comment were all updated in one commit with no
  behaviour change.
- Still unfixed and recorded: `192c62c` on `main` has the same trailer defect. `main` history is not
  rewritten without explicit approval.
- Local verification at `46c0d4c`: Playwright 160/160 across five browser projects, jest upload
  59/59, lint 0/0, `tsc --noEmit` 0 errors, `npm run build` success.

## MERGED (2026-09-12)

- User explicitly instructed the merge by PR number. Exact head `46c0d4c`, all 16 checks, non-draft
  and MERGEABLE/CLEAN were rechecked immediately before merging, and all five review threads were
  resolved after being answered.
- Merged as `925e75832787c861308fc381b82d753ccb8b4bc1`. All six check-runs on that exact merge commit
  succeeded: E2E Tests, Post-merge build, Post-merge tests, Run Tests, Lint, Security Audit. The
  merge commit's own Lore trailers parse (8).
- Local `main` fast-forwarded to `925e758`. The incoming commits touch only `DECISIONS.md`,
  `OMRUploadForm.tsx`, its test and the new E2E spec — no HANDOFF.md — so the user's uncommitted
  HANDOFF edit was verified byte-identical after the pull.
- Both branch tips had zero commits outside `main` before deletion; the branch was then deleted on
  origin and locally.
- Verified on merged `main`: `md:grid-cols-2` at line 420, `min-w-0` on all three fieldsets, D-059
  present, jest upload 59/59.
- The final repeat of the trailer finding was answered rather than acted on and is worth recording
  as a pattern: it cited a third non-existent object (`0ac34911`, after `2ddc085e` and `14c73f51`),
  while every commit on the branch parsed 10–11 trailers with `Confidence`, `Scope-risk` and
  `Tested` all present. The first instance of that same finding was correct and was fixed.
- ISSUE #146 IS STILL OPEN. Stage 4 remains.
