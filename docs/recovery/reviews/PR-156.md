# PR156 — untrack the files that can never be committed

Date: 2026-09-12
PR: https://github.com/landfill/ClairKeys/pull/156
Head: c8ae0cb
Branch: codex/issue-146-untrack-private-audit-evidence

- Purpose: make AGENTS.md lifecycle rule 7 able to clear. The rule refuses to delete any branch
  while user-owned uncommitted changes exist, and two paths kept that condition permanently true
  because neither can be resolved by committing it.
- `docs/recovery/validation/2026-09-07-ui-audit/*.png` (8 frames) contain the signed-in account's
  name and library contents. This repository is public, and issue 146's own evidence policy is why
  they were never attached to the issue. They are now ignored.
- `.claude/settings.local.json` was ALREADY listed in `.gitignore`, but it had been committed
  before that rule existed and `.gitignore` does not apply to a tracked file, so the rule sat
  there satisfied-looking and inert. `git rm --cached` makes the declared intent effective.
- Deliberately narrow: the ignore names this one audit directory, not validation screenshots in
  general. `2026-09-07-finger-badge/` and `2026-09-07-ui-renewal/` hold 16 intentionally tracked
  public screenshots, and a blanket pattern would silently start dropping this program's evidence.
- Side effect that needs the user's decision: anyone pulling this loses their own working copy of
  `.claude/settings.local.json`, because it stops being tracked. The PR body states this and offers
  to reduce the change to the PNGs alone — at the cost of the blocker not clearing.
- Verified: `git status` shows neither path after the change, leaving only the user's normally
  committable HANDOFF edit; the working copy of `.claude/settings.local.json` still exists on disk
  after `git rm --cached`; `git ls-files` confirms the 16 other screenshots remain tracked.
- No application code, test suite or build is affected, and none was run for this change.
- Automated review raised one P1 claiming the commit omits the mandatory Lore trailers. That is
  incorrect: `git log -1 --format=%B c8ae0cb` carries `Constraint`, three `Rejected` entries,
  `Confidence`, `Scope-risk`, `Reversibility`, `Directive`, `Tested`, `Not-tested` and `Related`,
  with no key outside the protocol. The review appears to have read the PR description, which is
  Markdown with headings. Answered on the thread with the reproduction command; no rewrite made.
- Explicit user approval is required before main merge.
