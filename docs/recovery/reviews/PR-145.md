# PR145 — Source-backed wedge recovery

Date: 2026-09-06 KST
PR: https://github.com/landfill/ClairKeys/pull/145
Implementation: d35b14d; decision-link follow-up:44dc0f7

## Scope and local review

Codex Sol authored the implementation; root independently reviewed and executed verification.
The retry is triggered only by a missing exported measure plus null-time wedge endpoints, retains
source-backed native symbols in an isolated candidate, and preserves the selected result on failure.
Root review corrections include native graph serialization, exact rest/ledger ink proof, scoped
head bijection, full note metadata/order and tie/slur preservation, division normalization,
source-chord-bound direction cursors, namespaced free glyphs, assigned-only cache-label handling,
deadline/cancellation coverage, constructor compatibility, CI test inventory and real GNU patch
CRLF compatibility. All were implemented and independently checked.

## Validation

See [implementation evidence](../validation/2026-09-06-wedge-implementation.md).
Actual processor selection passes21 13/13,20 15/15 plus1 rest,22 10/10. Four native no-region
controls pass;145 Python/977 Jest tests and separate type/lint/build pass. Exact Dockerfile patch
and compiler output match the native tested engine classes. All temporary private VM staging is
collected/hash-verified/removed; production unchanged.

## GitHub lifecycle

- Created review-ready (not Draft) after explicit user authorization for the17-file commit/PR scope.
- CI and automated review are running; final checks and actionable comments remain to be inspected.
- No merge/rollout approval for this PR. No merge or deployment performed.

The wedge phase document is first introduced by PR145 and does not yet exist on main. Its latest progress is recorded here and in HANDOFF; the local Status/Progress update is retained for direct-main application after that plan file is merged. Do not create the unapproved plan on main solely to record its status.


## Hosted review of44dc0f7 (2026-09-06)

All GitHub CI, both E2E jobs, CodeQL and Vercel checks passed. CodeRabbit review5125082149
completed with four actionable findings, independently triaged by root:

| Comment | Status | Finding / evidence |
| --- | --- | --- |
|3943698623|OPEN|Mark the initial pre-native phase checkpoint explicitly Historical; current main HANDOFF already records actual success.|
|3943698628|OPEN|Require a native ledger glyph before retaining at the post-analysis boundary; LedgerInter permits source-less construction, though the fresh PDF builder normally provides one.|
|3943698636|OPEN|Root reproduced TypeError for missing head grade and pitch through recovery_region_constant; malformed numeric attributes should abstain.|
|3943698638|OPEN|Exercise matched/missing/unexpected restEvents in actual evaluator tests, not only fixture metadata assertions.|

Codex Sol task `task_19e5e971fe76`, dispatch `ctx_2dd28424e19e`, implements these bounded fixes
in the same checkout. Root independently reviews/tests and owns Git/VM execution. Previous worker
`task_f956cd1a5218` completed; release returned `retained/user_takeover`, so no terminal process was
closed. Its result is recovered in repository validation records; no active task depends on its
terminal. The reviewed branch still awaits fixes/verification; no merge approval is present.
