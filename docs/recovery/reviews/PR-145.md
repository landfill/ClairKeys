# PR145 — Source-backed wedge recovery

Date: 2026-09-06 KST
PR: https://github.com/landfill/ClairKeys/pull/145
Implementation: d35b14d; decision-link follow-up:44dc0f7; review fixes:f614245

## Scope and local review

Codex Sol authored the implementation; root independently reviewed and executed verification.
The retry is triggered only by a missing exported measure plus null-time wedge endpoints, retains
source-backed native symbols in an isolated candidate, and preserves the selected result on failure.
Root review corrections include native graph serialization, exact rest/ledger ink proof, scoped
head bijection, full note metadata/order and tie/slur preservation, division normalization,
source-chord-bound direction cursors, namespaced free glyphs, assigned-only cache-label handling,
deadline/cancellation coverage, constructor compatibility, CI test inventory and real GNU patch
CRLF compatibility. All were implemented and independently checked.

## Validation

See [implementation evidence](../validation/2026-09-06-wedge-implementation.md).
Actual processor selection passes21 13/13,20 15/15 plus1 rest,22 10/10. Four native no-region
controls pass;145 Python/977 Jest tests and separate type/lint/build pass. Exact Dockerfile patch
and compiler output match the native tested engine classes. All temporary private VM staging is
collected/hash-verified/removed; production unchanged.

## GitHub lifecycle

- Created review-ready (not Draft) after explicit user authorization for the17-file commit/PR scope.
- Latest hosted CI passes; the four original review findings are fixed and resolved. See final verification below.
- No merge/rollout approval for this PR. No merge or deployment performed.

The wedge phase document is first introduced by PR145 and does not yet exist on main. Its latest progress is recorded here and in HANDOFF; the local Status/Progress update is retained for direct-main application after that plan file is merged. Do not create the unapproved plan on main solely to record its status.


## Hosted review of44dc0f7 (2026-09-06)

All GitHub CI, both E2E jobs, CodeQL and Vercel checks passed. CodeRabbit review5125082149
completed with four actionable findings, independently triaged by root:

| Comment | Status | Finding / evidence |
| --- | --- | --- |
|3943698623|FIXED|Mark the initial pre-native phase checkpoint explicitly Historical; current main HANDOFF already records actual success.|
|3943698628|FIXED|Require a native ledger glyph before retaining at the post-analysis boundary; LedgerInter permits source-less construction, though the fresh PDF builder normally provides one.|
|3943698636|FIXED|Root reproduced TypeError for missing head grade and pitch through recovery_region_constant; malformed numeric attributes should abstain.|
|3943698638|FIXED|Exercise matched/missing/unexpected restEvents in actual evaluator tests, not only fixture metadata assertions.|

Codex Sol task `task_19e5e971fe76`, dispatch `ctx_2dd28424e19e`, implements these bounded fixes
in the same checkout. Root independently reviews/tests and owns Git/VM execution. Previous worker
`task_f956cd1a5218` completed; release returned `retained/user_takeover`, so no terminal process was
closed. Its result is recovered in repository validation records; no active task depends on its
terminal. That review checkpoint initially awaited fixes/verification; the final section records their resolution. No merge approval is present.


## Final verification of f614245 (2026-09-06)

- All four findings fixed in f614245 and independently checked by root; all four GitHub threads
  resolved after the actual corrected-image processor run passed.
- Root149 Python tests pass. Updated native image c7326c16…37c34 compiles through the exact Dockerfile
  path; bytecode confirms null glyphs cannot reach recovery. Actual processor still selects exact
 21/20/22 recovery with no trigger;53 artifact hashes match, and both review staging directories
  are removed. See the appended validation section for full hashes/commands.
- All hosted CI, both E2E jobs, build/type/lint/security/CodeQL and preview checks pass at f614245.
- CodeRabbit skipped its second automatic review and reports a successful skip status; this is
  explicitly not recorded as another automated approval. Root manually verified the complete final
  diff and all four original findings. No unresolved actionable thread remains.
- Review worker task_19e5e971fe76 completed and its owned terminal was released with transcript
  archived. No active worker remains. No PR145 merge or production rollout performed or approved.


## Explicitly approved merge (2026-09-06)

User said `머지` for PR145. Root rechecked f614245, every hosted check, CLEAN/MERGEABLE,
non-Draft status and all four resolved threads, then ran
`gh pr merge 145 --merge --match-head-commit f614245784b632b029ea7a6299555c3103da4ddc`.
GitHub reports MERGED at2026-09-06T11:16:12Z with merge commit
`1edbceacdd13ef7947d7cf0a3dcc22bcc88b1c81`. Local main was fast-forwarded without switching the
working checkout. Both local and remote feature tips are contained in main; implementation diff
against the feature tip is empty. Initial merge check-runs are in progress, with no failure observed.

Cleanup blocker: user-owned settings and pre-existing HANDOFF edits remain dirty. Under AGENTS.md
neither local nor remote work branch is deleted. No production rollout was authorized/performed.
The phase plan now exists on main, so its retained Status/Progress record can be applied directly.


## Explicitly approved OMR VM rollout completed (2026-09-06)

User instructed `vm 도 그럼 배포해`. Exact merge1edbcea was built, image-tested and deployed as
1a28f10e…24173, with b9d3ac6 retained for rollback. Service restart/image identity/health/auth checks
passed. Actual production Love conversion returns431 notes/123.0s, with every note and duration
identical to the validated candidate. Result collection/hash verification and temporary cleanup
completed. See [deployment validation](../validation/2026-09-06-wedge-deployment.md). This explicitly
fulfills the VM rollout approval; prior no-rollout statements are historical checkpoints.
