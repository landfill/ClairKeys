# PR #98 review log

Date: 2026-08-30 KST
Head: `340c595`
Merge state: `CLEAN`
Required checks: `Lint`, `Security Audit`, `Run Tests`, `E2E Tests` (both jobs) — all PASS

## Independent Codex review (gpt-5.6-terra)

The independent review identified three Major findings. Each was checked against the code and the user's disposition was applied:

| Finding | Evidence | Disposition |
|---|---|---|
| 1. `/sheet/[id]` distinguishes 403 and 404, enabling resource enumeration | `src/app/api/sheet/[id]/route.ts` uses `findUnique`; missing rows return 404 and existing rows then undergo the permission check, with optional `session?.user?.id`. Anonymous requests could distinguish this before DS-6. | Not changed. The DS-6 regression criterion that private sheets remain 403 is satisfied. Recorded as a separate security follow-up alongside DS0-1. |
| 2. Public cards do not show playback duration | `SheetMusic` has no duration column; `prisma/schema.prisma` `durationSeconds` belongs to `PracticeSession`. Duration exists only in Supabase animation JSON. | Removed from DS-6 completion criteria and recorded in D-035. Adding a normalized column or fetching every animation JSON were rejected because they require a data decision and add list/storage coupling. |
| 3. E2E fixture was self-disabling | `addInitScript` returned synthetic responses for the public list, detail, and animation paths without calling `originalFetch`, so three `page.route` fixtures were dead. The live matchers remained loose, and the test only checked `toBeVisible()` for a button without clicking it. | Fixed in `7d309a8`/`340c595`: one `page.route('**/*')` pathname dispatcher, service-worker blocking, `toBeInViewport()` for first-screen controls, and an actual play-button click while retaining `privateAnimationRequests === 0`. |

## Playback evidence boundary

`useFallingNotesPlayer.ts:59` intentionally sets `isPlaying` only when `startAudio` returns true; this is an audio-bound state contract, not a test seam to bypass. In local headless browser runs, AudioContext creation/resume did not yield a playing state across the five projects, so `340c595` removes the unprovable playing-state assertion and renames the E2E to `explore a public sheet preview`; issue #76 completion criterion 3 remains a secret-window manual playback check. CodeRabbit was not rerun for actionable findings; its result for the new commits was `Review skipped: manual review required for this OSS repository`.
