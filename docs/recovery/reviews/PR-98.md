# PR #98 리뷰 기록

Date: 2026-08-30 KST  
Status: review-ready, non-draft, open

## CI

Required `Lint`, `Security Audit`, `Run Tests`, `E2E Tests`: 모두 PASS. `E2E Tests`는 `pr-checks.yml`과 `test.yml` 양쪽에서 실행되어 두 결과 모두 PASS했다.

## CodeRabbit

CodeRabbit 리뷰가 완료됐다 (Run ID `a5a0d0a0-6dc2-455b-b47e-c815638255a1`, actionable comments 2건).

1. `src/app/api/sheet/search/route.ts`: 수용. 로그인 사용자의 기본 공개 검색 분기에도 `provenance != demo`를 적용하고, `totalPublic` 및 카테고리 통계 집계에도 같은 조건을 적용했다. 로그인 상태 기본 검색을 검증하는 회귀 테스트를 추가했다.
2. `e2e/application-smoke.spec.ts`: 수용. fixture handler를 정확한 `URL.pathname` 비교로 제한하고 `/api/files/animation` 호출 횟수를 추적해 0건을 단언한다.

## 리뷰 표면

- `pulls/98/comments`: 리뷰 스레드 없음
- `pulls/98/reviews`: 리뷰 본문 없음
- `issues/98/comments`: 초기에는 CodeRabbit OSS 수동 리뷰 필요 안내와 Vercel 알림만 조회됐으나, 이후 CodeRabbit 리뷰가 도착해 위 두 스레드에 답변했다.

초기 기록에서 조회 시점 때문에 “CodeRabbit 리뷰가 실행되지 않음”으로 잘못 적었다. CodeRabbit 체크가 `Review in progress`인 동안에는 결착을 기다린 뒤 세 리뷰 표면을 다시 조회해야 한다는 교훈을 남긴다.
