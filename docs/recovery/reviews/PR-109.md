# PR #109 review log

Date: 2026-09-02 KST
PR: [공유 비밀이 향하는 콜백 주소를 요청 Host에서 유도하지 않는다](https://github.com/landfill/ClairKeys/pull/109)
Branch: `codex/issue-71-callback-fail-closed`
Head: `0dd2e07`
Last checked: 2026-09-02 10:55 KST

## Current state

- Review-ready PR created on 2026-09-02 for issue #71 (PR #68 review item R10).
- **This PR changes a recorded decision.** D-018 Decision 1 named the request-origin fallback; the same commit
  amends D-018 in place and adds D-036 with the reasoning, per AGENTS.md (a DECISIONS entry travels with the
  code that depends on it).
- Two Lore commits: regression `dea17cc` (unset/blank NEXTAUTH_URL → observed `OMR_SERVICE_UNAVAILABLE` after
  a row was created) and fix + decision `dd6f62b`.
- Test contract change worth a reviewer's attention: all 11 existing upload cases passed without `NEXTAUTH_URL`,
  i.e. they exercised the fallback. The fixture now sets it explicitly. No assertion in those 11 changed except
  the callback URL expectation, which moved from `http://localhost:3000/...` to the configured host.
- Out of scope, recorded in D-036 Rejected: host/scheme validation in `omr-service` `notify_completion`
  (issue #71 option 2) — needs an image rebuild and VM deploy to be called done. Split into a follow-up issue
  when #71 closes.
- Local verification: `npx jest src/app/api/omr/upload` 13/13; `npm test -- --runInBand` 85 suites / 781;
  `npx tsc --noEmit` exit 0; lint clean; `npm run build` PASS; `git diff --check` clean. CI workflows all set
  `NEXTAUTH_URL`, so hosted E2E is unaffected.
- Manual `@coderabbitai review` requested at creation → **`Review rate limited`** (no review happened, as in PR #68
  Iteration 3). Re-requested once. Following the PR #68 precedent, an independent review of the full diff was run
  instead (`/code-review 109 high`); its nine findings are classified below. Two were confirmed defects in the
  first cut and are fixed in `24424e7` (regression) + `0dd2e07` (fix).
- Merge requires the user's explicit approval after CI and actionable review are clean.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| all hosted checks at `dd6f62b` | pass (16/16) | Security Audit·Scan·CodeQL·E2E×2 included |
| hosted checks at `0dd2e07` | pending | re-running after review fix |
| CodeRabbit | rate limited ×1, re-requested | no review object |

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | independent review | `upload/route.ts` callback block | `new URL(path, base)`만 검사해 `ftp://`·`ws://`·`file://` 스킴이 통과 — `getOmrServiceUrl()`은 막는 규칙 | ACCEPT | **FIXED** | `24424e7` 회귀(스킴 케이스 실패 관측) → `0dd2e07` `getOmrCallbackUrl()`이 같은 `parseHttpUrl` 사용 |
| R2 | independent review | `DECISIONS.md` D-036 | 운영 `NEXTAUTH_URL` 설정 근거가 2026-08-28 콜백 주소 관측인데, 제거되는 fallback도 같은 주소를 만들므로 증거가 안 됨 | ACCEPT | **VERIFIED** | `vercel env ls production` / `preview`에 `NEXTAUTH_URL` 존재(이름만, 389d). D-036 Context 갱신 |
| R3 | independent review | `upload/route.ts` | Preview에 `NEXTAUTH_URL`이 없어 preview 업로드가 503 | REJECT | — | Preview에도 설정돼 있음(`vercel env ls preview`). 이 PR 전에도 fallback을 타지 않았다. D-036 Consequence에 기록 |
| R4 | independent review | `upload/route.ts` `console.error(..., error)` | `ERR_INVALID_URL.base`가 raw `NEXTAUTH_URL` — userinfo 포함 시 로그 유출. `serviceUrl.ts`는 의도적으로 피함 | ACCEPT | **FIXED** | `24424e7` 회귀(`hunter2` 로그 관측) → `0dd2e07` 헬퍼가 값 없는 메시지로 throw, 라우트는 `error.message`만 로그 |
| R5 | independent review | `upload/route.ts` | trim/empty/parse를 인라인 재구현 — `serviceUrl.ts` 헬퍼 옆에 두어야 규칙이 어긋나지 않음 | ACCEPT | **FIXED** | `0dd2e07` `getOmrCallbackUrl()` + 공유 `parseHttpUrl` |
| R6 | independent review | `upload/route.ts` | 빈값 분기와 try/catch가 한 일을 두 분기로 | ACCEPT | **FIXED** | R5로 흡수 — 헬퍼 한 호출 |
| R7 | independent review | `upload/route.ts:116` | 설정 게이트 둘이 세션·formData 파싱 뒤에 있어 오설정 시 body 버퍼링 비용 | REJECT | — | pre-existing 순서. 인증보다 앞으로 올리면 비인증 호출자가 설정 상태를 알게 됨 — 별도 결정 |
| R8 | independent review | `upload/route.ts` | `NEXTAUTH_URL`의 path 접두를 `/api/omr/finalize`가 덮어씀 | REJECT (noted) | — | basePath 미사용, pre-existing. D-036 Consequence에 기록 |
| R9 | independent review | `upload/__tests__/route.test.ts` | 거부 케이스 3건이 같은 4개 assertion을 두 곳에서 반복 | ACCEPT | **FIXED** | `24424e7` 한 테이블(unset·blank·invalid·ftp·credentials) |

## Iteration log

### Iteration 1 — initial (2026-09-02 10:24 KST)

- Feedback fetched: 없음
- Changes made: 회귀 `dea17cc`, 수정+D-036 `dd6f62b`
- Validation: focused Jest 13, full Jest 781, tsc, lint, build, diff check 통과
- Remaining actionable items: hosted CI·CodeRabbit 결과 대기

### Iteration 2 — CodeRabbit rate limited → 독립 리뷰 9건 처리 (2026-09-02 10:55 KST)

- Feedback fetched: hosted 16/16 pass at `dd6f62b`. CodeRabbit `Review rate limited` — 리뷰 없음. `/code-review 109 high`
  독립 리뷰 9건.
- Verification performed: `vercel link`(`.vercel/`는 gitignore) 후 `vercel env ls production|preview` — `NEXTAUTH_URL`
  양쪽 존재. link가 부수 효과로 만든 `.env.local`(dev 값 다운로드)과 `.gitignore` 수정은 즉시 삭제·원복했다.
  worktree clean 확인.
- Changes made: R1·R4·R5·R6·R9 FIXED, R2 VERIFIED, R3·R7·R8 REJECT (근거 위 표). 회귀 `24424e7`, 수정 `0dd2e07`.
- Validation: `npx jest src/app/api/omr src/lib/omr` 5 suites / 64; `npm test -- --runInBand` 85 / 793; tsc 0; lint clean;
  build PASS; diff check clean.
- Commit: `0dd2e07`
- Remaining actionable items: `0dd2e07` hosted 재실행 확인; CodeRabbit 재요청 결과(있으면) 처리; 사용자 병합 승인 대기
