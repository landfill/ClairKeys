# PR159 — #134 잔여 인식 오류를 원본 17마디 기준으로 분리하고, 쉼표 뒤 Largo 표기를 시작 템포로 읽는다

Date: 2026-09-13
PR: https://github.com/landfill/ClairKeys/pull/159
Head: bec1e34de33792ea6fefe4dacc5626899f5f2ee8
Branch: `codex/issue-134-residual-timing`
Issue: #134 (부분 — 이 PR은 이슈를 완료하지 않는다. 본문은 닫힘 키워드를 쓰지 않고 `closingIssuesReferences`가 빈 목록임을 확인했다)

- 생성 시 non-draft, MERGEABLE. **병합 승인은 존재하지 않는다.** 초록 체크는 승인이 아니다.
- 커밋 두 개, 증거 우선:
  - `5d5333e`: 원본 17마디 기준 fixture와 평가기 오류 분류. trailer 13개가 파싱된다.
  - `bec1e34`: D-060 결정과 `scan_score`의 시작 템포 해석. trailer 15개가 파싱된다.
  근거: [검증 기록](../validation/2026-09-13-issue-134-residual-timing.md).
- 변경 파일: `fixtures/recognition/clair-de-lune-full-reference.json`(신규),
  `omr-service/omr/recognition_evaluation.py`(오프라인 도구), `omr-service/omr/musicxml_timing.py`,
  `omr-service/tests/test_recognition_evaluation.py`, `src/utils/__tests__/musicxmlTiming.test.ts`,
  `docs/recovery/DECISIONS.md`(D-060 — 결정이므로 예외 없이 이 PR에 포함).
  converter의 canonical 필드 형태, API·DB·앱 코드, Audiveris 실행 경로는 바뀌지 않았다.
- 초기 hosted 체크(생성 직후): Build Check, CodeQL, Detect changes, Lint, Lint and Type Check,
  Run Tests, Security Audit, Security Scan, Unit Tests, Vercel은 pass이고 E2E 2건은 pending이다.
  CodeRabbit은 "manual review required for this OSS repository"로 skip됐다. 최신 결과는 PR에서
  확인한다.
- 알려진 한계:
  - 평가기는 둘잇단 누락과 점 누락을 이벤트만으로 구분하지 못한다(m3 C5).
  - 판독은 한 명의 시각 판독이다.
  - 점·타이·잇단음표·성부 누락은 고치지 않았다.
  - 운영 배포와 재변환, 청취를 하지 않았다.
