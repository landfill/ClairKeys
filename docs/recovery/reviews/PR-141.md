# PR #141 — MusicXML beat positions and rhythm diagnostics

Date: 2026-09-05
PR: https://github.com/landfill/ClairKeys/pull/141
Branch: `codex/audit-musicxml-timing`
Initial head: `890cf99`

## Iteration 1

- Before implementation: 8/9 timing/diagnostic regressions failed. User-override behavior already passed.
- Rational quarter positions now precede a piecewise tempo integral. Covers in-measure changes, held notes,
  sound/direction offset precedence, late first tempo, fractional divisions and shared controlling-part boundaries.
- Self-review caught a regression for explicitly non-controlling parts. Added a failing case (second controlling
  bar moved from 3 to 4), then preserved independent boundaries and excluded those parts from the shared maximum.
- Real #134 MXL at its exact hash: **all 133 stored note dictionaries unchanged**; ten known overfull measures
  now appear in optional metadata diagnostics. This is not musical repair or grounds to close #134.
- Final converter/timing: 3 suites / 32 tests PASS. Full Jest: **101 suites / 959 tests PASS**.
- Independent typecheck/lint, production build and Python compilation PASS. Build skips its bundled type/lint;
  separate commands ran. The final Python exception fix is also covered by spawned-Python Jest tests.
- Built Chromium 390x844: actual converter output, 10-measure notice, non-correction explanation, no overflow
  or page errors. Score/session APIs mocked. Screenshot inspected.
- Hosted checks/review pending at submission. User delegated merge judgment. VM deploy only after merge.

## Deployment preparation

- VM `/opt/clairkeys-deploy` is clean at `acf25f8`; running image ID before this change:
  `12b9a021fad9b77bb74752fcd0da82f1d358916e9bfb4ed10e576addbae8559f`.
- Read `omr-service/deploy/README.md` and D-012. Retain rollback image, do not expose/change the shared secret,
  check no conversion is running, build the exact merged commit, restart and verify image/health/auth boundary.
- Existing same-PDF MXL in `/data/analysis/issue134-9m1By2/out/input.mxl` allows smoke verification without
  retaining or uploading the PDF again. No deployment has occurred at this entry.
