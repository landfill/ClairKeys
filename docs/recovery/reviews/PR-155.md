# PR155 — explore cards unified and made reachable

Date: 2026-09-12
PR: https://github.com/landfill/ClairKeys/pull/155
Head: 24b7e13
Branch: codex/issue-146-explore-upload
Issue: #146 stage 3, explore screen only

- Non-draft and MERGEABLE at this entry. Hosted checks were still running when this was written;
  consult the PR for their outcome before any merge decision.
- Three commits, regression-first: `77bb253` adds the tests (5 of 8 failing on the audited
  defects), `b265c7a` unifies the cards, `24b7e13` adds viewport/keyboard measurement and repairs
  an overflow that measurement exposed.
- Changed files: `src/components/browse/PublicSheetMusicBrowser.tsx`, `src/app/explore/page.tsx`,
  `src/components/browse/__tests__/PublicSheetMusicBrowser.test.tsx`, `e2e/explore-cards-responsive.spec.ts`.
  No API, database, animation-contract, playback or audio file is touched.
- Preserved and asserted: the three sections, their 3/4/8 sizes and data order, the error state
  with retry, and the empty state. Issue 146 requires the existing sections and order to stay, so
  splitting the shared feed to remove the visible repetition was out of scope.
- Removed rather than restyled: the `SHEET MUSIC / 악보 미리보기` placeholder (no stored preview
  image exists, and shrinking it keeps the same false promise) and three `전체 보기` buttons that
  had no handler and no destination.
- Behaviour deliberately unchanged: callers passing `onSheetMusicClick` still get the previous
  router-push navigation. The cards became links so keyboard users can reach a sheet at all and
  so open-in-new-tab works; the explore page's own navigation path is identical.
- A regression was caught by the new E2E and fixed before submission, not after. An `sr-only` rank
  span inside a truncating heading escaped the clip and widened the document to 1692px against a
  1440px viewport under WebKit CSS zoom 200%. It was confirmed against a clean `main` worktree that
  this was introduced here rather than pre-existing. The rank moved into the link's accessible name.
- Local verification: jest browse 8/8; full jest 1004 passed / 1 failed; full Playwright 105/105;
  explore responsive spec 40/40 across five browser projects and six viewport cases; tsc clean;
  ESLint clean; build succeeds.
- The single unit failure is `src/ci/__tests__/omrCallbackDelivery.test.ts` with `fastapi` missing
  from this machine's Python. It was reproduced on a clean `main` worktree rather than assumed,
  and this branch touches no Python or `omr-service` file.
- Not verified: real device, native browser zoom, screen reader, colour-contrast measurement, and
  live-data rendering (no local database; route fixtures were used). CSS zoom does not re-evaluate
  media queries and is not equivalent to browser zoom.
- WebKit does not tab to links by default; the keyboard case allows that for the `webkit` project
  only and still requires focus and Enter to follow the link there.
- Explicit user approval is required before main merge. The upload form slice of stage 3 is not in
  this PR and issue #146 does not close with it.
- Evidence: validation/2026-09-12-explore-cards.md.
