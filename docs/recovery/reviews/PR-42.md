# PR #42 — Take storage credentials off the OMR service instead of upgrading them (D-011)

- URL: https://github.com/landfill/ClairKeys/pull/42
- Branch: `codex/p1-omr-result-handoff`
- **Base: `codex/p1-upload-failure-visibility` (PR #41)** — stacked. Retarget to
  `main` once #41 merges.
- Head at creation: `e5aab93`
- Status: `OPEN` (review-ready, not draft)
- Opened: 2026-08-23

## Origin

D-011 was decided with the user on 2026-08-21 and recorded in HANDOFF, but had
no code and no `DECISIONS.md` entry. This PR is both.

The fact it rests on: `omr/storage.py` uploaded with `SUPABASE_ANON_KEY`, and
Storage RLS rejects that key with 403. The service could never have stored
anything; being undeployed is the only reason nobody saw it.

## The design question, and why it went the way it did

The reflexive repair is to give the service `SUPABASE_SERVICE_ROLE_KEY`. That
key bypasses RLS across the whole project, and the host would be a public-IP VM
with SELinux `Disabled` and firewalld `inactive`. The upload therefore moved to
the Next.js side, which already holds that key on Vercel, and the service now
holds no credential at all.

The secondary decisions all follow from how the route is called:

- The payload is on `GET /result/{job_id}`, not `/status`, because `/status` is
  polled in a loop.
- The store is keyed on the job id with `upsert`, because two polls can observe
  the same completed job at once. A random filename would produce two objects
  and orphan whichever lost the race to the database row.
- `title`/`composer` are no longer copied back from the service. Before PR #38
  that echo was the PDF's filename — the second link in the concealment chain.

## Shared secret

`/process`, `/status`, and `/result` require `X-ClairKeys-Token`; `/health`
stays open for nginx and uptime checks. An unset `OMR_SHARED_SECRET` makes the
service refuse **every** request rather than run unauthenticated, with
`ENVIRONMENT=development` the only exception — the same fail-closed shape as the
local-fallback guard it replaces.

Correction from the 2026-08-23 VM run: `/health` is **not** the only open
endpoint. `GET /` carries no `Depends(require_shared_secret)` either and answers
200 unauthenticated in both modes. It returns only a service name, a version
string, and `"status": "running"`, so the exposure is disclosure of the service's
existence to someone who already reached the port — but the sentence above, and
the same sentence in `omr-service/README.md`, overstate what is closed.

`omr/auth.py` imports stdlib only, with the FastAPI wiring left in `app.py`.
That is deliberate: `omr-service/tests` runs in an environment without fastapi,
so a check that imported it could only be asserted from an AST. It is the same
property that made the deleted `storage.py` testable for real.

## Correction carried by this PR

`animation-data` **exists** — public, 10 MB limit, `application/json` only. The
`[]` recorded on 2026-08-21 was the anon key lacking permission to list buckets.
D-011's premise is therefore confirmed rather than assumed.

## Verification at `e5aab93`

| Check | Result |
|---|---|
| 6 new Jest tests (status route) | Pass |
| Same 6 against pre-change code | **4 fail**; the 2 that pass are negative assertions that hold trivially when no store exists |
| `python -m unittest` (service contract + runtime) | 25 tests, OK |
| Python tests vs. pre-change tree | Fail by construction — `storage.py` existed, `animation_data_url` was emitted, `/result` was not declared |
| `npx jest` full suite | 13 failures, **byte-identical** to the pre-change baseline |
| `npx tsc --noEmit` | Only the 2 pre-existing errors |
| `next lint` | Clean |

## Verification on the NAVER VM at `dcc946a` (2026-08-23)

Full record: `docs/recovery/validation/2026-08-23-pr42-vm-verification.md`. The
service half of this PR has now been exercised against a live service, which is
the only place it can be — `omr-service/*.py` runs in no CI workflow, and this
PR's `codex/*` base means no workflow has run on it at all.

| Claim | Result |
|---|---|
| Image carries this PR's code (`storage.py` gone, `auth.py` present, `/result` declared) | PASS |
| Unset secret refuses `/process`,`/status`,`/result` with 503; `/health` open | PASS |
| Secret set: no token / wrong token → 401 on all three | PASS |
| Same request on the pre-change image | **422** — it accepted the request. 422→401 is the gate |
| Container env holds no `SUPABASE`/`SERVICE_ROLE` variable | PASS |
| Bach WTK1 Prelude 1 completes with no storage credentials | PASS — 47 s, `completed` |
| The identical job on the pre-change image | **failed** at 80%, per the PR #38 guard |
| `GET /result` returns the payload | PASS — 200, 45,580 bytes, 514 notes, `version` 1.0 |
| `GET /status` omits `animation_data` | PASS |
| Service wrote nothing (`find /data -type f`) | PASS — 0 files, no `file://`/`supabase` in logs |
| 30 s `/result` timeout headroom | 0.0138 s for 45 KB on loopback |

514 notes matches the 2026-08-21 `contract-fix` run, so the conversion is
unchanged by this PR — only the destination of its result.

## Finding from that run: the restart claim below was wrong

The "Not verified" list previously said a restart between completion and
collection "fails the row, which is correct but untested". Tested now, and it
does not fail the row.

After `podman restart`, `/status/{job}` returns **404**. The Next.js route treats
every non-ok status identically at
`src/app/api/omr/status/[jobId]/route.ts:88-93` — it logs, returns 502
`OMR_SERVICE_ERROR`, and **writes nothing to the database**. The row keeps
`status = processing` and its `omrJobId`, and every later poll takes the same
path to the same 502. The row can never be moved again.

That is the failure shape PR #41 exists to remove, reached through a different
door. It is **not a regression this PR introduces** — the pre-change service also
held job state in memory and also 404'd after a restart. What is new is only the
claim that it is handled.

The route does not distinguish "unreachable, poll again later" — the `catch`
branch above it, which deliberately leaves the stored status alone — from "the
service answered, and this job is permanently gone". A 404 from `/status` is the
second, and only the second is safe to mark failed. The service has no systemd
unit, so a VM reboot produces this for every in-flight job at once.

Not fixed here; it needs a decision on whether it belongs in this PR, in #41, or
in its own.

## Still not verified

- The **Next.js half** of D-011 — `/api/omr/status/[jobId]` fetching `/result`
  and storing it with `SUPABASE_SERVICE_ROLE_KEY` — has only ever run against
  Jest mocks. No real Supabase upload has happened.
- No browser-to-database end-to-end path, because the service is not reachable
  from Vercel at all: it binds `127.0.0.1:8000`, and the VM has no nginx, no TLS,
  and no domain.
- `omr-service/tests/*.py` still runs in no CI workflow, so the Python half of
  this evidence protects nothing automatically. The Next.js tests do run.

## Review history

- 2026-08-23: opened at `e5aab93`.
- 2026-08-23: `dcc946a` — self-review found the store had no time budget.
  D-011 moved the upload into `/api/omr/status/[jobId]`, so the completing poll
  now does a status fetch, a `/result` fetch with a 30 s timeout, and a Supabase
  upload in one invocation, while neither OMR route declared `maxDuration` and
  both ran on Vercel's shorter default. The failure would have been quiet: the
  function killed mid-store, the row keeping its empty `animationDataUrl`, and
  every subsequent poll taking the identical path. `maxDuration = 60` (the
  Hobby ceiling) added to the status route.

  Two related checks came back clean and are recorded so nobody repeats them:
  `Dockerfile.audiveris` uses `COPY . .`, so deleting `omr/storage.py` cannot
  break the image build; and no client code reads `animation_data_url` — the
  only reference was the route line this PR replaces.
- 2026-08-23: verified on the NAVER VM at `dcc946a`, before requesting merge
  approval rather than after. Three of the four "Not verified" items are closed;
  the restart item turned out to be a wrong claim rather than an untested one
  (see above). No code changed — the head is still `dcc946a`.

  Two corrections to this document came out of the same run: `GET /` is open
  without a token, not just `/health`; and the claim that a restart fails the row
  is false.

  Worth recording so nobody re-derives it: the VM build reused podman's cached
  layers and finished in seconds, because `Dockerfile.audiveris` is unchanged in
  this PR. A fast build is therefore not evidence the image is stale — but it is
  also not evidence it is correct, which is why the image contents were checked
  directly rather than inferred.
