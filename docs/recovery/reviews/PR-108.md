# PR #108 review log

Date: 2026-09-02 KST
PR: [browserslist advisory로 붉어진 Security Audit 게이트를 되돌린다](https://github.com/landfill/ClairKeys/pull/108)
Branch: `codex/deps-browserslist-audit`
Head: `3f7e277341c4f497c98841aacc19d665748e0d76`
Last checked: 2026-09-02 10:05 KST

## Current state

- Review-ready PR created on 2026-09-02. Not tied to a phase or issue: it unblocks a required CI gate.
- Cause: GHSA-c83g-rgw3-j3cx (high, `browserslist <=4.28.6`) was published 2026-09-01T16:42Z. `main`'s last
  successful Security Audit was 2026-09-01T06:12Z on `c9019b4`. Both `test.yml` Security Audit and
  `pr-checks.yml` Security Scan run `npm audit --audit-level high`, so every PR fails them from that
  moment. First observed on PR #107; also observed on `main` handoff commit `6ed651b`.
- Change: `package-lock.json` only. `npm audit fix` without `--force` bumped seven transitive packages
  (`browserslist` 4.28.6→4.28.8, `postcss` 8.5.22→8.5.26, and the browserslist data family). `package.json`
  unchanged. `--force` was rejected because it installs `next@16`.
- Local verification: `npm audit --audit-level high` 0 vulnerabilities; Jest 85 suites / 777 tests;
  `npx tsc --noEmit` exit 0; lint clean; `npm run build` PASS with 33/33 static pages.
- CodeRabbit manual review was **not** requested: the diff is a lockfile-only dependency bump with no
  application code.
- Merge requires the user's explicit approval. **Merge this before PR #107** so #107 can re-run its Security
  checks against a clean lockfile (rebase or merge `main` into `codex/issue-72-finalize-route`).

## CI status

| Check | Status | Last evidence |
|---|---|---|
| All 17 hosted checks (Lint, Lint and Type Check, Run Tests, Unit Tests, Build Check, E2E Tests ×2, Security Audit, Security Scan, CodeQL, Vercel, aggregate) | **pass** | head `3f7e277`, 2026-09-02 10:05 KST |
| Mergeability | `MERGEABLE` / `CLEAN` | same |

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| — | — | — | 아직 리뷰 피드백 없음 | — | — | — |

## Iteration log

### Iteration 1 — initial (2026-09-02)

- Feedback fetched: 없음
- Changes made: lockfile bump, single Lore commit
- Validation: audit 0, Jest 777, tsc, lint, build 통과
- Commit: `3f7e277`
- Remaining actionable items: 없음. 사용자 병합 승인 대기

### Iteration 2 — hosted CI 완료 (2026-09-02 10:05 KST)

- Feedback fetched: 17/17 pass, Security Audit·Security Scan 모두 pass — 이 PR의 목적이 hosted에서 확인됨
- Changes made: 없음
- Remaining actionable items: 없음. `MERGEABLE`/`CLEAN`. 사용자의 명시적 병합 승인 대기
