# PR Review Log — PR #129

PR URL: https://github.com/landfill/ClairKeys/pull/129
Branch: `codex/issue-126-fingering-cost-model` (head `57bf74e`)
Issue: [#126](https://github.com/landfill/ClairKeys/issues/126) 2순위 (비용 모델)
Last checked: 2026-09-05 KST — OPEN, Codex 독립 리뷰 1라운드 반영 완료, 병합 승인 대기

## 목적

`phrase-dp-v1`이 하행·반복·비CAGED 구간에서 엄지를 연속 반복하는 원인을 비용 모델에서 제거한다.
`transitionCost`가 음 진행과 반대 방향의 손가락 이동을 일괄 `+10` 처벌했는데 엄지 넘김이 정의상 그
전이이고, `fingerDelta === 0`은 어떤 처벌도 받지 않았다.

이슈 #126의 2순위만 다룬다. 1순위(경계 문맥 전달)와 3순위(계약 확장)는 범위 밖이므로 **이 PR은 이슈를
닫지 않는다.**

## 작업 순서 이탈 기록

이슈 #126은 1순위 → 2순위 순서를 제안했다. 이번 작업은 2순위를 먼저 했다. 사용자가 2026-09-05에 순서를
명시적으로 선택했고, 화면에 보이는 결함이 2순위에 속하며, 2순위의 결과가 1순위 항목 하나("패턴 층을 조표
기반으로 일반화한다")를 소멸시킨다. AGENTS.md "구현이 스펙·phase 문서와 달라야 할 이유가 생기면 코드부터
고치지 않는다"에 따라 D-041 Context에 이유를 먼저 기록한 뒤 구현했다.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Detect changes | pass | run 33903706427 |
| Lint | pass | run 33903706431 |
| Lint and Type Check | pass | run 33903706427 |
| Security Audit | pass | run 33903706431 |
| CodeRabbit | pass | Review skipped — 10 star 미만 OSS repo 자동 리뷰 제외 |
| Vercel / Vercel Preview Comments | pass | 배포 완료 |
| Build Check / E2E Tests / Security Scan / Unit Tests / Run Tests | pass | `82bc04a`에서 전부 통과 확인 |
| CodeQL / All Checks Complete / PR Summary | pass | `82bc04a` |

`57bf74e` push 후에도 `gh pr checks 129`의 모든 체크가 pass다 (2026-09-05 확인).

로컬 검증은 전부 통과했다: 전체 Jest 870건, `tsc --noEmit`, `next lint`, `next build`.
근거는 `docs/recovery/validation/2026-09-05-issue-126-fingering-cost-model.md`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | self | `fingeringUtils.ts` DP 상태 | 손 위치를 별도 상태 차원으로 넣으면 전이가 25→10,000 (이슈 명시 제약) | anchor를 `(손가락, 음높이)`에서 유도해 상태 5개 유지 | CLOSED | 12,000음 12.6ms (v1 9.9ms), 1.27배 |
| R2 | self | `applyMajorScaleRuns` | 조표 기반 일반화 대신 삭제할 근거가 있는가 | DP 비용표를 출력해 확인 — DP 자체 답이 정석이고 패턴 층은 경계 불연속을 만들고 있었다 | CLOSED | validation 2절 (G장조 9음 진단) |
| R3 | self | `fingeringUtils.test.ts:352` | 기존 테스트 왼손 기댓값 `[5,3,2,5]` 변경 | v1은 16반음 베이스 구간에서 엄지 미사용. v2 결과 `[5,1,2,5]`로 갱신하고 주석에 이유 기록 | CLOSED | `82bc04a` |
| R4 | self | 반복음 회귀 테스트 | `[3,2,1,3,2,1,3,2]`를 정확히 기대했으나 실제로는 동점 경로 존재 | 모델을 기댓값에 맞추지 않고, 동점 해소를 고정하지 않는 성질 검사로 변경 | CLOSED | `82bc04a` |
| R5 | self | 상수 튜닝 | LH 2옥타브 C장조가 교과서와 4-그룹 위치가 다르다 | 튜닝하지 않는다 — D-039가 유일한 정답 주장을 거부한 전제를 되돌린다. 사실을 검증 기록에 남긴다 | CLOSED | validation 4절, D-041 Rejected 4 |
| R6 | Codex 워커 | `fingeringUtils.test.ts` same-centre 화음 회귀 | 성질 단언이 수정 전 중심 비교 구현(`1 5 2 4`)도 통과한다 — 잡으려던 결함을 통과시킨다 | ACCEPTED, 정확 출력 `1 4 1 3` 고정으로 교체 | FIXED | `57bf74e`, 결함 주입 재현 validation 10-1 |
| R7 | Codex 워커 | `fingeringUtils.test.ts` `crossings` 헬퍼 | `pitchDelta === 0`에서 `Math.sign(0)` 때문에 손가락 변화를 전부 넘김으로 센다 (잠복) | ACCEPTED, 두 delta가 모두 0이 아닐 때만 센다 | FIXED | `57bf74e`, validation 10-2 |
| R8 | Codex 워커 | `fingeringUtils.test.ts:352`, validation 8절 | "G#3으로 넘어간다"는 설명이 사실과 다르다 — 9반음은 `CROSSING_MAX_INTERVAL`(5) 초과라 넘김이 아니라 손 이동 | ACCEPTED, 실제 기전(완전5도 = 자연 손 벌림 → anchor 일치, travel 0)으로 정정 | FIXED | `57bf74e`, validation 10-3 |
| R9 | Codex 워커 | `phases/ISSUE-126-*.md` 완료 조건 | "어떤 phrase에서도 3연속 없음"이 거짓 — 하행 완전5도가 `5 2 2 2 2 2 5 1` | ACCEPTED. 모델이 아니라 문서를 고친다: 넘김은 5반음 이내에서만 성립하므로 도약 연속에는 후보가 없고, 한 손가락 재사용이 실제 연주와 일치한다. 이슈가 허용한 "정당한 예외 명시"로 처리하고 회귀 고정 | FIXED | `57bf74e`, validation 10-4 |
| R10 | Codex 워커 | 복잡도 표현 | "전이 25개"는 단음 기준. 화음 이벤트는 후보 `C(5,k)`=10개라 전이 최대 100개 | ACCEPTED, 주석과 D-041을 "단음 이벤트당"으로 한정. 함께 제기된 `samePitchSet` O(k) 우려는 워커 자신이 정정(총 음 수 기준 O(N) 상수배) | FIXED | `57bf74e`, validation 10-5 |
| R11 | Codex 워커 | 이슈 #126 live state | PR·HANDOFF는 "열린 채"라고 쓰는데 실제로는 CLOSED | ACCEPTED — 원인은 `main` 커밋 `39f561c`의 `Directive: Do not close #126` 문구다. GitHub이 `close #126`만 매칭해 닫았다 | FIXED | `gh issue reopen 126`, validation 11절 |

## Iteration log

### Iteration 1 — 생성 (2026-09-05)

- Feedback fetched: 없음 (PR 생성 직후). CodeRabbit은 이 저장소에서 자동 리뷰를 건너뛴다.
- Changes made: `82bc04a` — 비용 모델 교체, 패턴 층 삭제, 회귀 13건, D-041, 단계 문서
- Validation: `docs/recovery/validation/2026-09-05-issue-126-fingering-cost-model.md`
  (재현 → 진단 → 재현 → 24개 조성 192개 전수 스윕 → fixture 불변식 → 성능 → 필수 검증)
- Remaining actionable items: 독립 리뷰 미실시.

### Iteration 2 — Codex 독립 리뷰 (2026-09-05)

- Feedback fetched: Orca orchestration 워커 (run `run_9eba0e2f17e4`, task `task_e1e2f41a7fe5`, dispatch
  `ctx_84ac42063753`). **현재 워크트리 공유·읽기 전용**으로 실행했다 — 워커는 브랜치 전환도 파일 수정도
  하지 않았고, diff는 `git show <branch>:<path>`로만 읽었다. `worker_done` 수신 후 `worker-release` 완료.
- **워커의 6개 주장을 전부 이 세션에서 v2 소스에 대고 직접 재현했다. 기각한 항목은 없다.** 특히 R6은 옛
  중심 비교 구현을 사본에 주입해 성질 단언이 결함을 통과함을 실증했다(validation 10-1).
- Changes made: `57bf74e` — 회귀 2건 정정, 잘못된 설명 2곳 정정, 완료 조건에 도약 예외 명시와 회귀 1건
  추가, 복잡도 표현 한정
- Validation: `npx jest --runInBand` **871 passed**, `npx tsc --noEmit` exit 0, `npm run lint` 경고 0,
  `npm run build` 성공
- Remaining actionable items: 없음. 사용자의 명시적 병합 승인 대기.

## 병합 후 남는 것

- 이슈 #126은 열린 채로 둔다. 1순위(`keySignature`·`timeSignature`·`staff`·`voice` 전달, `voice` 기반
  이벤트 묶기)가 남아 있다. 1순위 항목 중 "패턴 층을 조표 기반으로 일반화"는 이 PR로 소멸했다.
- 411음 운영 JSON의 연주 가능성 수동 검토는 앱에서 수행해야 한다 (원본이 저장소에 없다 — D-040).
