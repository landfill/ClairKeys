# PR #41 — Stop an undeployed OMR service from looking like an application bug

- URL: https://github.com/landfill/ClairKeys/pull/41
- Branch: `codex/p1-upload-failure-visibility`
- Head at creation: `8629ead`
- Head now: `48d123c`
- Status: `OPEN` (review-ready, not draft)
- Opened: 2026-08-23

## Origin

The user reported on 2026-08-23 that uploading sheet music in **production**
(`clairkeys.vercel.app`) created a database row, returned `Internal server
error`, and stored no file.

All three symptoms trace to one fact already recorded in HANDOFF: the OMR
service has never been deployed anywhere, and `OMR_SERVICE_URL` still named
`https://clairkeys-omr.fly.dev`. Confirmed live:

```
$ curl -sS -m 15 https://clairkeys-omr.fly.dev/health
curl: (35) schannel: failed to receive handshake, SSL/TLS connection failed
```

Fly's wildcard DNS resolves the name, so the request reaches TLS and `fetch`
**throws**. A thrown `fetch` is not a non-ok response, so it skipped the
`!omrResponse.ok` branch — which would have said "Failed to start OMR
processing" — and fell into the outer `catch`, whose message is exactly the
`Internal server error` the user quoted. Storage never ran because storage was
the OMR service's step.

## The defect this PR fixes

The `catch` branch never marked the created row `failed`. The row stayed at
`processing` with no `omrJobId`, and `/api/omr/status/[jobId]` looks rows up by
`omrJobId` — so nothing could ever move it again. One permanent orphan per
upload attempt.

## What it deliberately does not do

It does not make upload succeed. D-010 accepted that upload fails visibly until
issue #22 lands. No demo generator, no fallback, no nicer message presented as a
fix. The failure is now honest about *which* failure it is.

## Incidental finding, recorded because it corrects the record

`animation-data` **exists**. A service-role probe of `GET /storage/v1/bucket`
returned all three buckets (`sheet-music-files`, `temp-uploads`,
`animation-data`; the last is public, 10 MB limit, `application/json` only). The
`[]` recorded on 2026-08-21 was the anon key lacking permission to list buckets,
not a missing bucket. This removes a risk from the D-011 work: the service-role
upload path has a bucket to write to.

## Verification at `8629ead`

| Check | Result |
|---|---|
| 3 new regression tests, written first | Failed against previous code (500 not 503; no `failed` update), pass after |
| `npx jest` full suite | 367 passed / 13 failed |
| Failure set vs. pre-change baseline | **Byte-identical** (`diff` of sorted failure names, empty) |
| `npx tsc --noEmit` | Only the 2 pre-existing errors (stale `.next` validator ref to a deleted route, missing `web-vitals`) |
| `next lint` | No warnings or errors |

The 13 baseline failures are environment dependent: 10 `converter corpus`
cases plus the `.mxl` CLI case (need Python), `omrRuntimeContract`,
`prChecksWorkflow`, and one `uploadPathInventory` demo-writer assertion.

`uploadPathInventory.test.ts` was updated in the same commit: it asserted the
route source contains the literal `OMR_SERVICE_URL`, which moved into
`src/lib/omr/serviceUrl.ts`. The property it pins is unchanged — one route, and
only one, reaches the service — so the assertion now looks for
`getOmrServiceUrl` and additionally pins that the two non-canonical routes call
neither.

## Second commit `1750ec5` — the same stranded row, reached from the other side

Added 2026-08-23 at the user's direction, after the VM verification of PR #42
found it. It belongs here rather than in #42 because removing the stranded row
is what this PR is for; #42 only happened to be the run that exposed the second
route into it.

The upload route reached that state by creating a row before a `fetch` that
threw. The status route reaches it from the other side: it looks rows up by
`omrJobId`, and it returned 502 for **every** non-ok status without writing
anything. A row it could not resolve stayed at `processing` while every later
poll took the identical path to the identical 502.

What turned this from theory into a measurement is the live service. Job state
is held in memory, so after `podman restart` the same job answers **404** on
both `/status` and `/result` — observed on the VM on 2026-08-23. The host has no
systemd unit, so a reboot does this to every in-flight job at once.

The fix is a single distinction the route never drew:

- **"I cannot reach the service"** — transient. The `catch` branch above already
  gets this right by deliberately leaving the stored status alone. 5xx, 401, and
  503 belong with it: a failing service, a shared secret that does not match
  yet, one not set at all. An operator fixes those and the same job resumes, so
  failing the row would destroy recoverable work.
- **"the service answered, and this job is gone"** — permanent. Only a 404 means
  this, and only 404 now fails the row.

The 404 response is `200` with `status: 'failed'`, not an error status. The
poller in `OMRProcessingStatus.tsx:72-74` throws on any non-ok response and
reports a generic "failed to check status"; answering 200 puts it on the branch
it already has for a service-reported failure, which carries a message the user
can act on.

A 404 from `/result` after `/status` said `completed` is not separately handled,
and does not need to be: it requires a restart between two calls in one
invocation, and the next poll's `/status` 404 fails the row through the path
above.

### Verification at `1750ec5`

| Check | Result |
|---|---|
| 7 new cases in `lostJob.test.ts`, written first | **3 fail** against pre-change code (the 404 cases); 4 pass before and after |
| Why the 4 pass both ways | They pin the transient statuses — 500/503/401 and an unreachable service — so the fix cannot overreach into them |
| `npx jest` full suite | **44 suites / 399 tests, 0 failures** |
| `npx tsc --noEmit` | exit 0, no errors |
| `next lint` | No warnings or errors |

The zero-failure suite is not a contradiction of the 13-failure baseline above.
That baseline is for a machine without Python; this run had Python 3.14.3, so
the 10 `converter corpus` cases and the `.mxl` CLI case execute instead of
failing. `omrRuntimeContract` and `prChecksWorkflow` also passed here. **Compare
future runs against the environment, not against a remembered number.** The two
`tsc` errors recorded above are likewise gone — they came from a stale `.next`
validator reference.

The test lives in `lostJob.test.ts` rather than `route.test.ts` on purpose: PR
#42 adds a `route.test.ts` for the same route, and two branches adding the same
path is a merge conflict for no benefit.

### Stack maintenance

PR #42 is stacked on this branch, and edits the same file. The change here is
confined to the `!statusResponse.ok` block, which #42 does not touch, so
`codex/p1-upload-failure-visibility` merged into `codex/p1-omr-result-handoff`
cleanly (`f540752`). That branch now carries both, and its full suite is 45
suites / 405 tests with 0 failures, `tsc` exit 0, lint clean.

## Third commit `48d123c` — CodeRabbit's one finding, verified and fixed

CodeRabbit left a single inline comment, on `src/lib/omr/serviceUrl.ts` at
`8629ead`: `getOmrServiceUrl` checks only that the value is non-empty, so
`OMR_SERVICE_URL='/'` returns `''` and the caller fetches a relative URL.

**Checked against the code before acting, and its Major rating is too high.**
`markFailed` runs in the upload route's `catch`, so the row is marked `failed`
rather than stranded, and no data is damaged. What survives verification is the
**misclassification**: every malformed value throws at `fetch` and is reported
`OMR_SERVICE_UNAVAILABLE` — "잠시 후 다시 시도해 주세요" — for a value that will
never work, while the operator who could fix it is told nothing.

That is this PR's own defect one layer up. It removed a *default* naming a host
nobody deployed; the same concealment remained for an *input* nobody can reach.

Each case was run through Node's `URL` and `fetch` before the fix was written,
rather than reasoned about:

| `OMR_SERVICE_URL` | Before | After |
|---|---|---|
| `http://101.79.16.73:3000` | works | works |
| `http://101.79.16.73:3000/` | works (trailing slash already stripped) | works |
| `101.79.16.73:3000` (no scheme) | `TypeError` → "try again later" | `OMR_SERVICE_NOT_CONFIGURED` |
| `example.com:3000` | `fetch failed` → "try again later" | `OMR_SERVICE_NOT_CONFIGURED` |
| `/` | `TypeError` → "try again later" | `OMR_SERVICE_NOT_CONFIGURED` |
| `ftp://…`, `htp://…`, `file://…` | `fetch failed` → "try again later" | `OMR_SERVICE_NOT_CONFIGURED` |
| unset / whitespace | already correct | unchanged |

`example.com:3000` is the case that makes parsing insufficient on its own:
`new URL` accepts it, reading `example.com:` as the scheme. The check is on the
scheme, not on whether parsing succeeded.

The error carries a reason but **never the value** — this route logs the error
and echoes its message in development, and a URL can hold credentials. Naming
the scheme diagnoses a typo without writing a password to a log; there is a test
pinning that `ftp://admin:hunter2@host` produces neither the password nor the
host.

Why now rather than later: the next step of this work is a person typing this
variable into the Vercel dashboard. A dropped `http://` is the likeliest mistake
there, and it would have produced a message sending the reader after a network
fault that does not exist.

### Verification at `48d123c`

| Check | Result |
|---|---|
| 16 cases in `serviceUrl.test.ts`, written first | **9 fail** against pre-change code |
| The 7 that pass both ways | Valid http/https URLs, trailing slash, whitespace trim, unset — the fix must not disturb these |
| `npx jest` full suite | 45 suites / 415 tests, 0 failures |
| `npx tsc --noEmit` | exit 0 |
| `next lint` | Clean |
| Hosted checks | **17/17 pass** |

## Review coverage — read this before trusting the green CodeRabbit check

`gh pr checks` reports `CodeRabbit  pass` on this PR, and that does **not** mean
it reviewed anything. The message beside it is `Review skipped: manual review
required for this OSS repository`. Only `8629ead` was ever reviewed; `1750ec5`
and `48d123c` were not, and a `@coderabbitai review` request on 2026-08-23
returned `Review rate limited`.

This is the same shape as the CI trap on #42 — a check that is green because it
did not run. It is also the third and fourth occurrence of the rate-limit
pattern already recorded for PRs #34 and #35.

## Not verified

- No live OMR service exists **that this code can reach** — the VM service is
  bound to `127.0.0.1`, so no browser-to-database path has run. The 404 fix and
  the URL validation are exercised only by Jest.
- The 503/502 response bodies have not been rendered by `OMRUploadForm` in a
  browser.

## Review history

- 2026-08-23: opened at `8629ead`. Every hosted check passed.
- 2026-08-23: `1750ec5` — the 404-after-restart stranded row, found by the PR #42
  VM verification and assigned to this PR by the user. CodeRabbit reported
  `Review skipped: manual review required for this OSS repository` on this push,
  so there is no automated review of this commit. 17/17 hosted checks pass.
- 2026-08-23: `48d123c` — CodeRabbit's only finding, on `8629ead`, addressed.
  Verified against the code first: the finding is valid but its Major rating is
  not, since `markFailed` already prevents the stranded row it implies. 17/17
  hosted checks pass; again no CodeRabbit review of the commit itself.
- 2026-08-23: `codex/p1-upload-failure-visibility` merged into
  `codex/p1-omr-result-handoff` twice, at `f540752` and `f328dc9`, to keep the
  stack carrying both fixes. Both merges were automatic — `serviceUrl.ts` is
  touched by both branches but in different regions (#42 appends
  `omrAuthHeaders`, this PR edits `getOmrServiceUrl`). Merged-branch suite:
  46 suites / 421 tests, 0 failures, `tsc` exit 0, lint clean.
