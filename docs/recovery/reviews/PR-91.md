# PR Review Log — PR #91

PR URL: https://github.com/landfill/ClairKeys/pull/91
Branch: `codex/ds-3-upload-processing`
Last checked: 2026-08-29 KST

DS-3(업로드와 처리 상태). 커밋 5개, head `eace453`.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Run Tests / Unit Tests | pass | `eace453` — 72 suites / 736 tests |
| E2E Tests (×2) | pass | `eace453` |
| Lint / Lint and Type Check | pass | `eace453` |
| Build Check | pass | `eace453` |
| Accessibility Check | pass | `eace453` |
| CodeQL / Security Scan / Security Audit | pass | `eace453` |
| Vercel | pass | Preview 배포 완료 |
| All Checks Complete | pass | `eace453` |

15개 체크 전부 성공. `mergeStateStatus`는 `BLOCKED` — 승인 리뷰가 없기 때문이며, 사용자 승인 대기와
같은 의미다.

**axe가 이 PR을 판정하지 않는다는 점에 주의한다.** Accessibility Check는 `NEXTAUTH_URL`(홈)만
훑는데 이 PR이 바꾼 `/upload`는 `AuthGuard` 뒤에 있다. 초록색이 이 화면의 접근성을 말해 주지
않는다 — 라벨 연결·`aria-live`·색 외 구분은 Jest 회귀로 직접 걸었고, 반응형과 상태 렌더는 로컬에서
세션 쿠키를 발급해 실제 브라우저로 측정했다
(`docs/recovery/validation/2026-08-29-DS-3-upload-processing.md`).

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| — | — | — | 아직 리뷰 피드백 없음 | — | — | — |

**CodeRabbit**: 자동 리뷰는 `Review skipped: manual review required for this OSS repository`로
건너뛰었다(별 10개 미만 저장소). 2026-08-29에 `@coderabbitai review`로 수동 요청했다
([코멘트](https://github.com/landfill/ClairKeys/pull/91#issuecomment-5462635605)). PR #90에서는
같은 요청이 rate limit에 걸렸다.

**수집 방법**: 세 표면을 모두 조회한다 — 리뷰 스레드(GraphQL `reviewThreads`), 리뷰
본문(`pulls/91/reviews[].body`의 접힌 블록), 일반 코멘트(`issues/91/comments`). PR #89에서 스레드만
보고 3건을 놓친 적이 있다.

## 스스로 확인한 것 (리뷰 전 자기 점검)

- **범위 이탈 1건을 규약대로 처리했다.** DS-3의 Out of scope가 백엔드 변경을 막는데
  `/api/omr/status/[jobId]`에 `code: 'OMR_JOB_LOST'`를 더했다. 코드보다 결정을 먼저 고쳤고
  (D-028, 커밋 `fd5d85d`), 구현은 그다음 커밋 `ded91b7`이다.
- **완료 조건 하나를 문자 그대로 따르지 않았다.** `이 페이지를 닫아도 계속 처리됩니다`를 "고정
  노출"하되, 남은 작업이 없으면 내린다. 전부 끝난 화면에서 그 문장은 고정 노출이 아니라 사실이
  아닌 문장이기 때문이다. 양쪽 다 회귀로 걸었다.
- **테스트가 무의미하게 통과하던 것을 하나 잡았다.** `useFakeTimers()`를 `render` 뒤에 켜면 이미
  진짜 타이머로 잡힌 폴링 간격이 울리지 않아, "완료 뒤에는 더 묻지 않는다"가 아무것도 검사하지
  않은 채 통과했다. 옆 테스트가 실패해서 발견했다.

## 병합

- 미병합. 사용자의 명시적 승인 대기 중.
