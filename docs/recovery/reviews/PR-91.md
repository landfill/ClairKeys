# PR Review Log — PR #91

PR URL: https://github.com/landfill/ClairKeys/pull/91
Branch: `codex/ds-3-upload-processing`
Last checked: 2026-08-29 KST (병합 완료)

DS-3(업로드와 처리 상태). 커밋 8개, head `6a5eeea`.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Run Tests / Unit Tests | pass | `6a5eeea` — 73 suites / 751 tests |
| E2E Tests (×2) | pass | `6a5eeea` |
| Lint / Lint and Type Check | pass | `6a5eeea` |
| Build Check | pass | `6a5eeea` |
| Accessibility Check | pass | `6a5eeea` |
| CodeQL / Security Scan / Security Audit | pass | `6a5eeea` |
| Vercel | pass | Preview 배포 완료 |
| All Checks Complete | pass | `6a5eeea` |

17개 체크 전부 성공(`6a5eeea`). 앞선 head `eace453`·`bb08e7f`에서도 전부 성공했다. `mergeStateStatus`는 `BLOCKED` — 승인 리뷰가 없기 때문이며, 사용자 승인 대기와
같은 의미다.

**axe가 이 PR을 판정하지 않는다는 점에 주의한다.** Accessibility Check는 `NEXTAUTH_URL`(홈)만
훑는데 이 PR이 바꾼 `/upload`는 `AuthGuard` 뒤에 있다. 초록색이 이 화면의 접근성을 말해 주지
않는다 — 라벨 연결·`aria-live`·색 외 구분은 Jest 회귀로 직접 걸었고, 반응형과 상태 렌더는 로컬에서
세션 쿠키를 발급해 실제 브라우저로 측정했다
(`docs/recovery/validation/2026-08-29-DS-3-upload-processing.md`).

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | CodeRabbit 인라인 | `src/lib/upload/pdfInspection.ts:42` | `fileSignature`가 `name:size`뿐이라 같은 이름·크기의 **다른** 파일을 중복으로 막는다 | **accept** | FIXED | `bb08e7f` — `lastModified` 추가, 회귀 3건 선행 |
| R2 | CodeRabbit 인라인 | `src/lib/upload/processingStages.ts:17` | `ProcessingStage` 인터페이스를 `src/types/`로 이동 | **reject** | REJECTED | 아래 근거 |
| R3 | CodeRabbit 리뷰 본문 (nitpick) | `src/components/upload/OMRUploadForm.tsx:265-273` | 성공 응답 처리까지 `try`가 감싸 업로드 성공을 서비스 장애로 표시할 수 있다 | **accept**(다른 방식) | FIXED | `6a5eeea` |
| R4 | CodeRabbit 리뷰 본문 (outside diff) | `docs/recovery/phases/DS-3-upload-processing.md:20` | In scope의 "4단계"를 "5단계"로 | **reject** | REJECTED | 아래 근거 |

**R2 기각 근거 — 저장소의 실제 코드.** `src/types/`는 화면과 서비스가 함께 쓰는 도메인
계약(animation·piano·sheet-music·category)의 자리이고, 모듈이 소유한 인터페이스는 모듈 옆에 두는
것이 이 저장소의 관행이다: `src/lib/analytics.ts`(`PerformanceMetric`, `PerformanceAlert`),
`src/lib/cache.ts`(`CacheItem`, `CacheOptions`, `StoredCacheItem`), `src/services/*`(`UploadResult`,
`AudioSettings`, `ProvenanceCandidate` 등). `src/lib` 전체에 `export interface`가 0건인 것은 규칙이
아니라 지금까지 lib 모듈이 인터페이스를 내보낼 일이 없었기 때문이다. `ProcessingStage`는 D-026
결정 4의 표를 인코딩한 것이고 소비자가 둘뿐이라, 표에서 떼면 대조할 자리가 멀어진다. AGENTS.md의
Project Reference 절도 "코드가 바뀌면 낡아질 수 있다 — 불확실하면 실제 코드를 우선한다"고 스스로
밝힌다.

**R4 기각 근거 — 오기가 아니다.** D-026 G1-2도 "**4개 처리 단계**는 업로드 화면에서만 표시한다"고
쓰고 같은 자리에 5행짜리 표를 둔다. 표의 첫 행 `0 (queued)`는 단계가 아니라 단계 이전 상태다.
화면은 대기 중 + 4단계를 그리므로 문서와 구현은 일치한다. 문구를 고치면 오히려 D-026과 어긋난다.

**R3은 제안된 diff와 다르게 고쳤다.** 제안은 `classifyUploadResponse`로 분류하는 것인데, 그러면
화면이 "잠시 후 다시 시도해 주세요"를 띄운다. **변환은 이미 시작된 뒤**라 그 안내를 따르면 같은
악보의 행이 둘 생긴다. 이 경우는 실패 4종(D-026)이 아니라 폼이 상태를 잃은 것이므로 401·빠르기
오류와 같은 폼 수준 안내로 처리하고, 다시 올리라고 하는 대신 결과를 확인할 곳(내 악보)을 가리킨다.

### 2차 — Orca 오케스트레이션 Codex 워커 (2026-08-29)

사용자 지시로 Orca 오케스트레이션 워커를 띄워 리뷰했다(`run_0ebb510b2751` /
`task_8cfd7e6775bc` / dispatch `ctx_45140e18436e`, 현재 워크트리, 워크트리 분리 없음).
7건을 받아 **6건 수용, 1건 이월**했다. 전부 코드에서 재확인한 뒤 판정했다.

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| X1 | Codex 워커 (**blocker**) | `OMRUploadForm.tsx` + `page.tsx` | 중복 가드가 **작업 유실 복구를 불가능하게** 한다. 화면이 "같은 파일을 다시 올려 주세요"라고 한 뒤 그 파일을 막는다. 완료·변환 실패 뒤에도 같다 | **accept** | FIXED | `12575c0` |
| X2 | Codex 워커 (major) | `pdfInspection.ts`, `OMRUploadForm.tsx` | Vercel Function 요청 본문 한도 4.5MB인데 화면은 50MB를 약속한다. 5MB PDF는 413으로 거부되고 "잠시 후 재시도"로 분류돼 **성공할 수 없는 재시도**를 권한다 | **defer** | CARRIED | 아래 |
| X3 | Codex 워커 (major) | `OMRProcessingStatus.tsx:86-160` | `setInterval`이 async 폴링을 겹쳐 실행할 수 있고, 늦게 온 응답이 완료·실패를 `processing`으로 되돌린다 | **accept** | FIXED | `12575c0` |
| X4 | Codex 워커 (major) | `OMRProcessingStatus.tsx:278` | 미도달 단계 라벨 `text-ink-muted opacity-60` → **2.71:1** (AA 4.5:1 미달) | **accept** | FIXED | `12575c0` |
| X5 | Codex 워커 (minor) | `OMRProcessingStatus.tsx` ×3 | `<Link><Button>`이 `<a><button>` 중첩을 만든다 | **accept** | FIXED | `12575c0` |
| X6 | Codex 워커 (minor) | `OMRUploadForm.test.tsx` | "키보드로 도달할 수 있고" 테스트가 키보드를 검사하지 않는다 | **accept** | FIXED | `12575c0` |
| X7 | Codex 워커 (minor) | `pdfInspection.ts:107-118` | `/Encrypt` 꼬리 substring 검사가 멀쩡한 PDF를 거부할 수 있다 | **accept** | FIXED | `6a5eeea` |

**X1이 이 리뷰의 값어치다.** 폼과 패널을 따로 보면 둘 다 옳은데 합치면 서로의 안내를 무효로
만든다 — 어느 컴포넌트의 단위 테스트도 볼 수 없는 자리였다. 원인은 소유권이었다: 중복 가드는
폼에 있고 작업의 끝은 패널만 안다. "무엇이 아직 변환 중인가"를 둘 다 보는 페이지가 판정하게
바꾸고, 회귀를 `src/app/upload/__tests__/page.test.tsx`(신규)에 뒀다. 고치기 전 3건이 실패하는
것을 확인했다.

**X4는 직접 계산해 확인했다.** `--ck-ink-muted`(#565c6b)는 흰 표면에서 6.69:1인데 `opacity-60`이
합성색 #9a9da6을 만들어 2.71:1이 된다. **DS-1 토큰을 썼다는 사실이 대비를 지켰다는 뜻이 되지
않는다** — 팔레트는 불투명도 1을 전제로 계산돼 있다.

**X3은 단조성 가드 대신 겹침 자체를 없앴다.** 앞선 조회가 끝난 뒤 다음 조회를 예약하면 리뷰어가
제시한 경합 상태에 도달할 수 없다. 닿지 않는 방어 코드는 검증할 수도 없다.

**X2 이월 근거.** Vercel Function 요청 본문 4.5MB 한도는 실재하고 지적은 타당하다. 다만 (1) 50MB는
이 PR 이전부터 `/api/omr/upload`에 있던 값이고, (2) 홈(DS-2)도 같은 "최대 50MB"를 말하며,
(3) 한도를 낮추는 것은 제품 결정이라 D-026·phase 문서 갱신과 결정 기록이 선행해야 한다.
**DS-3 범위 밖의 결정 사안으로 이월한다.** 아래 HANDOFF blocker 표에 올렸다.

**리뷰어가 확인하고 문제없다고 판정한 것**: D-026 단계 계약 준수, 502/503 비실패 처리와 폴링 지속,
서버 문자열 비노출, D-010/D-013/D-018 회귀 유지, `jest.setup.js` File 대역 제거의 숨은 영향 없음,
D-028 판단의 타당성("가장 작은 정합적 해법이며 더 나은 범위 내 대안을 찾지 못했다").

리뷰어는 `docs/recovery/validation/2026-08-29-DS-3-upload-processing.md`를 찾지 못했다고 적었는데,
그 파일은 `main`에만 있고 PR 브랜치에는 없어서다 — 실제 누락이 아니다.

**리뷰 스레드 상태**: CodeRabbit 2건 모두 resolved. 미해결 actionable review 0건.

**CodeRabbit**: 자동 리뷰는 `Review skipped: manual review required for this OSS repository`로
건너뛰었다(별 10개 미만 저장소). 2026-08-29에 `@coderabbitai review`로 수동 요청해
([코멘트](https://github.com/landfill/ClairKeys/pull/91#issuecomment-5462635605)) 위 4건을 받았다.
PR #90에서는 같은 요청이 rate limit에 걸렸었다 — 이번에는 통과했다.
대응 요약은 [이 코멘트](https://github.com/landfill/ClairKeys/pull/91#issuecomment-5462685033).

**수집 방법**: 세 표면을 모두 조회한다 — 리뷰 스레드(GraphQL `reviewThreads`), 리뷰
본문(`pulls/91/reviews[].body`의 접힌 블록), 일반 코멘트(`issues/91/comments`). PR #89에서 스레드만
보고 3건을 놓친 적이 있다.

## 스스로 확인한 것 (리뷰 전 자기 점검)

- **범위 이탈 1건을 규약대로 처리했다.** DS-3의 Out of scope가 백엔드 변경을 막는데
  `/api/omr/status/[jobId]`에 `code: 'OMR_JOB_LOST'`를 더했다. 코드보다 결정을 먼저 고쳤고
  (D-028, 커밋 `fd5d85d`), 구현은 그다음 커밋 `ded91b7`이다.
- **완료 조건 하나를 문자 그대로 따르지 않았다.** `이 페이지를 닫아도 계속 처리됩니다`를 "고정
  노출"하되, 남은 작업이 없으면 내린다. 전부 끝난 화면에서 그 문장은 고정 노출이 아니라 사실이
  아닌 문장이기 때문이다. 양쪽 다 회귀로 걸었다.
- **테스트가 무의미하게 통과하던 것을 하나 잡았다.** `useFakeTimers()`를 `render` 뒤에 켜면 이미
  진짜 타이머로 잡힌 폴링 간격이 울리지 않아, "완료 뒤에는 더 묻지 않는다"가 아무것도 검사하지
  않은 채 통과했다. 옆 테스트가 실패해서 발견했다.

## 병합

- 2026-08-29 — 사용자의 명시적 승인("merge")으로 병합했다. merge commit
  `f8c22499a0c20ea92378c213eb866aeb2a18250a`, 최종 head `6a5eeea`(커밋 8개).
- 병합 직전 재확인: `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, Draft 아님, 체크 17/17 성공,
  미해결 리뷰 스레드 0건, `CHANGES_REQUESTED` 리뷰 0건.
- 병합 후 확인: `main`이 `f8c2249`로 이동했고 `6a5eeea`가 조상으로 포함됨, merge commit
  check-runs 6/6 성공(E2E·Post-merge build·Post-merge tests·Run Tests·Lint·Security Audit).
- 브랜치 정리: 로컬 tip과 원격 tip이 모두 `6a5eeea`이고 `main`에 포함됨을 확인한 뒤 원격 →
  로컬 순으로 삭제했다. 미커밋 변경 없음.
