# PR #102 review log

Date: 2026-08-31 KST
PR: [Document complete OMR VM replacement procedure](https://github.com/landfill/ClairKeys/pull/102)
Branch: `codex/ops-vm-replacement-guide`
Commit: `3e097f0b3cbe5fd2d2425c9d65bd7d4a76c44118`

## Current state

- Review-ready PR created on 2026-08-31; it is not Draft.
- Scope is documentation only: new canonical replacement runbook plus entry links and reusable IP placeholders.
- No live VM, Vercel variable, application code, OMR runtime, systemd unit, or cloud resource was changed.
- Focused local verification passed; detailed evidence is in
  `docs/recovery/validation/2026-08-31-omr-vm-replacement-guide.md`.
- Required PR checks passed: Lint, Lint and Type Check, Run Tests, Security Audit, E2E Tests,
  Vercel, and the aggregate checks. Change-aware jobs correctly skipped runtime-only Build Check,
  Unit Tests, Security Scan, and the duplicate PR-check E2E job for this documentation-only diff.
- The direct-main status-record commit `c317a68` also completed all six post-push checks
  successfully, including E2E Tests and Post-merge build/tests.
- First inspection of all three review surfaces found no actionable feedback. CodeRabbit was
  skipped by its fewer-than-10-stars policy, so a manual review was triggered. It completed with
  four inline findings; all were classified and answered below. Commit `9c43d35` carries the fixes.
- The PR description was rewritten to the full repository template after CodeRabbit identified the
  missing Recovery phase, Key decisions, evidence table, risks/rollback, and review checklist.
- CI for `9c43d35` settled successfully: Lint, Lint and Type Check, Run Tests, Security Audit,
  E2E Tests, Vercel, and aggregate checks passed. Change-aware checks correctly skipped the
  runtime-only duplicates for this documentation diff.
- User feedback identified that a replacement VM also changes its PEM lifecycle. Commit `c7c4013`
  adds a unique new-VM NCP authentication key, admin-password retrieval, separate SSH public-key
  enrollment, lost-PEM recovery, and assignment-aware old-key retirement. Its Lint, Lint and Type
  Check, Run Tests, Security Audit, E2E Tests, Vercel, and aggregate checks all passed.
- User then corrected the provider boundary: operators never create NAVER Cloud resources; 모두의AI
  assigns an OS-only VM, address, administrator user, and new SSH PEM. Commit `0dad647` removes the
  NCP console/password-decryption-key path and adds allocation intake, direct PEM SSH, host-key
  verification, provider network requests, and old allocation/PEM return. Latest-head CI is pending.
- User explicitly directed that this documentation PR must not receive another CodeRabbit review
  request. CodeRabbit remains auto-skipped on `0dad647`; no manual review will be triggered.
- `0dad647` completed Lint, Lint and Type Check, Run Tests, Security Audit, E2E Tests, Vercel, and
  aggregate checks successfully. The PR is review-ready, `MERGEABLE`/`CLEAN`, and awaits explicit
  user merge approval.
- Merge requires explicit user approval even after CI and reviews are clean.

## Review items

| ID | Severity | Finding | Decision | Status | Evidence |
|---|---|---|---|---|---|
| R1 | Minor | `docs/deployment.md` diagram fence lacks a language tag | ACCEPTED | FIXED | `text` fence in `9c43d35`; same consistency fix in service README |
| R2 | Major | Remove public 3000/HTTP and require HTTPS for cutover | PARTIAL / REJECTED AS WRITTEN | FIXED | Explicit isolated-test safety gate and real-user TLS requirement added. Removing current test HTTP would silently reverse accepted D-012, so that portion is rejected pending a replacement user decision |
| R3 | Major | URL/secret swap can strand in-flight old/new VM callbacks | ACCEPTED | FIXED | Cutover and rollback now stop uploads, enumerate accepted job IDs, wait for conversion/delivery, and verify callback 2xx + Supabase persistence before switching |
| R4 | Minor | Diagram uses bind wildcard as if it were the public destination | ACCEPTED | FIXED | Diagram now uses `<VM_PUBLIC_IP>:3000` in `9c43d35` |
| R5 | Review body | PR description omitted required template sections | ACCEPTED | FIXED | PR body now contains all template sections and validation evidence |

All four inline threads are resolved after replies that state acceptance or rejection rationale.
CodeRabbit subsequently marked each finding "Confirmed as addressed by @landfill". Final inspection
of inline comments, review bodies, and general comments found no new actionable item through
`c7c4013`; unresolved review threads remain 0. Per user direction, `0dad647` used normal CI and
direct document inspection only, with no new CodeRabbit request.
