# PR Review Log — PR #133

PR URL: https://github.com/landfill/ClairKeys/pull/133
Branch: `codex/issue-130-metric-validation` (head `ef728d1`)
Issue: [#130](https://github.com/landfill/ClairKeys/issues/130) 3단계 재정의
Last checked: 2026-09-05 KST — OPEN, 병합 승인 대기

## 목적

3단계를 두 번 시도해 둘 다 실패한 뒤, 원인이 기전이 아니라 **목표 지표**임을 밝히고 검증된 지표로
교체한다. 운영 코드는 바꾸지 않는다.

## 버려진 두 번의 시도 — 기록

| 시도 | 기전 | 결과 |
|---|---|---|
| Codex 1차 (`4d1d052`) | 방향 쪽 끝 손가락 착지에 `DIRECTIONAL_BUDGET=10` | 재배치 65→61, **최장 run 3→4 회귀** |
| Codex 2차 (`7a3d5e8`) | 계측 강화 후 재시도 | 다섯 축을 모두 만족하는 모델 없음. 정직하게 blocked 보고 |
| 코디네이터 시도 | reposition landing 규칙 | 재배치 61→**64**로 악화. 되돌림 |

세 시도 모두 같은 지표를 낮추려 했고, 그 지표가 틀렸다는 것이 이 PR의 발견이다. **버린 브랜치
`codex/issue-130-directional-budget`은 병합하지 않는다.**

## 결정적 증거

3마디 왼손(26반음 상행)에 여러 운지를 넣고 `repositionsInMonotoneRuns`로 재면 결함 운지가 2점,
정석 운지가 전부 3점이다. 손 벌림보다 넓은 진행은 손이 반드시 이동하고 총 이동량은 음정 구간이 정하므로
(네 운지 모두 21), 지표는 그 총량이 몇 번에 나뉘는지만 세고 있었다.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| (PR 생성 직후) | — | 아래 Iteration 1에 기록 |

로컬 검증 전부 통과: Jest 896건(신규 6건), `tsc --noEmit`, `next lint`, `next build`.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R1 | self | 3단계 목표 | 지표가 정석 운지를 결함보다 나쁘게 매긴다 | 목표에서 실격. 삭제하지 않고 역전을 테스트로 고정 | CLOSED | D-044 결정 2 |
| R2 | self | `wastedHandTravel` 1차안 | 생 손 이동량은 악보 자신의 도약을 잡는다 | 하한(`|음정| - 손 너비`)을 뺀 초과분으로 변경 | FIXED | `a832731` |
| R3 | self | `wastedHandTravel` 2차안 | 엄지 넘김을 낭비로 집계 → 정석이 결함보다 나쁘게 나옴 | 넘김 제외. 판정은 해부학이므로 `handReach.ts`에 | FIXED | `09dc39d` |
| R4 | self | 넘김 제외 범위 | 제외가 지표를 무력화하지 않는가 | 277개 전이 중 19개만 면제, 최대도 `E3:1→A3:3`(음정 5, 이동 8)인 tuck | CLOSED | 직접 측정 |
| R5 | self | 반복 run 지표 | 개수만 세어 v4의 3→4 후퇴를 통과시켰다 | 길이와 음 변화 축 추가 | FIXED | `a832731`, `67fc548` |
| R6 | Gemini 워커 검증 | 위임 결과 전반 | 약한 모델이므로 재현 검증 필요 | 6개 검증 명령 재실행, 테스트 파일·상수·금지 파일 무결성 확인 — 전부 통과 | CLOSED | 아래 Iteration 1 |

## Iteration log

### Iteration 1 — 생성 (2026-09-05)

- 위임: 지표 수정 부분을 Antigravity CLI의 **Gemini 3.8 Flash**(`agy --model gemini-3.8-flash-high`)
  워커에 맡겼다. `--agent gemini`는 launch-time 모델 선택을 지원하지 않으므로
  `terminal create --command` + `worker-start --terminal` 2단계 경로를 썼다(supervised 유지).
- 사용자가 모델 능력이 낮다고 알려와, **설계를 코디네이터가 확정하고 함수 본문까지 스펙에 적어** 타이핑만
  넘겼다. 테스트 파일 수정 금지, 상수 변경 금지, `fingeringUtils.ts` 금지를 명시했다.
- **워커 결과를 전부 재현 검증했다.** 보고(하네스 6/6, Jest 94 suites/896, 낭비 이동 59→43)가 정확했고,
  붙여넣은 `isThumbCrossing` 본문이 스펙과 문자 단위로 일치했다. 금지 파일 무변경,
  `WASTED_TRAVEL_TOLERANCE` 불변, 금지 trailer 없음도 확인했다.
- Changes made: `a832731` 검증 하네스 + 새 지표, `09dc39d` 넘김 제외(Gemini), `67fc548` 래칫 교체와
  D-044, 단계 문서·이슈 완료 조건 수정
- 새 기준선: 낭비 손 이동 **43/167**, 같은 손가락 도약 **13/167**, 최장 run 3, 음 변화 run 1,
  도달 불가 0/154
- Validation: Jest 896건, `tsc --noEmit` exit 0, lint 경고 0, build 성공
- Remaining actionable items: 없음. 사용자의 명시적 병합 승인 대기.

## 병합 후 남는 것

- 4단계: 새 지표를 목표로 한 모델 변경. 핵심 사례는 `B2:1 → G#3:2`(11반음 이동, 필요한 것은 2반음).
- 5단계: 도약 예외 재정의.
- 검증 세트가 4개 악구뿐이다. 대위·옥타브 연속·빠른 패시지는 미포함.
- 이 PR은 이슈 #130을 닫지 않는다.
