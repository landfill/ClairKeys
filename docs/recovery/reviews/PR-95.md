# PR Review Log — PR #95

PR URL: https://github.com/landfill/ClairKeys/pull/95
Branch: `codex/ds-wcag-requirement-removal`
Last updated: 2026-08-30 KST (생성 직후)

D-030 / 이슈 #76 완료 조건 개정. Head `6be2702`. 계획 문서 + CI + 구현 계약 변경.

## Scope

사용자 지시(2026-08-30)로 이슈 #76의 WCAG AA 요건을 문서·CI·구현 계약 세 층에서 모두 제거한다.
요건은 계획에 있으므로 결정과 계획 문서를 먼저 고치고(`4c27ca4`), 그 요건만을 근거로 서 있던
게이트와 계약을 뒤이어 걷어냈다(`6be2702`).

지시 시점에 스크린리더·키보드 사용자에게 실제 기능 저하가 발생한다는 점을 사용자와 함께 확인했고,
사용자가 그 범위를 선택했다.

## 변경 요약

| 층 | 변경 |
|---|---|
| 계획 | 완료 조건 7 삭제, 조건 8 → 조건 7 재번호 (8개 → **7개**). `ROADMAP.md`의 "조건 7의 판정 기준" 절 제거. DS-1~DS-7의 "접근성·반응형 검증" → "반응형 검증" |
| CI | `accessibility-check` job(홈 1화면 `@axe-core/cli@4.12.1`) 제거. `prChecksWorkflow.test.ts`의 검사 3건 제거 |
| 구현 | `PlaybackControls` ARIA 시맨틱·`aria-label`·live region, `icons.tsx` `role`/`aria-hidden`, `OptimizedImage` 로딩 `aria-label` 제거 |
| 테스트 | `playbackControlsA11y.test.tsx`(166줄) 삭제, `globalStyles.test.ts` 포커스 계약 가드 제거 |

CI job과 `prChecksWorkflow.test.ts`는 한 묶음이다. 그 테스트는 워크플로 YAML을 문자열로 읽어
`split(/^  accessibility-check:$/m)[1]`로 job 블록을 잘라내므로, job만 지우면 `undefined`가 되어
런타임 에러로 터진다.

## 의도적으로 남긴 것

| 남긴 것 | 이유 |
|---|---|
| `PlaybackControls`의 `tabIndex`·`onKeyDown` | 요건이 아니라 동작하는 기능이다 |
| `OptimizedImage`의 `alt` | `next/image` 필수 prop — 지우면 타입이 깨진다 |
| `globalStyles.test.ts`의 토큰 describe | hex 리터럴 유출·D-025 다크 팔레트 재유입 방지로 접근성과 무관 |
| DS-5의 실기기 회귀(276·326·390px) | D-021·D-022가 남긴 회귀 계약 |
| DS-0의 "접근성·회귀 테스트" 절 | 기하·전환 계약을 함께 고정한다 |
| 나머지 29개 파일의 `aria`/`role` | RTL `getByRole`·`getByLabelText`가 의존 — 요건과 무관한 테스트를 대량으로 깨뜨린다 |
| 과거 Progress·validation 기록의 조건 번호 | 그 시점의 사실이자 역사적 증거다 |

## 로컬 검증

| 명령 | 결과 |
|---|---|
| `npx tsc --noEmit` | 통과 (exit 0) |
| `npm run lint` | 경고·오류 0건 |
| `npx jest --ci` | **75 suite / 745 test 전부 통과** |
| js-yaml 워크플로 파싱 | job 8개 — `changes`, `lint-and-type-check`, `test-unit`, `test-e2e`, `security-scan`, `build-check`, `pr-summary`, `all-checks` |
| `grep -niE 'accessib\|axe\|browser-driver' .github/workflows/*.yml` | 0건 |
| `grep -rn 'WCAG' docs/recovery/ROADMAP.md docs/recovery/phases/` | 0건 |

## required check 영향 확인

`gh api repos/landfill/ClairKeys/branches/main/protection`의 `required_status_checks.contexts`는
`["Lint","Security Audit","Run Tests","E2E Tests"]`다. **"Accessibility Check"는 포함되지 않는다.**
`pr-summary`·`all-checks`의 `needs`에도 `accessibility-check`가 없어 참조가 끊기지 않는다.
따라서 job 제거가 PR 머지를 막지 않는다.

PR 생성 후 체크 목록에서 "Accessibility Check"가 사라진 것을 확인했다.

## 상태

- `isDraft: false` (review-ready), `mergeable: MERGEABLE`, `mergeStateStatus: BLOCKED` (체크 진행 중)
- CI 결과와 리뷰 세 표면(스레드 · `pulls/95/reviews` 본문 · `issues/95/comments`) 조회는 미기록 —
  다음 갱신에서 채운다.
- 병합은 사용자의 명시적 승인 전까지 하지 않는다.
