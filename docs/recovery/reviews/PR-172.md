# PR172 — 시스템 경계 붙임줄 다섯 개 (D-073)

- Date: 2026-09-20
- PR: https://github.com/landfill/ClairKeys/pull/172 (non-draft)
- Head: `b8de0a836a45856cb8597211670e870ed332341c` (main 상태 기록 동기화 후 실행코드는 `a8d6b28`와 동일)
- 상태: 실제 Codex 리뷰 완료·지적0. 타입/lint/unit 및 E2E2개를 포함한 나머지 CI는 통과했다. Security Audit만 npm 유지보수로 재시도2회 실패했다. **완료/병합 준비 통과로 표시하지 않는다.**
- 사용자 승인 범위는 로컬 구현·검증·PR/CI/리뷰 대응이다. main 병합 및 운영 VM 접근·배포는 제외한다.

## 범위와 검증

- 같은 음·보표·휨의 서로 유일한 반쪽을 일반 slur보다 먼저 짝짓는다. 오선/작은 호 복원은 내부 페이지 경계의 matched-tie 전용 후보다.
- Clair191/191 유지, 타이37→42/43·오검출0 최종3회 동일. raw차이는 목표5개 start/stop10필드뿐이다.
- 전체 이미지196개 중190통과·6보관진단skip, native19개 모두 실행. 타입/lint/Jest105suites1038tests 통과.
- 기준선/후보12곡:10동일, 모노노케 인쇄D♭5 타이1개 복구, truongca 동일SCALE실패126줄.
- 최종 d073d 정식 재빌드는 실행 검증 d073c와 엔진/앱/테스트/의존성이 같고 입력patch context공백만 다르다(40955항목 비교).
- m12 교차1개와 m3 C5 성부2→1의 canonical 병합은 후속 범위다. 새로운 반쪽의 페이지 간 복원은 하지 않는다.
- [검증·원본 대조·자체 리뷰·한계](../validation/2026-09-20-issue-134-cross-system-ties.md).

## CI와 실제 리뷰

- 최초조회 head `b8de0a8`: Lint and Type Check·Lint·Detect changes·Vercel 성공, Unit/Run Tests와 E2E 진행 중.
- [Security Audit](https://github.com/landfill/ClairKeys/actions/runs/35457848798/job/105936203747)는 quick audit endpoint HTTP400으로 실패했다.
  같은 오류가 변경 없는 main과 로컬에서도 발생했다. 공개패키지 하나만 bulk API에 요청해도503과
  `We are currently performing maintenance` 응답을 받아 lockfile과 무관한 외부 서비스 문제를 확인했다.
  검사 기준 변경/강제 성공 처리는 하지 않는다. 복구 뒤 같은 job을 재실행한다.
- CodeRabbit는 자동리뷰 조건으로 skip했다. 실제 리뷰로 세지 않는다.
- Codex review summary는 `b8de0a8`, `PR opened`, Running(2026-09-19T17:21:47Z)을 명시한다.
  inline comment0·formal review0은 이 조회 시점의 수치이며 리뷰 완료를 뜻하지 않는다.

## Findings

현재 head의 Codex 실제 리뷰는 완료됐으며 외부 actionable finding은 없다. 아래는 자체 리뷰에서 보강한 항목이다.

| Finding | 상태 | 근거 |
|---|---|---|
| 자체 점검: 새 rescue가 기존 orphan 매칭/삭제 대상인지 보장 | FIXED | `13431f0`: 내부 페이지 경계와 기존 orphan predicates 요구. 부정 fixture에서 실제 유출은 재현되지 않았음을 구별해 기록 |

## 최신 head 결과 (2026-09-20)

- Head `b8de0a836a45856cb8597211670e870ed332341c`, non-draft. 실행코드 변경 없이 유지 중이다.
- [Codex 실제 리뷰](https://github.com/landfill/ClairKeys/pull/172#issuecomment-5743871464): head를 명시하고17:24:58UTC Completed.
  bot +1(17:25:02UTC), inline0·reviewThreads0·formal review0 확인. CodeRabbit skip은 리뷰로 세지 않는다.
- 나머지 CI: Unit Tests·Run Tests(hosted Jest105suites1038tests), Lint·Lint and Type Check, E2E2개(7m48s/6m52s),
  All Checks Complete·PR Summary·Vercel 성공. Build Check/Security Scan은 기존 path조건 skip이다.
- Security Audit: [attempt1](https://github.com/landfill/ClairKeys/actions/runs/35457848798/job/105936203747) quick400.
  workflow 종료 후 `gh run rerun35457848798 --failed`가 수락돼 attempt2를 실행했고,
  [attempt2](https://github.com/landfill/ClairKeys/actions/runs/35457848798/job/105937786498)도 같은 quick400으로 실패했다.
  공개패키지 하나의 bulk 요청503/maintenance와 로컬 동일 audit 오류가 별도로 확인됐다. 검사 완화 없음.
- 상태 기록 main 커밋600ae80/621b576/da53a24/e7a4d24의 보안 감사 실패도 동일 서비스 대기 범위로 기록한다.
  아직 통과로 바꾸지 않았으며, 현재 작업 goal은 외부 검사 때문에 미완료다.

## 다음 행동

1. npm bulk API의 실제 복구를 확인한다. 공지된 점검 종료는19:00UTC(2026-09-20 04:00KST) 예정이다.
2. PR head가 그대로인지 확인하고 `gh run rerun35457848798 --failed`로 동일 Security Audit을 재실행해 실제 통과를 확인한다.
3. 새 head/코드 변경이 없다면 이미 완료한 로컬 검증을 반복할 필요가 없다. 새 리뷰/CI 결과만 저장소에 회수한다.
4. Security Audit 실패가 남아 있으면 goal 완료로 표시하지 않는다. main 병합 및 운영 VM 작업은 수행하지 않는다.
