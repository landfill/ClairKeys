# PR #107 review log

Date: 2026-09-02 KST
PR: [finalize 콜백이 자기 방어와 행 상태에 대해 사실만 말하게 한다](https://github.com/landfill/ClairKeys/pull/107)
Branch: `codex/issue-72-finalize-route`
Head: `bac8cdd5b2f1602bdb2a0845cb322d086fa87dc5`
Last checked: 2026-09-02 09:50 KST

## Current state

- Review-ready PR created on 2026-09-02 for issue #72 (PR #68 review items R8·R11).
- Two Lore commits separate regression evidence (`e77e4a5`, observed 1 failed / 6 passed) from the
  fix (`bac8cdd`).
- Local verification passed: `npx jest src/app/api/omr` 4 suites / 33 tests, `npm test -- --runInBand`
  85 suites / 778 tests, `npx tsc --noEmit` exit 0, `npm run lint` clean, `npm run build`, `git diff --check`.
  Record: `docs/recovery/validation/2026-09-02-issue-72-finalize-route.md`.
- Hosted checks at `bac8cdd`: Lint, Lint and Type Check, Run Tests, Unit Tests, Build Check, E2E Tests (one
  job; the second was still pending), Vercel all **pass**. **Security Audit and Security Scan fail.**
- The two Security failures are **not caused by this PR** — classified as `EXTERNAL`, see R-CI-1 below.
  Unblocked by PR #108 (lockfile-only browserslist bump). After #108 merges, merge `main` into this branch
  and let the checks re-run.
- CodeRabbit automatic review skipped (fewer-than-10-stars policy). Manual `@coderabbitai review` requested
  on 2026-09-02 09:50 KST; result pending.
- Merge requires the user's explicit approval after CI and actionable review are clean.

## Design boundary held

- D-018 Decision 3·Directive name the stored `omrJobId` as the `/result` fetch target. The DB round-trip and
  the nullable-column 409 branch were therefore **kept** and relabelled as type narrowing, not removed.
  Removing them would be a decision change and would also drop the taint boundary that closed the CodeQL
  `js/request-forgery` alert in PR #68 Iteration 2.
- The R11 status repair is conditional (`processingStatus !== 'completed'`) so the existing idempotency test
  (row already completed → no `update`) is unchanged.

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Detect changes | pass | run 33575504844 |
| Lint | pass | run 33575504789 |
| Lint and Type Check | pass | run 33575504844 |
| Run Tests | pass | run 33575504789 |
| Unit Tests | pass | run 33575504844 |
| Build Check | pass | run 33575504844 |
| E2E Tests (×2) | pass / pending | run 33575504844 |
| Security Audit | **fail** | run 33575504789 — R-CI-1 |
| Security Scan | **fail** | run 33575504844 — R-CI-1 |
| Vercel | pass | preview deployed |
| CodeRabbit | skipped → manual requested | fewer-than-10-stars policy |

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| R-CI-1 | CI | `package-lock.json` (untouched by this PR) | `npm audit --audit-level high` fails on GHSA-c83g-rgw3-j3cx (`browserslist <=4.28.6`, high), published 2026-09-01T16:42Z. `main` last passed at 06:12Z the same day; the same check also fails on `main` handoff commit `6ed651b`. Reproduced locally with `npm audit --audit-level high`. | EXTERNAL — fix in a separate PR | TRACKED | PR [#108](https://github.com/landfill/ClairKeys/pull/108) |

## Iteration log

### Iteration 1 — initial (2026-09-02)

- Feedback fetched: 없음 (PR 생성 직후)
- Changes made: 회귀 `e77e4a5`, 수정 `bac8cdd`
- Validation: focused Jest 33, full Jest 778, tsc, lint, build, diff check 모두 통과
- Commit: `bac8cdd`
- Remaining actionable items: hosted CI·CodeQL·CodeRabbit 결과 대기

### Iteration 2 — Security 게이트 실패 분류 (2026-09-02 09:50 KST)

- Feedback fetched: Security Audit·Security Scan fail. 나머지 hosted checks pass. 인라인 리뷰 코멘트 0건.
- Classification: R-CI-1 `EXTERNAL`. 이 PR은 lockfile을 건드리지 않았고, 같은 체크가 `main`의 `6ed651b`에서도
  실패하며, advisory 공개 시각이 `main`의 마지막 성공 이후다.
- Changes made: 이 브랜치에는 없음. 별도 PR #108(lockfile-only)로 해소. 한 PR 하나의 목적 규약에 따라 여기
  섞지 않았다.
- Validation: `npm audit --audit-level high` 로컬 재현 — `browserslist <=4.28.6` high 1건.
- Commit: 없음
- Remaining actionable items: #108 병합 → 이 브랜치에 `main` merge → Security 재실행 확인; CodeRabbit 수동 리뷰
  결과 대기
