# PR167 — 공유 기둥의 긴 성부 화음 분리 (D-068)

- Date: 2026-09-19
- PR: https://github.com/landfill/ClairKeys/pull/167
- Related: #134, D-067, D-068
- Head: `cacce3a0c670ae439e07acca1f4b01287d1aa4f1` (cross-staff guard 수정·push)
- 생성 시 non-draft 확인. CI·자동 리뷰 확인 중. 병합 및 운영 배포 미승인.

## 범위와 근거

- m5의 F4 점2분·점4분, m7의 C4 점4분이 8분으로 나오던 세 건을 엔진 내부 화음 분리로 해결한다.
- head-stem 연결을 보존하고 긴 화음에 빔 소유 예외를 저장한다. 빔 쪽 조회와 동일 시점의 타이 끝점 검사도 일관되게 처리한다.
- [검증·실패 실험·자체 리뷰](../validation/2026-09-19-issue-134-shared-stem-chord-split.md).
- 상태·검증·리뷰 기록은 AGENTS/WORKFLOW에 따라 main에 직접 커밋하며 코드 브랜치에 복사하지 않는다. PR 본문은 main의 canonical 기록을 연결한다.

## 로컬 검증

- Clair 182→185/191, 타이30/43·tempo69·canonical160 유지; 3회 raw 해시 동일, 마디5·7만 변화.
- native 양성12사례 기준선 실패/패치 통과. OMR 저장·재로딩, 일반 점 없는2도 제어 포함.
- 정식 Dockerfile 이미지 전체186개 중180통과·6skip(보관 로컬 진단 자료 미포함); native9개 모두 실제 실행.
- corpus12곡 양쪽 새 실행: 성공11곡 raw 바이트 동일·animation 생성시각 외 전 필드 동일; truongca는 같은3쪽 SCALE 실패.
- 타입 검사·lint·105 Jest suites/1038 tests 통과. 두 엔진 클래스 및 이미지 소스 해시 대조 완료.
- 한계: singleton·standard·single-staff·non-mirror·beamed 형태에 제한. 점 하나가 누락된 일반 점음표 화음과의 모호성은 D-068에 기록했다.

## 자체 리뷰

- 최종 diff 전체 확인. 구조·빔 귀속·성부/onset·타이·재로딩·빌드 반영을 점검했다.
- 실험 중 발견한 머리 삭제·타이 강등·테스트 export 경로·참조 마운트·patch 공백 문제는 수정 및 재검증했다.
- 미해결 구현 finding 없음. 상세는 위 validation의 자체 리뷰 절을 따른다.

## Hosted CI / 자동 리뷰

- 생성 직후 current head를 조회했고 non-draft·head 일치를 확인했다.
- 필수 CI와 자동 리뷰는 진행 중. 성공으로 간주하지 않는다.
- 아직 수신한 actionable feedback 없음. 새 finding은 OPEN/ACCEPTED/FIXED/REJECTED/SUPERSEDED로 기록하고 대응한다.

## 자동 리뷰 findings (head 63487c0)

| Finding | 판단 / 상태 | 대응 |
|---|---|---|
| [4051423235](https://github.com/landfill/ClairKeys/pull/167#discussion_r4051423235), P1: 한 화음은 단일 보표여도 빔 그룹이 다른 보표까지 이어질 수 있음 | ACCEPTED; `cacce3a` 수정, 전체 재검증 중 | 실제 splitter를 호출하는 native 그래프 fixture가 기존 이미지에서 `cross-staff group was split`로 실패. 빔 그룹 전체 staff 검사 추가 후 normal/recovery 통과. 같은 보표 제어는 분리에 진입함도 확인 |
| [4051423244](https://github.com/landfill/ClairKeys/pull/167#discussion_r4051423244), P1: reviewed commit에 Lore trailer 없음 | REJECTED | GitHub commits API 및 git 원문에서 `63487c0`의 Confidence/Scope-risk/Tested/Not-tested/Directive/Related를 확인. 앞 두 구현 커밋도 필수 trailer가 있다. [원문 근거 답변](https://github.com/landfill/ClairKeys/pull/167#discussion_r4051450818). 실제 history rewrite 불필요 |

- Codex 첫 리뷰는 63487c0에서 완료됐다. 수정 head에는 별도 CI·리뷰 확인이 필요하다.
- CodeRabbit은 별 수 정책 때문에 리뷰를 건너뛰었고, SUCCESS status를 코드 리뷰 통과로 세지 않는다.
- native 그래프 fixture는 새 PDF 인식 성공을 주장하는 raster fixture가 아니다. 이미 인식된 단일보표 화음들이 cross-staff 그룹에 속한 경우
  실제 엔진 splitter가 mutation 전에 abstain하는지를 검사한다. 기존 raster 양성 fixture는 별도로 유지한다.

## 수정 head 확인

- `cacce3a` push 후 GitHub head 일치·non-draft를 확인했다. [유효 지적 답변](https://github.com/landfill/ClairKeys/pull/167#discussion_r4051466645).
- Clair3회는 직전 최종 raw와 바이트 동일(185/191, 타이30/43). 관련 Jest runtime contract17개 통과.
- [현재 head 재리뷰 요청](https://github.com/landfill/ClairKeys/pull/167#issuecomment-5737512341). 전체 이미지 suite와12곡 패치 재실행은 진행 중.
- Lore REJECTED thread는 원문 근거 답변 후 resolved 처리했다. cross-staff thread는 전체 재검증 완료 전까지 열어 둔다.

## 최신 이미지·재리뷰 결과

- `cacce3a`에 대한 [Codex 재리뷰](https://github.com/landfill/ClairKeys/pull/167#issuecomment-5737533847)는 완료됐고 새 findings는0건이다.
  GitHub summary도 같은 head의 Completed를 표시한다. CodeRabbit skip과 구별한다.
- `d068b-patched` 전체 이미지 suite는 **187개 중181통과·6skip**(231.804초), native10개 모두 실제 실행했다.
- 최신 corpus 재실행과 현재 head E2E가 남았다. cross-staff thread는 재검증을 마칠 때 resolved 처리한다.

## 승인 경계

- PR 병합 승인은 아직 없다. 완료된 CI·자동 리뷰만으로 병합하지 않는다.
- 운영은 PR166이며 이 PR을 배포하지 않았다. 운영 배포는 별도 승인 대상이다.
