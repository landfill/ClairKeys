# PR Review Log — PR #90

PR URL: https://github.com/landfill/ClairKeys/pull/90
Branch: `codex/ds-2-prelogin-value`
Last checked: 2026-08-29 KST

## CI status

| Check | Status | Last evidence |
|---|---|---|
| Run Tests / Unit Tests | pass | `b0167ef` |
| E2E Tests (×2) | pass | `b0167ef` |
| Lint / Lint and Type Check | pass | `b0167ef` |
| Build Check | pass | `b0167ef` |
| **Accessibility Check** | **pass** | `b0167ef` — `5575a15`에서 **FAILURE**였다 (아래 A1) |
| CodeQL / Security Scan / Security Audit | pass | `b0167ef` |
| Vercel | pass | Preview 배포 완료 |
| CodeRabbit | pass | **리뷰는 돌지 않았다** — 아래 참조 |

**CodeRabbit 리뷰 없음**: 이 저장소는 별 10개 미만이라 자동 리뷰 대상이 아니고, 수동
`@coderabbitai review` 요청은 rate limit(무료 OSS 리뷰 소진, PR #89가 사용)에 걸렸다. 따라서 이 PR에는
**사람·봇 리뷰 피드백이 하나도 없다.** 리셋 후 재요청했다.

## Review items

| ID | Source | File/line | Summary | Decision | Status | Commit/evidence |
|---|---|---|---|---|---|---|
| A1 | CI Accessibility Check | 홈 (axe) | `color-contrast` 5건 + `select-name` 2건. 전부 인증 뒤에 있던 플레이어를 공개 홈에 올리면서 **드러난** 기존 결함 | **accept** | DONE | `b0167ef` |
| C1 | Codex 워커 (blocker) | `e2e/application-smoke.spec.ts`, `src/app/page.tsx` | 1440×900 최초 뷰포트에 낙하 노트·3단계가 없는데 E2E가 통과한다 (`toBeVisible`/`toBeAttached`는 화면 밖에도 통과) | **accept** | DONE | `2ba8c3c` |
| C2 | Codex 워커 (major) | `src/lib/returnPath.ts` | URL 파서가 제거하는 탭·LF·CR로 오픈 리다이렉트 우회 | **accept** | DONE | `1f0ee47` |
| C3 | Codex 워커 (major) | `src/app/page.tsx`, `auth/signin` | "나에게만 보입니다"가 DS0-1(비공개 악보 public 버킷)과 충돌 | **accept** | DONE | `2ba8c3c` |
| C4 | Codex 워커 (major) | `PlaybackControls.tsx` | seek 바가 `onClick` div — 키보드 불가(WCAG 2.1.1). 재생 상태 live region 없음 | **accept** | DONE | `f9b7b73` |
| C5 | Codex 워커 (minor) | `homeSample.ts` | `tempoSource: 'user'`가 "직접 입력"으로 표시돼 오해 소지 | 미처리 | OPEN | — |
| C6 | Codex 워커 (minor) | `page.test.tsx` | 스크롤 가드가 매칭 0개라 vacuous | 미처리 | OPEN | — |

**수집 방법**: PR #89에서 리뷰 스레드만 보고 3건을 놓쳤다. 세 표면을 모두 조회한다 —
리뷰 스레드(GraphQL `reviewThreads`), 리뷰 본문(`pulls/N/reviews[].body`의 접힌 블록),
일반 코멘트(`issues/N/comments`).

## 병합

- 2026-08-29 — 사용자의 명시적 승인으로 merge commit `a789deadac9d51a83b537e42925ac598c7a8eabf`로
  병합했다. 최종 head `6d24dc8`(커밋 8개), 미해결 리뷰 스레드 0건, merge commit check-runs 6/6 성공,
  Vercel Production `a789dea` 배포. 로컬·원격 작업 브랜치 tip이 main에 포함됨을 확인한 뒤 삭제했다.

  **병합 승인 이후 CodeRabbit이 뒤늦게 5건을 남겨 그대로 병합하지 않고 먼저 처리했다.**
  2건은 이미 처리된 것(outdated), 2건 수용(`6d24dc8`), 1건 기각. 전부 답변·resolve했다.

  | 지적 | 판정 | 근거 |
  |---|---|---|
  | E2E에 `toBeInViewport()` | 이미 다르게 처리 | `boundingBox` 실측 — 실패 메시지에 좌표가 남는다 |
  | `role="status"` 부재 | 이미 처리 | `f9b7b73` |
  | `LoginButtonProps` export | 수용 | AGENTS.md 규약 |
  | select id 고정 문자열 충돌 | 수용 | `AnimationPlayer`·`AdvancedPlaybackControls`가 각각 렌더 → 실제 조합. `useId()` + 다중 인스턴스 회귀 |
  | `flex-shrink-0`가 v4에서 제거됨 | **기각** | 빌드 산출물에 `.flex-shrink-0,.shrink-0{flex-shrink:0}` 별칭 존재. 전제가 사실이 아니다 |

## Iteration log

### Iteration 1

- Feedback fetched: 없음 (PR 생성 직후)
- Changes made: 커밋 3개
  - `a6a948c` 전송 버튼 `aria-label` + **D-027**(D-019 결정 8을 접근성 속성에 한해 좁힘)
  - `d5a43e0` 복귀 계약 — `src/lib/returnPath.ts`, `LoginButton` 기본값 제거, 렌더 중
    `router.push` 제거, `AuthGuard` 토큰화
  - `5575a15` 홈·로그인 화면 재작성, 저장소 내 샘플(`src/fixtures/homeSample.ts`)
- Validation: Jest **67 suites / 643 tests** 통과, lint 무경고, `tsc` 통과, `npm run build` 성공,
  E2E chromium 5/5. 신규 회귀 23건(샘플 계약 7, 복귀 경로 8, 홈 계약 8)
- Commit: `5575a15` (head)
- Remaining actionable items: 없음

### Iteration 2

- Feedback fetched: **CI Accessibility Check FAILURE** (A1). axe가 홈에서 7건을 잡았다.
  DS-1에서 통과하던 검사가 실패한 이유는 이 PR이 결함을 만들어서가 아니라, 플레이어가
  `/sheet/[id]`(인증 뒤)에만 있어 axe의 검사 범위 밖이었기 때문이다 — 화면을 공개 홈으로 옮긴 것이
  접근성 감사 역할을 했다
- Changes made: `b0167ef`
  - `select-name` 2건 — 속도·모드 `<select>` 옆에 `<label>`이 있었지만 `htmlFor` 연결이 없어 이름이
    없었다. `id`/`htmlFor` 연결
  - `color-contrast` 5건 — `text-gray-500`·`text-gray-600`·`text-slate-500`이 DS-1 토큰으로 넘어오지
    않은 잔재. `--ck-ink-muted`로 이동
  - 접근 가능한 이름 회귀 4건 추가 (다시 잃으면 CI보다 유닛 테스트가 먼저 잡는다)
  - 전부 속성·색 토큰만. 버튼 배치·개수·크기는 그대로라 **D-027 범위 안**이다
- Validation: 로컬 axe는 ChromeDriver가 설치된 Chrome과 버전이 맞지 않아 실행하지 못했다. 대신 위반
  두 종류를 각각 결정적으로 확인했다 — 이름은 회귀 테스트로, 대비는 실제 배경색과의 산술로
  (흰건반 6.69, 눌린 건반 4.71, 카드 6.69, 페이지 6.21). **CI가 최종 판정했고 통과했다.**
  Jest 68 suites / 647 tests, lint 무경고, `tsc`, build 성공
- Commit: `b0167ef`
- 작업 기록: 이 수정을 브랜치가 아니라 `main`에서 시작했다. `git stash`로 옮겼고 `main`에는 커밋되지
  않았다
- Remaining actionable items: 사용자의 명시적 병합 승인 대기
