# DS-7 검증 기록

Date: 2026-08-30 KST  
PR: #99  
Code commit: `0dd0b4e4c4ed6d7f1893cb5efad666636a72287d`

## 자동 검증

| 검사 | 결과 | 근거 |
|---|---|---|
| `npm run lint` | PASS | 경고·오류 없음 |
| `npx tsc --noEmit` | PASS | 타입 오류 없음 |
| `npm test -- --runInBand` | PASS | 80 suites, 755 tests |
| `npm run build` | PASS | Next production build 성공 |
| `npm run test:e2e` (필수 env 주입) | PASS | 5개 프로젝트, 25 tests |
| 보호 파일 diff | PASS | `git diff origin/main -- src/utils/playbackGeometry.ts src/utils/pianoLayout.ts src/hooks/usePlaybackOrientation.ts` 출력 없음 |

E2E 실행 환경:

```text
DATABASE_URL=postgresql://test:test@localhost:5432/clairkeys_test
NEXTAUTH_SECRET=test-secret
NEXTAUTH_URL=http://localhost:3000
```

## 이슈 #76 전체 완료 조건 판정

| 조건 | 판정 방법 | 판정 결과 | 근거(파일·검사·커밋) |
|---|---|---|---|
| 1. 홈 최초 뷰포트에 결과 영역·3단계·주 CTA | 관측 가능 | 충족 | `e2e/application-smoke.spec.ts` 5개 프로젝트 PASS; 홈 `page.tsx`의 3단계·CTA, `HomeSamplePlayer.tsx`의 결과 영역; DS-5 `d970fae`와 DS-2 완료 근거 |
| 2. 주요 CTA가 `내 악보로 시작하기` | 문자열 검사 | 충족 | `src/app/page.tsx`; 전체 Jest 및 E2E PASS |
| 3. 로그인 없이 실제 학습 결과 1회 재생 | 시크릿 창 수동 확인 | 사용자 확인 대기 | 사용자가 시크릿 창에서 탐색 → 상세 → 미리보기 → 재생을 실행하고 실제 소리·낙하 노트를 확인해야 함. E2E는 보조 근거일 뿐 실제 재생 판정이 아님; DS-6 `8dd593f` |
| 4. 업로드 후 현재 처리 단계·예상 대기 시간 | 실제 업로드 1회 | 충족 | `OMRProcessingStatus.tsx`, `OMRUploadForm.tsx`; 관련 Jest 업로드/처리 상태 스위트 PASS |
| 5. 처리 중 이탈해도 계속된다는 문구 | 관측 가능 | 충족 | `OMRProcessingStatus.tsx`의 고정 문구 `이 페이지를 닫아도 계속 처리됩니다.`; Jest 전체 PASS |
| 6. 첫 플레이어 진입 후 재생·속도 조절 도달 | 1차 컨트롤 구성 확인 | 충족 | `PlaybackControls.tsx`, `FallingNotesPlayer.tsx`; 관련 Jest 및 전체 테스트 PASS |
| 7. 홈 → 업로드 → 이탈 → 완료 → 첫 재생 | 종단 수동 1회 | 사용자 확인 대기 | 사용자가 로그아웃 홈 진입부터 완료 후 첫 재생까지 아래 체크리스트를 1회 수행해야 함. 자동 검사로 대체하지 않음 |

## 사용자 수동 체크리스트

### 조건 7 — 종단 여정

1. 로그아웃한 시크릿 창에서 홈에 진입한다. 히어로와 `내 악보로 시작하기`, `PDF → 변환 → 시각적 연습` 3단계를 본다.
2. 홈의 낙하 노트 결과 영역이 첫 화면에 확보되어 있는지 확인한다.
3. `내 악보로 시작하기`를 누른다. 로그인 화면의 세 가지 이유와 원래 행동 복귀가 보이는지 확인한다.
4. 로그인하고 업로드 화면으로 돌아온다. PDF 드롭존과 메타데이터 입력이 보이는지 확인한다.
5. PDF를 올리고 변환을 시작한다. 처리 단계와 `보통 1~3분` 안내가 보이는지 확인한다.
6. 처리 중 다른 페이지로 이동한다. 업로드 화면을 떠나도 처리가 계속된다는 계약을 확인한다.
7. `내 악보`에서 같은 곡을 찾는다. 완료 배지(연습 가능)가 표시되는지 확인한다.
8. 같은 곡의 연습 진입점을 누른다. 플레이어가 열리고 별도 안내 없이 재생·속도 조절 컨트롤이 보이는지 확인한다.
9. 재생을 한 번 실행한다. 낙하 노트와 피아노 반응이 시작되면 조건 7을 통과로 기록한다.

### 조건 3 — 익명 공개 체험

1. 로그아웃한 시크릿 창에서 탐색으로 이동한다.
2. 공개 악보 카드를 선택해 상세 화면을 연다.
3. 상세 화면에서 로그인 없이 미리보기가 표시되는지 확인한다.
4. 재생을 누르고 실제 오디오와 낙하 노트가 시작되는지 확인한다.
5. 미리보기 후 로그인하고 같은 곡 상세로 복귀하는지 확인한다.

## 완료 판정

DS-7 Status는 `IN_REVIEW`다. 조건 3과 조건 7의 사용자 확인 기록, PR CI·리뷰 결착이 모두 남은 뒤에만 `DONE`으로 전환한다.

