# 2026-09-19 — #134 m3 둘잇단

## 상태와 범위

IN_PROGRESS. 구현·전체 검증·PR 미완료. 원격 main ff88b8d와 동기화한 뒤 요청한 작업 브랜치를 생성했다.
사용자 history 메모 SHA256 `36207439c88183b72476b4c98aa6962ec9a5b4439f4837711699e5d177f8a947` 보존.
로컬 Docker/수정/검증/PR까지만 승인됐으며 병합·VM·유료 서비스는 제외한다.

## 새 기준선 실행

보존 이미지 `clairkeys-omr:d069b-patched` (기록 index ba44da315a7a80d04252517ced3f8e266a43e7647c91abb47fe1b8c1f6526bd9).
`docker run --rm --platform linux/amd64 --memory=5g`에 scores read-only, 이번 산출물 /work 마운트,
`cd /app && PYTHONPATH=/app python3 /work/run_case.py /scores/Clair_de_Lune_easy_300dpi.pdf /work/baseline-clair`.
실제 wrapper와 converter를 실행했다. 25.092초, 자식RSS1,024,060KiB, raw191/canonical160,
점116, tempo69,65.217392초. 전체 원본 기준표 재평가187/191, 타이30/43.
m3 목표는 C5 onset1.5/3.75, E5 onset2.25/3.0의 각0.75박이며 현재는0.5박과 앞당겨진 onset이다.

## 원본·단계·소스 대조

- 원본2480×3508 이미지에서 두 숫자2가 각각 두 음 빔 위에 있다. 첫2의 바닥 획과 둘째2의 상단이 슬러와 연결된다.
- 같은 이미지의 Audiveris를 새로 `-batch -save -step CURVES|SYMBOLS|LINKS`로 각 실행했고 모두exit0.
- CURVES에 숫자 잉크가 긴 곡선 glyph에 포함된다. SYMBOLS에는 둘잇단/운지/숫자2 text inter가 없다.
  해당 지역 SYMBOL glyph는 둘째2 옆1×5 및1×1 조각뿐이다. 첫 빔855, 둘째857과 각각 두 화음은 보존된다.
- 고정 upstream commit9e1e55cd의 Shape는 TUPLET_THREE/SIX만 지원한다. TupletInter.getFactor는2/3와4/6,
  TupletsBuilder.expectedCount는3/6만 지원한다. 인식 잉크 손실과 리듬 표현 미지원이 모두 존재한다.
- 원본의 좁은 숫자 crop을 기존 classifier에 넣는 진단은 안정적인DIGIT_2를 주지 않았다.
  내장Bravura/Leland 숫자 template와 곡선 마스크 비교를 실험 중이며 아직 런타임 정책으로 채택하지 않았다.

## 동작 변경 전 회귀

`omr-service/tests/DupletFixture.java`, 커밋cdb43d9. logical shape(physical label 순서 유지),3/2 배율,
expectedCount2, 기존triplet 배율을 확인한다. baseline normal/recovery 모두
`IllegalArgumentException: No enum constant org.audiveris.omr.glyph.Shape.TUPLET_TWO`로 실패했다.
JDK25 javac에 baseline jars를 classpath로 주고 각 엔진 내장java로 실행했다.
초기fixture의 isPhysical API 오타와 진단 이미지TYPE_BYTE_GRAY 오류는 바로잡고 재실행했으며 이를 기준선 실패로 세지 않는다.

## 근거 위치와 남은 검증

Git 제외 `local-test-data/results/issue134-m3-duplet-2026-09-19/`: baseline-clair의선택MXL/evaluation/events/animation,
각stage로그/OMR, classifier로그, template진단, baseline-fixture.log. 원본PDF/이미지/OMR은Git에 넣지 않는다.
엔진 구현, 정상·부정fixture, 실제Dockerfile전체빌드,source/class양쪽무결성,이미지전체/native,
Clair3회와타이개별목록,corpus12쌍,타입/lint/Jest,자체리뷰,PR최신CI/실제리뷰가 남았다.
다른 타이 원인과D-065후속을 섞지 않는다. #134 전체DONE이 아니다.

## 후보 구현과 실험 (1f4816c)

- D-070과 phase 계획을 먼저 적은 뒤 엔진 내부 후보를 구현했다. logical shape 추가는 physical label 뒤이며,
  두 음의3/2배율은 기존 tuplet 관계와 exporter를 거친다. 마디/곡/좌표·음표 사후 보충은 없다.
- 원본 두 후보의 좁은 classifier crop과 Leland/Bravura/Primus/Serif 실험은 첫2를7과 안정적으로 구별하지 못했다.
  최종 후보는 Leland와 DejaVu Sans Italic의 모든숫자0–9를 동등하게 비교한다. 곡선 양옆run을 보간해 획만 제외하고,
  정확한 픽셀/1px거리 일치와 설명되지 않은 잉크를 점수에 반영한다. 임계값 하향으로 원본을 강제 수용하지 않았다.
- 초기 후보는 LINKS에서 박자표를 얻지 못해 아무것도 하지 않았다. PageRhythm의 박자표 배정 직후로 이동했다.
  첫 실험은 오래된 로컬 SymbolsLinker 소스를 사용해D-068을 누락했고184/191이었다. 이 실험은 폐기하고,
  저장소patch0008을 고정 원본에 적용해 바로잡았다. 최종 수정에는 SymbolsLinker 변경이 없다.
- native fixture의 기존sextuplet 검사는 약분된num/den 대신actualNum/actualDen을 확인하도록 바로잡았다.
  기준선의TUPLET_TWO없음 실패는 유효하며, 수정 엔진에서 triplet/sextuplet 보존도 통과했다.
- exp6 실제 wrapper 변환은 **191/191·타이31/43·오검출0**,25.241초/RSS1,055,476KiB.
  raw191/canonical160/점116/tempo69/65.217392초 유지. 평가객체에서 달라진 마디는3뿐이다.
  새 곡선 검출을 의미하지 않는다. 기존E5타이 시작이2.0→2.25박으로 맞아 누락/오검출 각1개가 줄었다.
- m3 남은타이는G4(MIDI67,staff2,onset0),A4(69,1,1.5),C5(72,1,3.75)이다.
  다른마디 누락9개는 그대로여서 전체누락12개다. 다른원인의타이해결은 이번범위가 아니다.
- exp6 normal/recovery 모두 factor검사와2글꼴×3크기×숫자/곡선·가림·운지204사례+박자24사례를 통과했다.
  이후 작은장식음/기존tuplet/없는source guard·가시잉크만의거리계산·화음geometry11사례를 추가했으며 정식빌드에서 검증 중이다.
- 실제 `docker build --platform linux/amd64 -f omr-service/Dockerfile.audiveris -t clairkeys-omr:d070-patched omr-service` 진행 중.
  소스8개checksum과 두엔진 동일 적용,fonts-dejavu-core 명시 설치를 포함한다. 빌드만으로 전체검증을 주장하지 않는다.
- 타입검사와lint 통과. 첫전체Jest는1037/1038: Dockerfile 정적테스트가 `rm -rf /tmp/jdk25`문자열을 요구했는데
  새cleanup 경로를 앞에 둬 실패했다. jdk25를 다시 첫경로로 유지했고 전체재실행 예정이다. 기존실패로 분류하지 않는다.
- main 상태기록43d1a91 push 직후4개queued를 확인했다. 후속CI확인은 아래 기록으로 갱신한다.

## 정식 전체 빌드·무결성·테스트

- 소스head1f4816c. 실제Dockerfile전체빌드 성공, tag `clairkeys-omr:d070-patched`.
  index `fecb4f29d9b08054e9b74ac2801ef19a21e1848ca333837c9912c10feff05863`,
  amd64 manifest `631cfe2336de9877aab379d2c16212e4d3636e2dc9b0c2a838b6bc16ca07fc2d`,
  config `620ceb6021d30967e529ee1f66e6bc58f7e4a90845443f3ca2e79f9e0a8ac496`.
- 신규upstream8소스와 기존패치의고정checksum 확인·patch 적용·compile 성공. 내장Dockerfile/Java/Python/patch소스
  전체해시가현재작업파일과같다(`integrity-patched.json`). 코드가없는실험이미지를정식빌드로표현하지않는다.
- baseline대비JAR변경24항목은8개수정클래스의내부클래스와신규DupletBuilder/Match뿐이다.
  24개는normal/recovery서로같다. 두엔진사이유일한차이는원래의LedgersPostAnalysis3클래스다.
  기존dot/tie/beam/shared-stem클래스,분류모델등다른JAR항목모두기준선과같다.
- dpkg패키지이름/버전은기준선과동일하다. DejaVu는이미간접설치돼있었고이번에명시의존성으로만고정했다.
  글꼴바이너리해시도같으며fontconfig`.uuid`3개만다르다. 모델/폰트교체로개선을만든것이아니다.
- 이미지전체 `python3 -m unittest discover -s tests -v`: **189개중183통과·6진단skip**,276.558초.
  새duplet을포함한**native12개모두실행·통과**. 새fixture는각엔진factor/기존triplet,
  204잉크+24박자+11화음geometry사례를실행했다. skip6개는local진단산출물이이미지에없기때문이며native skip은없다.
  명령은5GB amd64컨테이너에fixtures/src를read-only마운트하고`ln -s /app /omr-service`후위unittest를실행했다.
- `npx tsc --noEmit`, `npm run lint` 통과. cleanup정적계약수정후전체Jest **105suites/1038tests통과**,25.67초.
  PATH앞에기존격리`issue134-chord-split-2026-09-19/ci-venv/bin`을두어requirements-ci의존성을사용했다.
- 첫상태기록43d1a91의정확한SHA check-runs6개모두성공.95f964c는4개성공/build·E2E진행중을확인했다.
- 최종이미지Clair3회와12곡fresh기준선/패치쌍은`run_gates.py`에서OMR한개씩순차진행중이다.
  무결성/단위테스트완료는이전체변환/corpus/PR게이트를대체하지않는다.

## 최종 이미지 Clair3회와 개별 비교

세 실행 모두 **191/191·타이31/43·누락12·오검출0**. raw event SHA256은 모두
`2a0e5b81a5373695efe4f9659359cc7c2ac0b88af972cb80512ac4dd8f925390`이다.
선택MXL을원본191이벤트전체로평가했고모든음표/쉼표를quarter단위로정규화해전필드를비교했다.

| 실행 | 초 | 자식최대RSS KiB |
|---|---:|---:|
| clair-1 | 26.325 | 1031908 |
| clair-2 | 27.332 | 1037844 |
| clair-3 | 26.413 | 1051584 |

- 변경음은m3 C5@1.5·E5@2.25·E5@3·C5@3.75의4개뿐이다. 각각duration0.5→0.75,
  E5두음과마지막C5의onset도원본에맞아졌다. time-modification은actual-notes2/normal-notes3이다.
- MusicXML divisions가2→4여서음표/쉼표204개의duration정수는전부바뀐다. 이는단위표현이며,
  나누어비교하면m3목표4개외의음가/시작박은모두동일하다. 음높이·점116·성부·보표·타이표식·박자표·쉼표보존.
- raw191음/canonical160음,tempo69/시작위치0,전체65.217392초및모든마디시작위치가같다.
  canonical변경은3개: index21 C5 duration0.434783→0.652174;
  index22 타이로합친E5 start9.565217→9.782609/duration0.869566→1.304348;
  index24 C5 start10.434783→11.086957/duration0.434783→0.652174. 다른157개전필드동일.
- m3밖평가객체도완전히같다.52개곡선의좌표/방향/tie flag와glyph bounds/run pixels를각3회baseline과비교해모두같다.
  타이검출이새로늘었다는의미가아니라기존E5타이의시작박2.0→2.25가맞아누락1/오검출1이해소된것이다.
- 남은타이개별목록 `(MIDI,staff,onset quarter)`:
  m3 `(67,2,0),(69,1,1.5),(72,1,3.75)`;m6 `(64,1,3)`;
  m9 `(52,2,1),(55,2,1),(72,1,3),(76,1,3)`;m10 `(74,1,1.5)`;
  m12 `(53,2,0)`;m13 `(55,2,0)`;m14 `(50,2,0)`. 오검출목록은빈배열이다.
- `evaluate_clair.py`가정규화4개변경/다른마디불변/타이목록/3회해시동일을단언했고,
  `compare_slurs.py`가52개곡선/glyph/tie flags동일을단언했다.상세객체는clair-comparison.json/slur-comparison.json.
- 실행중컨테이너를이번/work마운트로식별해memory5368709120,concurrency1,timeout900,Audiveris1개를확인했다.
  한차례이미종료된cid의inspect는실패했고새cid를검증해바로잡았다.성능개선benchmark로해석하지않는다.

## 최종diff 직접 자체리뷰 (1f4816c, corpus 진행 중)

- 코드/fixture/Dockerfile/patch전체diff를직접읽고upstream의TupletInter/TupletsBuilder/ChordTupletRelation/
  PageRhythm호출순서와대조했다.숫자shape는physical목록뒤에추가하며기존모델JAR항목은바이트동일하다.
- 인식은원본잉크에근거하고곡명/마디/페이지좌표를사용하지않는다.후처리XML/JSON보정과새음표생성은없다.
  애매한숫자는다른9개숫자와비교해거부하고가시성·미설명잉크·박자·두음빔·정확한화음링크를함께확인한다.
- 기존tuplet에속한화음/작은장식음/점·혼합음가/cross-staff/오선내영역/이미지밖/없는binary는처리하지않는다.
  새tuplet관계만추가하고원래curve/head/beam을수정하지않는다.원본에없는마디음가사후보충도없다.
- 검색은고정된interline영역과숫자/글꼴/크기범위로한정된다.기존서비스의concurrency/timeout/heap/메모리/복구선택은변경하지않는다.
  normal/recovery클래스동일과OMR저장후meter재시도재로딩을실제3회에서확인했다.
- 남은한계: IL8–31·복합분모8·단일빔의점없는8분두음만지원한다.완전히같은위치/잉크의중앙운지2는보편구별불가이며
  문맥제한으로통제한다.다른글꼴의추가부정진단과12곡corpus를끝내기전최종회귀통과로선언하지않는다.
- 현재실행근거에서미해결구현결함은관찰되지않았다.이자체리뷰는PR실제리뷰를대체하지않는다.
