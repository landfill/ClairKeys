# 2026-09-19 — #134 m3 둘잇단

## 상태와 범위

IN_PROGRESS. 구현·전체 검증·PR 미완료. 원격 main ff88b8d와 동기화한 뒤 요청한 작업 브랜치를 생성했다.
사용자 history 메모 SHA256 `36207439c88183b72476b4c98aa6962ec9a5b4439f4837711699e5d177f8a947` 보존.
로컬 Docker/수정/검증/PR까지만 승인됐으며 병합·VM·유료 서비스는 제외한다.

## 새 기준선 실행

보존 이미지 `clairkeys-omr:d069b-patched` (기록 index ba44da315a7a80d04252517ced3f8e266a43e7647c91abb47fe1b8c1f6526bd9).
`docker run --rm --platform linux/amd64 --memory=5g`에 scores read-only, 이번 산출물 /work 마운트,
`cd /app && PYTHONPATH=/app python3 /work/run_case.py /scores/Clair_de_Lune_easy_300dpi.pdf /work/baseline-clair`.
실제 wrapper와 converter를 실행했다. 25.092초, 자식RSS1,024,060KiB, raw191/canonical160,
점116, tempo69,65.217392초. 전체 원본 기준표 재평가187/191, 타이30/43.
m3 목표는 C5 onset1.5/3.75, E5 onset2.25/3.0의 각0.75박이며 현재는0.5박과 앞당겨진 onset이다.

## 원본·단계·소스 대조

- 원본2480×3508 이미지에서 두 숫자2가 각각 두 음 빔 위에 있다. 첫2의 바닥 획과 둘째2의 상단이 슬러와 연결된다.
- 같은 이미지의 Audiveris를 새로 `-batch -save -step CURVES|SYMBOLS|LINKS`로 각 실행했고 모두exit0.
- CURVES에 숫자 잉크가 긴 곡선 glyph에 포함된다. SYMBOLS에는 둘잇단/운지/숫자2 text inter가 없다.
  해당 지역 SYMBOL glyph는 둘째2 옆1×5 및1×1 조각뿐이다. 첫 빔855, 둘째857과 각각 두 화음은 보존된다.
- 고정 upstream commit9e1e55cd의 Shape는 TUPLET_THREE/SIX만 지원한다. TupletInter.getFactor는2/3와4/6,
  TupletsBuilder.expectedCount는3/6만 지원한다. 인식 잉크 손실과 리듬 표현 미지원이 모두 존재한다.
- 원본의 좁은 숫자 crop을 기존 classifier에 넣는 진단은 안정적인DIGIT_2를 주지 않았다.
  내장Bravura/Leland 숫자 template와 곡선 마스크 비교를 실험 중이며 아직 런타임 정책으로 채택하지 않았다.

## 동작 변경 전 회귀

`omr-service/tests/DupletFixture.java`, 커밋cdb43d9. logical shape(physical label 순서 유지),3/2 배율,
expectedCount2, 기존triplet 배율을 확인한다. baseline normal/recovery 모두
`IllegalArgumentException: No enum constant org.audiveris.omr.glyph.Shape.TUPLET_TWO`로 실패했다.
JDK25 javac에 baseline jars를 classpath로 주고 각 엔진 내장java로 실행했다.
초기fixture의 isPhysical API 오타와 진단 이미지TYPE_BYTE_GRAY 오류는 바로잡고 재실행했으며 이를 기준선 실패로 세지 않는다.

## 근거 위치와 남은 검증

Git 제외 `local-test-data/results/issue134-m3-duplet-2026-09-19/`: baseline-clair의선택MXL/evaluation/events/animation,
각stage로그/OMR, classifier로그, template진단, baseline-fixture.log. 원본PDF/이미지/OMR은Git에 넣지 않는다.
엔진 구현, 정상·부정fixture, 실제Dockerfile전체빌드,source/class양쪽무결성,이미지전체/native,
Clair3회와타이개별목록,corpus12쌍,타입/lint/Jest,자체리뷰,PR최신CI/실제리뷰가 남았다.
다른 타이 원인과D-065후속을 섞지 않는다. #134 전체DONE이 아니다.
