# container-clairkeys-omr-prod.service
# autogenerated by Podman 4.4.1
# Sun Aug 23 18:36:54 KST 2026

[Unit]
Description=Podman container-clairkeys-omr-prod.service
Documentation=man:podman-generate-systemd(1)
Wants=network-online.target
After=network-online.target
RequiresMountsFor=%t/containers

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
Restart=always
TimeoutStopSec=70
ExecStart=/usr/bin/podman run \
	--cidfile=%t/%n.ctr-id \
	--cgroups=no-conmon \
	--rm \
	--sdnotify=conmon \
	--replace \
	-d \
	--name clairkeys-omr-prod \
	-p 0.0.0.0:3000:8000 \
	--env-file /etc/clairkeys-omr.env \
	-v /data:/data clairkeys-omr:current
ExecStop=/usr/bin/podman stop \
	--ignore -t 10 \
	--cidfile=%t/%n.ctr-id
ExecStopPost=/usr/bin/podman rm \
	-f \
	--ignore -t 10 \
	--cidfile=%t/%n.ctr-id
Type=notify
NotifyAccess=all

[Install]
WantedBy=default.target
